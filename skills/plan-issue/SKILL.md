---
description: IssueからPlanサブエージェントで実装プランを作成し、レビュー基準に基づくセルフレビューと要確認事項の質問までを自動化する。Issue実装プランの作成、技術設計とタスク分解の計画立案に使用。「実装計画を立てて」「プランニングして」「タスク分解して」「設計を整理して」「Issueの計画を作って」といった依頼時にも使用
allowed-tools:
  - Read
  - Grep
  - Glob
  - Edit
  - Write
  - Bash(gh issue view*)
  - Agent
  - AskUserQuestion
---

# プラン作成（Plan Issue）

Issueの実装プランを作成し、`docs/plans/issue-{番号}.md` に保存する。
Planサブエージェントでプラン作成 → セルフレビュー → 修正のループを最大2周実行し、要確認事項をユーザーに質問する。

## 引数

- `$ARGUMENTS`: Issue番号またはURL。補足指示（オプション）
  - 例: `21`, `https://github.com/owner/repo/issues/21`, `21 テストは不要`
  - `--input <path>`: JSONファイルからIssue情報を読み込む（`gh issue view` をスキップ）

GitHub URLが含まれる場合、Issue番号を抽出する。番号以降のテキストは補足指示として扱う。

`--input` とIssue番号が同時に指定された場合は `--input` を優先する。

## 手順

### 1. 引数の解析

`$ARGUMENTS` から以下を分離する:

- **`--input` あり** → 固定入力モード（JSONファイルパスを取得）
- **Issue番号あり** → 通常モード
- 上記以外のテキスト → 補足指示

### 2. レビュー基準の読み込み

以下の順序でReadツールで読み込み、両方の内容をマージしてレビュー基準とする:

1. **デフォルト（必須）**: このスキルの `references/review-guide-default.md`
2. **プロジェクト固有（任意）**: `{プロジェクトルート}/.claude/plan-issue/review-guide.md`

デフォルトは常に読み込む。プロジェクト固有ファイルが存在する場合（Readが成功した場合）、その内容を追加のレビュー観点として加える。

### 3. Issue情報の取得

**固定入力モード（`--input`）:**

ReadツールでJSONファイルを読み込む。`gh` コマンドは実行しない。

**通常モード:**

```bash
gh issue view {issue_number} --json number,title,body,labels,comments
```

### 4. Planサブエージェントによるプラン作成

このスキルの `references/plan-prompt.md` をReadで読み込み、プロンプトを構築する。

**プロンプト構築:**

```
{plan-prompt.md の内容}
```

`{OUTPUT_FORMAT}` プレースホルダを本スキルの「出力フォーマット」セクションのMarkdownテンプレートで置換する。

その後、以下を追加する:

```
## Issue情報

{gh issue viewの結果}

## 補足指示

{補足指示（あれば）}

実行した全てのBashコマンドとツール呼び出しを、実行順に「実行ログ」セクションとして最終出力に含めること。
```

**サブエージェントの起動:**

Agent tool（`subagent_type: Plan`）でプラン作成を実行する。モデルは親と同じ（`inherit`）。

### 5. プランファイルの保存

サブエージェントの結果から「実行ログ」セクションを除去し、`docs/plans/issue-{番号}.md` にWriteで保存する。

### 6. セルフレビュー → 修正ループ（最大2周）

ステップ2で読み込んだレビュー基準に基づいて、プランファイルをレビューする。

**レビュー手順:**

1. `docs/plans/issue-{番号}.md` をReadで読む
2. レビュー基準の各観点でプランを評価する
3. 問題がある場合、Editでプランファイルを直接修正する
4. 修正後、再度レビューする（最大2周）

**ループ終了条件:**

- 指摘なし → ステップ7へ
- 2周で収束しない → 根本的な問題としてステップ7で質問する

### 7. 要確認事項の質問

以下の場合、`AskUserQuestion` でユーザーに質問する:

- セルフレビューで解決できない問題がある場合
- Issueの要件が曖昧でプランの品質を担保できない場合
- 設計上の選択肢があり、判断が必要な場合

質問がない場合は、プランファイルのパスを報告して完了する。

## 出力フォーマット

`docs/plans/issue-{番号}.md` に以下の形式で出力する:

```markdown
# 実装プラン: Issue #{番号} {タイトル}

## 概要

- **Issue**: #{番号}
- **スコープ**: [1-2文で要約]

## 技術設計

### 設計上の変更ポイント

**外部IO**（該当する場合）:
- [API/DB/ファイルシステム/外部サービスとの接点の変更]

**ビジネスロジック**（該当する場合）:
- [ドメインルール・バリデーション・状態遷移の変更]

### データフロー（該当する場合）

[入力→処理→出力の流れ]

### DBスキーマ変更（該当する場合）

| テーブル | 操作 | 内容 |
|---|---|---|
| `table_name` | 追加/変更/削除 | [カラム定義・制約・インデックス等] |

- **マイグレーション方針**: [破壊的変更の有無、ダウンタイムの考慮、ロールバック手順]

### API変更（該当する場合）

| エンドポイント | メソッド | 操作 | 内容 |
|---|---|---|---|
| `/api/path` | GET/POST/PUT/DELETE | 追加/変更/削除 | [概要] |

- **リクエスト型**: [主要フィールドと型]
- **レスポンス型**: [主要フィールドと型]

### エラーハンドリング（該当する場合）

[方針]

### 新規依存ライブラリ（該当する場合）

| ライブラリ | 用途 | 選定理由 |
|---|---|---|
| `package-name` | [何に使うか] | [代替案との比較・選定の根拠] |

### 変更対象ファイル

| ファイル/モジュール | 操作 | 変更内容 |
|---|---|---|
| `path/to/file` | 新規/修正/削除 | [概要] |

### テスト戦略

- [テストレベル別の方針（ユニット/統合等）]

## 参照ドキュメント（該当する場合）

- `path/to/document` - [ドキュメントの種類・概要]
- [URL](https://...) - [関連Issue・Wiki・外部仕様書等の概要]

## タスク分解

### Task 1: {タスク名}
- **ファイル**: `path/to/file`
- **内容**: [具体的な実装内容]
- **テスト**: [テスト観点]
- **完了条件**: [検証手段]
- **依存**: なし / Task N

### Task 2: {タスク名}
...

（各タスク ≒ 1コミットの粒度）
```
