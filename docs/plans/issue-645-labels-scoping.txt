1:[PASS] 01. validate valid → exit 0
2:[PASS] 02. validate missing-id → exit 1、CASE-A2 の対応行が無いことを診断として告げ、違反1件と数える
3:[PASS] 03. validate missing-origin → exit 1、CASE-A2 と由来の未記入を告げる
4:[PASS] 04. validate missing-asset-type → exit 1、CASE-A2 と資産種別の未記入を告げる
5:[PASS] 05. validate missing-carrier → exit 1、CASE-A2 と担い方の未記入を告げる
6:[PASS] 06. validate missing-layer → exit 1、CASE-A2 と3層の欠落を告げる
7:[PASS] 07. validate no-template → exit 0（prompt-template.md の欠落は validate では見ない）
8:[PASS] 08. validate（題材集合ディレクトリの省略）→ exit 2、使い方を示す
9:[PASS] 09. validate（存在しない題材集合ディレクトリ）→ exit 2、ディレクトリが存在しないことを理由として告げる
10:[PASS] 09a. validate/prompt（題材集合ディレクトリ名が - で始まる）→ ファイル引数をオプションと読まずに通す
11:[PASS] 09b. validate/report/prompt（題材集合ディレクトリ名・判定記録TSV 名が var=value 形）→ awk が変数代入と読まず、偽の違反も原因の取り違えも出さない
12:[PASS] 09d. validate/report/prompt（引数が空文字列）→ カレントディレクトリを既定値にせず、指定されていないことを名指しする
13:[PASS] 09c. validate（題材集合ディレクトリを読めず辿れもしない）→ 辿れないことを名指しする（ファイルの欠落へ化けない）
14:[PASS] 09e. validate（題材集合ディレクトリを読めるが辿れない）→ 辿れないことを名指しする（ファイルの欠落へ化けない）
15:[PASS] 09f. validate（題材集合ディレクトリが実行のみ可）→ 動く構成を弾かずに検査を通す
16:[PASS] 10. 題材集合ディレクトリの切り替えが効いている（valid で 0・missing-id で 1。本番の題材集合を見ていない）
17:[PASS] 11. prompt valid CASE-A1 → exit 0、標準出力は実在するファイルのパス1行
18:[PASS] 12. 組み立てたプロンプトに CASE-A1 の題材文が含まれる
19:[PASS] 13. 組み立てたプロンプトに期待帰結層・他題材に由来する文字列が含まれない
20:[PASS] 14. prompt（題材集合に無い題材ID）→ exit 1、既知の題材IDを列挙する
21:[PASS] 14a. prompt（題材IDに正規表現メタ文字を含む）→ exit 1、題材IDが題材集合に無いことを告げる（別の診断へ化けない）
22:[PASS] 15. prompt（存在しない対象文書）→ exit 2
23:[PASS] 16. prompt（題材集合ディレクトリの省略）→ exit 2、使い方を示す
24:[PASS] 17. prompt no-template → exit 2、prompt-template.md を欠けているファイルとして告げる
25:[PASS] 18. report valid-judgments.tsv valid → exit 0
26:[PASS] 19. report 出力がセル単位の試行間一致件数を含む
27:[PASS] 20. report 出力が項目別一致率を4項目分含む
28:[PASS] 21. report 出力が各項目の1点率（周辺分布）を試行ごとに含む
29:[PASS] 22. report 出力が期待帰結との差として CASE-A2 の項目3 の差を含む
30:[PASS] 22a. report 出力がカバレッジ件数行を含む
31:[PASS] 22b. report 出力が合計列・行き先列の試行間一致率を含む
32:[PASS] 22c. report 出力が期待帰結との差として合計列の差と差の総数を含む
33:[PASS] 23. report unknown-id-judgments.tsv → exit 1、CASE-ZZ を列挙する
34:[PASS] 24. report missing-case-judgments.tsv → exit 1、未カバー題材 CASE-A2 を列挙する
35:[PASS] 25. report（存在しない判定記録TSV）→ exit 2、判定記録TSV が存在しないことを理由として告げる
36:[PASS] 26. report（題材集合ディレクトリの省略）→ exit 2、引数の個数が合わないことを理由として告げる
37:[PASS] 27. サブコマンド無し → exit 2、使い方を示す
38:[PASS] 28. 不明なサブコマンド → exit 2、受け取ったサブコマンド名を添えて理由を告げる
39:[PASS] 29. validate duplicate-id → exit 1、題材IDの重複を CASE-A1 として告げる
40:[PASS] 30. validate total-mismatch → exit 1、CASE-A2 の期待_合計が項目1〜4 の和と一致しないことを告げる
41:[PASS] 31. validate unknown-partner → exit 1、どの題材の対の相手IDが題材集合に無いのかを告げる
42:[PASS] 32. validate asymmetric-pair → exit 1、対の相手IDが相互参照になっていないことを告げる
43:[PASS] 33. validate invalid-origin → exit 1、CASE-A2 の由来が語彙外であることを告げる
44:[PASS] 34. validate invalid-carrier → exit 1、CASE-A2 の規範の担い方が語彙外であることを告げる
45:[PASS] 35. validate no-header → exit 1、expectations.tsv のヘッダ行の欠落を告げる
46:[PASS] 36. report duplicate-judgments.tsv → exit 1、CASE-A1 の重複した行を告げる
47:[PASS] 36a. report（commit 列にプレースホルダが残る記録）→ exit 1、14列目・15列目それぞれを題材・試行・列名で名指しする
48:[PASS] 37. validate no-case-heading → exit 1、題材が1件も無いことを診断として告げる（無言で落ちない）
49:[PASS] 38. validate meta-inside-body → exit 1、メタ行が題材文ブロックの内側にあることを CASE-A1 として告げる（あわせて valid の prompt にメタ行が漏れない）
50:[PASS] 39. prompt（対象文書パスに & と \ を含む）→ 差し込み記号を再挿入せず、パスをそのまま載せる
51:[PASS] 40a. prompt/validate/report（$TMPDIR に it's を含む）→ exit 0、trap が壊れず題材文・期待帰結も読める
52:[PASS] 40b. prompt/validate/report（$TMPDIR に back\slash を含む）→ exit 0、trap が壊れず題材文・期待帰結も読める
53:[PASS] 40c. report（題材集合ディレクトリのパスに & と \ を含む）→ 期待帰結を読み、差2件をそのまま出す
54:[PASS] 40d. validate（題材集合ディレクトリのパスに & と \ を含む）→ exit 0
55:[PASS] 38a. validate（メタ行が内側にあり題材文本文がパイプ長を超える）→ exit 1、内側にあることを告げる
56:[PASS] 38b. validate（題材文が空）→ exit 1、3層のうち題材文が欠けていることを告げる
57:[PASS] 38c. prompt（題材文が空）→ exit 1、題材文が空であることを告げる
58:[PASS] 38d. validate（雛形が {{題材文}} を欠く）→ exit 1、欠けている差し込み記号を名指しする
59:[PASS] 38e. prompt（雛形が {{題材文}} を欠く）→ exit 1、欠けている差し込み記号を名指しする（題材文の無いプロンプトを生まない）
60:[PASS] 38f. prompt（雛形を読めない）→ exit 2、読めないことを名指しする（記号の欠落へ化けない）
61:[PASS] 40e. report（期待帰結を1件も読めない）→ 診断とラベルの両方で理由を告げ、集計本文を1行も印字しない
62:[PASS] 40f. report（期待帰結も題材も0件）→ 素通りせず、「差は無い」も印字しない
63:[PASS] 43a. report（未知の題材IDが複数）→ 診断が記録の出現順で並ぶ
64:[PASS] 43b. report（重複した行が複数）→ 診断が記録の出現順で並ぶ
65:[PASS] 43c. report（commit 列の未確定が複数）→ 診断が記録の出現順で並ぶ
66:[PASS] 43d. validate（対の相手IDが題材集合に無い題材が複数）→ 診断が記載の出現順で並ぶ
67:[PASS] 45. report（試行番号が2桁を含む）→ 試行番号を数値順に並べ、試行1と試行2 を比較する
68:[PASS] 46. prompt 成功時、$TMPDIR に残るのは返したプロンプト1件だけ（作業用の一時ファイルを残さず、返すファイルも消さない）
69:[PASS] 47. report（判定記録TSV を読めない）→ exit 2、判定記録TSV を名指しする（期待帰結の側へ化けない）
70:[PASS] 48. report（集計そのものが異常終了）→ 判定記録TSV を名指しし、期待帰結の打ち切りと取り違えない
71:[PASS] 49. prompt（組み立ての処理が異常終了）→ 原因を名指しして落ち、一時ファイルを残さない
72:[PASS] 50. prompt（題材文の一時ファイルを読めない）→ 原因を1度だけ名指しし、雛形の異常終了へ化けない
73:[PASS] 41. validate（メタ行がパイプ長を超える題材）→ 診断ゼロの異常終了をせず検査を完了する
74:[PASS] 42. 配布物内のスクリプトが配布元リポジトリ固有の名前（ディレクトリ構成・リポジトリ名・対象文書名）を含まない
75:[PASS] 44. 配布物（plugins/adr 配下）が配布元リポジトリの Issue 番号を含まない
