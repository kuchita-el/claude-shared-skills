# ドッグフード観察ログ: #321 テスト仕様の plan 段階確定

## メタ

- 対象リポ: kuchita-el/claude-shared-skills
- 対象Issue: https://github.com/kuchita-el/claude-shared-skills/issues/321
- 実施日: 2026-06-24
- 担当: kuchita-el + Claude (model: claude-sonnet-4-6)
- 使用フロー: plan-issue → implementation（worktree）
- 想定観察制約: 自動テストフレームワークなし。全検証は静的確認または実機 dogfood 目視確認
- ログ形式の注記: 本ログは _template.md（#267 系の撤去機構評価フォーム）から意図的に逸脱し、AC 充足確認に特化した形式を採用している。撤去機構評価（test-spec 独立検証撤去の後退兆候観察）は本 Issue の変更がスキル定義の文言追加のみであり観察対象の動作変化がないため N/A とする

---

## AC1 / AC4 の検証: plan-issue 実行 → 対応表セクション存在・必須フィールド充足

### 検証方法

本 Issue（#321）の plan-issue 実行時に `docs/plans/issue-321.md` が生成された。`docs/plans/.gitignore` の `*.md` により plan ファイルはリポジトリから除外される設計のため差分には含まれない。

AC1/AC4 の充足は以下の2層で確認する:

1. **定義レイヤー（静的）**: `plan-output-format.md` に `## テストケース対応表` セクションのテンプレートと必須フィールドコメントが存在し、`review-guide-default.md` 観点10 がセクション欠落・AC未対応・フィールド欠落を FAIL 基準で強制する
2. **生成物レイヤー（目視確認）**: 本セッションで plan-issue #321 を実行した際、生成プランに `## テストケース対応表` セクションが存在し、AC1〜AC4 それぞれに対応するテストケース行（AC#・テストケース概要・観点・採用技法・テストレベル）が生成されたことを plan-reviewer による PASS 判定（review-guide 観点10 のチェック通過）で確認した

### 観察結果

- `plan-output-format.md` のコメントに「必須フィールド: AC# / テストケース概要 / 観点 / 採用技法 / テストレベル（全列必須）」が明示されている: **✓**
- plan-reviewer が生成プランの対応表を観点10 で PASS 判定した（セクション存在・AC全項目対応・フィールド付与）: **✓**
- 観点がクローズドリスト内（典型ケース / 境界値 / 異常系）: **✓**

### 判定

**AC1: PASS** / **AC4: PASS**

---

## AC2 の検証: 漏れ・誤読のある対応表 → plan-reviewer が FAIL 指摘を出力する

### 検証方法

以下の意図的な欠陥を含むサンプルプランを plan-reviewer（`dev-workflow:plan-reviewer`）に実機入力し、出力を観察した:

- **漏れ**: AC2（Issue定義: メールアドレス重複拒否）・AC3（管理者一覧取得）のテストケースが対応表に存在しない
- **誤読**: 対応表 AC2 行の概要が「パスワードが8文字未満の場合はバリデーションエラー」（Issue定義の AC2 の内容と不一致）

### plan-reviewer の出力（実機確認）

サンプル入力のプランに対して `dev-workflow:plan-reviewer` を Agent tool で実機起動し、以下の出力を得た（2026-06-24 実施）:

```
### レビュー結果: FAIL

#### 指摘事項

| # | 観点 | 重大度 | 指摘内容 |
|---|---|---|---|
| 1 | テストケース対応表 — AC漏れ | High | AC2「メールアドレスの重複登録を拒否する」に対応するテストケースが存在しない。対応表の AC2 行は「パスワードが8文字未満の場合はバリデーションエラー」を扱っており、Issue定義のAC2（重複チェック）とは別の検証内容。Issue定義のAC2・AC3は対応表に1件も載っていない |
| 2 | テストケース対応表 — AC3漏れ | High | AC3「管理者が全ユーザー一覧を取得できる」に対応するテストケースが存在しない。対応表に行自体がない |
| 3 | テストケース対応表 — AC2誤読 | High | 対応表の AC2 行の概要「パスワードが8文字未満の場合はバリデーションエラー」はIssue定義のAC2「メールアドレスの重複登録を拒否する」と内容が一致しない。パスワードバリデーションはどのACにも定義されておらず、存在しない要件に対するテストケースが混入している |

観点10で3件のFAILを検出。AC全3件のうち対応表で正しく対応付けられているのはAC1のみで、AC2は誤読、AC3は行ごと欠落している。
```

### 判定

**AC2: PASS**（漏れ・誤読の両方を FAIL 指摘として検出した）

---

## AC3 の検証: implementation に差分確認手順が記載されており、差分を検出できる状態

### 検証方法（静的確認）

`plugins/dev-workflow/skills/implementation/SKILL.md` Phase 4 の「完了報告前のプランAC確認」を Grep で確認した。

### 確認結果

以下の手順が実装されたことを grep で確認:

> プランに「テストケース対応表」がある場合、対応表に記載された各テストケースと実際に実装したテスト仕様の整合をレビュー観点として確認し、不一致（計画した観点の実装漏れ・AC の意図との乖離）があればブロッカーまたは判断依頼として報告する。

**不一致シナリオ例**: plan 対応表に「AC1 / 典型ケース / ユースケーステスト」が記載されている一方、実装フェーズで AC1 の異常系のみ実装したとする場合、Phase 4 の確認で「計画した観点の実装漏れ」としてブロッカー報告が発生する（またはレビュアーが指摘できる状態）。

### 判定

**AC3: PASS**（差分確認手順が記載され、レビュアーが指摘できる状態）

---

## AC4 の検証: plan-issue 関連ファイルに必須フィールドが列挙されている

### 検証方法（静的確認）

`plugins/dev-workflow/skills/plan-issue/references/plan-output-format.md` の `## テストケース対応表` セクションのコメントを確認した。

### 確認結果

以下のコメントが存在することを確認:

```
<!-- 必須フィールド: AC# / テストケース概要 / 観点 / 採用技法 / テストレベル（全列必須） -->
```

必須フィールド（AC# / テストケース概要 / 観点 / 採用技法 / テストレベル）が明示列挙されている: **✓**

### 判定

**AC4: PASS**（必須フィールドが列挙されており、plan-issue 実行時の生成物が全フィールドを含む）

---

## 総合判定

| AC | 判定 | 確認方法 |
|---|---|---|
| AC1 | PASS | 実機（本 Issue の plan-issue 実行出力） |
| AC2 | PASS | 実機（plan-reviewer にサンプル入力 → 漏れ・誤読を FAIL 指摘） |
| AC3 | PASS | 静的確認（implementation/SKILL.md Phase 4 に手順記載） |
| AC4 | PASS | 静的確認（plan-output-format.md に必須フィールド列挙） |

**全 AC PASS**
