# manage-adr 参照面の再構成（2026-08-28）

`manage-adr` スキルの参照面（`SKILL.md` ＋ `references/` の `.md`）を再構成し、ある規範を改訂するときに触る文書が一意に決まる状態にした作業の記録。Issue #722 の受入条件 AC1・AC3・AC4（検証結果側）・AC7・AC11 の証跡である。

配布物側へ置かないのは、これらがいずれも再構成という一回限りの作業の証跡であり、配布先の利用者が使う規約ではないためである（制約「配布物へ配布元リポジトリの Issue 番号を書き込まない」にも掛かる）。検査項目7件と正本の対応表だけは規約として機能するため、`plugins/adr/skills/manage-adr/references/adr-model.md` 側に置いた。

## 本再構成が従う前提

以降の各節はこの前提のもとで作られている。

- **references 間の参照の作法**: 直接ファイル名で正本を指す。射程は本再構成で新設・改稿する参照に限り、既存の間接名（`transitions.md` の「SKILL.md が指す状態モデルの規約」形ほか）は据え置く
- **参照の深さ**: 参照ファイル間の直接参照を許し、深さの上限を2段とする。すべてのポインタを `SKILL.md` へ集約すると `SKILL.md` が第二の列挙面に戻り、正本の一意性と衝突するため
- **検査項目↔正本の対応表の列構成**: 「検査項目 / 正本（パス＋節名）」の2列とする。是正手段の列を足すと、是正手段が対応表と各正本文書の2箇所に載り、対応表自身が正本の重複を作る
- **`template.md` の帰趨**: 現状の責務（front-matter 雛形＋見出し骨格＋HTML 注釈＋`## 変更履歴` の配置規約）を維持し、値の再掲の縮退のみを行う
- **配送単位**: 単一 PR。各コミット時点で全スイートが緑を保つようタスクを並べた

## 再構成前の参照面9件の帰趨と責務

帰趨の値域は 維持／分割／統合／削除 の4値。本表の対象は**再構成前に存在した9件**（`SKILL.md` ＋ references 8本）であり、新設3本は行を持たない（次節の11件の表に現れる）。

| ファイル | 帰趨 | 再構成後の責務 |
|---|---|---|
| `SKILL.md` | 維持 | 依頼をどの判断へルーティングするかを決め、各判断の正本文書を指す入口として機能する（レイヤの列挙面を持たない） |
| `references/adr-demotion.md` | 維持 | 既存 ADR を退役させるかの格下げ判定の正本（本再構成での変更0件） |
| `references/adr-model.md` | 維持 | ADR のドメインモデルの正本として、状態の型・配置・採番方式・発番スクリプトの契約・lint 検査項目7件と正本の対応表を持つ |
| `references/adr-scoping.md` | 分割 | 行き先と束ねと命名規約を `adr-destination.md` / `adr-splitting.md` へ出し、ADR 化要否の判定コア（格下げ判定との使い分け・必要条件・粒度判定基準・判定する入力の値域と定義・判定に添える申告・起票のタイミングと発見型）のみを持つ |
| `references/cross-references.md` | 維持 | `## 関連ADR` の関係語彙・`Related:` の書式規約・機械検査の範囲と是正手段の正本（相互参照の語彙の正本であることを冒頭で明示する） |
| `references/edit-decision.md` | 分割 | 記録の参照原則を `adr-reference-principle.md` へ出し、core／非core／些末 の分類と操作分岐のみを持つ |
| `references/io-examples.md` | 削除 | 4例すべてが他文書に規範として既述であり、例が正本の重複を作る経路になっていたため責務を持たない |
| `references/template.md` | 維持 | 新規 ADR の生成物の骨格（front-matter 雛形＋見出し骨格）と `## 変更履歴` 節の配置規約の正本（状態値・front-matter の値の再掲は落とす） |
| `references/transitions.md` | 維持 | 5遷移と分割の実行手順・双方向相互参照の書き込み・index の再生成の正本（入出力例と H1 形式の値の再掲は持たない） |

行数は9で、再構成前の参照面のファイル数と一致する。帰趨「統合」に当たるファイル単位の統合は0件である（`transitions.md`「共通: front-matter の書き方」が持つ H1 形式の記述を `adr-model.md` の正本へ縮退させる部分統合は生じるが、ファイルとしての帰趨は維持である）。

## 再構成後の参照面11件

| # | ファイル | 保持する判断 |
|---|---|---|
| 1 | `SKILL.md` | ルーティング |
| 2 | `references/adr-model.md` | 状態モデル・配置・採番・検査項目と正本の対応 |
| 3 | `references/adr-scoping.md` | ADR 化要否の判定コア |
| 4 | `references/adr-destination.md` | 判定結果の行き先・命名規約の ADR 化基準 |
| 5 | `references/adr-splitting.md` | 新規 ADR の束ねの制約 |
| 6 | `references/adr-demotion.md` | 格下げ（退役）判定 |
| 7 | `references/cross-references.md` | 相互参照の語彙・書式・機械検査 |
| 8 | `references/edit-decision.md` | 編集分類（core／非core／些末）と操作分岐 |
| 9 | `references/adr-reference-principle.md` | 記録の参照原則 |
| 10 | `references/template.md` | 生成物の骨格・`## 変更履歴` の配置規約 |
| 11 | `references/transitions.md` | 5遷移と分割の実行手順 |

## front-matter の型と旧表・レイヤ1違反種別の対応

### 構成子 ↔ 再構成前の「遷移ごとの必須ルール」表の行

`adr-model.md` の旧「遷移ごとの必須ルール」表（遷移で索引された5行）を、`## 状態の型` の直和型（5構成子）へ置き換えた。両者は1対1に対応し、対応の付かない行も構成子も0件である。`plugins/adr/scripts/lint-adr.sh`:23-29 が持つ遷移表の5行とも同じ対応で一致する。

| 旧表の行（遷移） | 旧表の値組（status / validity / superseded-by） | 構成子 |
|---|---|---|
| 起票 | 提案中 / （無し） / （無し） | `提案中` |
| 承認 | 承認済み / 有効 / （無し） | `有効` |
| 上書き | 承認済み（不変） / 上書き済み / 必須 | `上書き済み of NonEmptyList<FullSlug>` |
| 廃止 | 承認済み（不変） / 廃止済み / （無し） | `廃止済み` |
| 却下 | 却下 / （無し） / （無し） | `却下` |

行数5・構成子数5。型が新たに許す状態も新たに禁じる状態も生じていない（値組は逐語で保存され、旧表の3つの補足も型の制約・補足として保持した）。

### レイヤ1違反種別8件 ↔ 型の制約

対応表そのものは配布物側（`adr-model.md`「型の制約と機械検査の対応」）に置いた。ここに残すのは、その対応が `plugins/adr/scripts/lint-adr.sh`:31-39 の列挙と過不足なく一致することの照合結果と、既存 fixture による裏づけの有無である。

| # | `lint-adr.sh`:31-39 の違反種別 | 排除する制約 | 専用 fixture |
|---|---|---|---|
| 1 | status 欠落（空） | 制約1（すべての構成子が `status` を伴う） | `invalid/01-status-missing`、`invalid/06-multi-violation` |
| 2 | status=承認済み かつ validity 欠落（空） | 制約2（`承認済み` を伴う3構成子はいずれも `validity` を伴う） | `invalid/02-validity-missing` |
| 3 | validity=上書き済み かつ superseded-by 欠落（空） | 制約5（`上書き済み` の後継は非空リスト） | `invalid/03-superseded-by-missing`、`invalid/06-multi-violation` |
| 4 | status の値が語彙外 | 制約6（値は各軸の値域に限る） | `invalid/12-status-unknown-vocab` |
| 5 | validity の値が語彙外（空は合法） | 制約6（値は各軸の値域に限る） | `invalid/13-validity-unknown-vocab` |
| 6 | status=提案中 または 却下 かつ validity が非空 | 制約3（`validity` を伴わないのは `提案中` / `却下` だけ） | `invalid/14-proposed-with-validity`、`invalid/15-rejected-with-validity` |
| 7 | status=提案中 または 却下 かつ superseded-by が非空 | 制約4（`superseded-by` を伴えるのは `上書き済み` だけ） | **無し**（下記） |
| 8 | validity=有効 または 廃止済み かつ superseded-by が非空 | 制約4（`superseded-by` を伴えるのは `上書き済み` だけ） | `invalid/16-active-with-superseded-by`、`invalid/17-abandoned-with-superseded-by` |

行数8で `lint-adr.sh`:31-39 の列挙と1対1に対応し、対応欄が空の行は0件。制約1〜6 はいずれも少なくとも1件の違反種別へ対応づき、使われない制約も0件である。

**fixture の被覆の欠落（事実の記録。本再構成では埋めない）**: 種別7（`status=提案中` または `却下` かつ `superseded-by` が非空）には専用 fixture が存在しない。`invalid/14`・`invalid/15` は `提案中` / `却下` に `validity` を付けた側（種別6）を、`invalid/16`・`invalid/17` は `有効` / `廃止済み` に `superseded-by` を付けた側（種別8）をそれぞれ固定しており、`提案中` / `却下` に `superseded-by` を付けた組み合わせを固定する fixture はいずれのディレクトリにも無い（`invalid/06-multi-violation` は種別1 と種別3 を持つ）。fixture の追加は本再構成の対象外である（テストの追加は IN に含まれない）。この欠落は後掲「レビュー指摘への対応」のとおり #793 が引き取る。

**型化が合法集合を動かしていないことの担保**: 既存 fixture（`valid` 8種・`invalid` 28種）は本再構成で1件も変更しておらず、`lint-adr.sh` も変更していないため、判定結果は文書編集の前後で必然的に同一である。したがって fixture は「lint の挙動を意図せず動かしていないこと」までを担保し、型化の正しさは上の2表（構成子↔旧表5行、違反種別8件↔制約）の突き合わせが担保する。

## 3語彙群の出現箇所一覧

**走査面**: 再構成後の参照面11件（`SKILL.md` ＋ references 10本）。同梱スクリプト（`lint-adr.sh` / `gen-adr-index.sh` / `next-adr-id.sh`）は走査面に含めない。実装が自らの検査内容を説明できなくなり、`lint-adr.sh` 冒頭のレイヤ1違反種別の列挙を参照先として要求する側の照合と衝突するためである（同趣旨の裁定が `adr-cross-reference-lint-crosscheck.md` に既にある）。

**出現箇所の単位**: 当該語彙群の値を**列挙または規定する**箇所を1件とする。単一の値を手順文の中で使うだけの行（「`status: 承認済み` と書く」等）は、規約の再定義に当たらないため個別に列挙せず、群ごとに件数で示す。これらはすべて「適用手順のみを述べる記述」に分類される。

ラベルの値域は「正本／正本へのリンク／正本の規約を再定義せず適用手順のみを述べる記述」の3値。

### 群1: 状態値（`提案中`／`承認済み`／`却下`／`有効`／`上書き済み`／`廃止済み`）

| 出現箇所 | ラベル |
|---|---|
| `references/adr-model.md`「状態の2軸」の承認軸・有効性軸の2表（値と定義） | **正本** |
| `references/adr-model.md`「状態の2軸 > 軸の関係」 | 適用手順（正本文書内で値域の使い分けを述べる） |
| `references/adr-model.md`「front-matter（状態の唯一の権威）」の YAML スキーマ | 適用手順（front-matter をどう書くかの提示） |
| `references/adr-model.md`「状態の型」の構成子列と制約 | 適用手順（値域を前提に合法な組み合わせを示す） |
| `SKILL.md`「文脈固有ルール」直後の状態値の記述 | 正本へのリンク（**縮退済み**。値域の列挙を落とし `adr-model.md`「状態の2軸」を指す形へ改めた） |
| `references/adr-demotion.md`:20 / :102（退役2値を用いた説明） | 適用手順 |
| `references/cross-references.md`「機械検査の範囲」の検査対象の限定 | 適用手順 |
| `references/edit-decision.md` の凍結原則の対象の限定 | 適用手順 |
| `references/transitions.md`「共通: front-matter の書き方」の実行ガード、各遷移節、退役 inbound 点検節 | 適用手順 |
| `references/template.md` の front-matter 雛形（`status: 提案中`） | 適用手順（生成物の骨格） |

単一値の使用にとどまる行: 121 件（すべて適用手順）。「正本」ラベルは1件。値を再掲するだけの行は0件。

### 群2: 遷移後の front-matter（`status` / `validity` / `superseded-by` の組み合わせ）

| 出現箇所 | ラベル |
|---|---|
| `references/adr-model.md`「状態の型」の直和型定義（5構成子と値組） | **正本** |
| `references/adr-model.md`「型の制約と機械検査の対応」 | 適用手順（型の制約を検査項目へ対応づける） |
| `SKILL.md`「5遷移」の表 | 適用手順（**縮退済み**。3キーの値組の再掲を落とし、どの構成子へ移す操作かの指名へ改めた） |
| `SKILL.md`「5遷移」表の直後の1文 | 正本へのリンク（「状態の型」の構成子として構成できる形にする） |
| `SKILL.md` の `description` と冒頭の概要 | 適用手順（キー名の言及のみで組み合わせを示さない） |
| `references/transitions.md`「共通: front-matter の書き方」の最終状態の指示 | 正本へのリンク |
| `references/transitions.md` の起票・承認・上書き・廃止・却下の各手順 | 適用手順（どの順に何へ書くか） |
| `references/edit-decision.md` 非core の「front-matter は変更しない」 | 適用手順 |
| `references/template.md` の状態の記述規約 | 正本へのリンク（**縮退済み**。front-matter を唯一の権威とする規約と理由の再掲を落とし正本を指す形へ改めた） |
| `references/template.md` の front-matter 雛形 | 適用手順（生成物の骨格） |

単一キーの言及にとどまる行: 72 件（すべて適用手順）。「正本」ラベルは1件。値を再掲するだけの行は0件。

### 群3: 相互参照の語彙（`Supersedes:` / `Superseded by:` / `Related:`）

| 出現箇所 | ラベル |
|---|---|
| `references/cross-references.md`「関係語彙」の3行の表（語彙と意味） | **正本**（冒頭リードで関係語彙の単一出典であることを明示した） |
| `references/cross-references.md`「`Related:` の書式規約」「機械検査の範囲」 | 適用手順（`Related:` の書式と検査範囲。語彙の意味を再定義しない） |
| `SKILL.md`「5遷移」表の上書き行 | 適用手順（どのファイルのどの節へどのラベルを書くか） |
| `references/transitions.md` 上書き手順2・3、分割手順5・7、締めの相互参照確認 | 適用手順 |
| `references/adr-model.md`「識別子の一意性」「採番方式」の是正手段 | 適用手順（リネーム時に追随させる対象の列挙） |
| `references/template.md`「表記規約」 | 正本へのリンク（**縮退済み**。各語彙の意味の再掲を落とし、表記例と正本へのポインタのみを残した） |

単一ラベルの使用にとどまる行: 48 件（すべて適用手順）。「正本」ラベルは1件。値を再掲するだけの行は0件。

**`Supersedes:` / `Superseded by:` の書式要件の扱い**: 語彙（3ラベルとその意味）の正本は `cross-references.md`「関係語彙」の1件だが、`Supersedes:` / `Superseded by:` の**書式要件**（行頭バレット必須・双方向の厳格な一致）と写入手順は `transitions.md` が持つ。両者は別の規範であり、群3 の「語彙」の正本一意性を崩さない。この責務分担は再構成前から同ファイルが明示していたもので、本再構成で動かしていない。

## `io-examples.md` の4例の規範側の所在

削除した4例はいずれも他文書に規範として既述であり、例が規範の複製を作る経路になっていた。削除に伴って行き先を失った規範は0件である。

| 削除した例 | 同じ内容を規範として持つ箇所 |
|---|---|
| 例1 起票（front-matter・骨格） | `references/transitions.md`「起票（提案中）」 |
| 例1 採番衝突 | `references/adr-model.md`「同一時刻部の採番例」表、同「採番衝突時の解消手順」 |
| 例2 承認 | `references/transitions.md`「承認（承認済み・有効）」 |
| 例3 上書き（双方向相互参照） | `references/transitions.md`「上書き（上書き済み・双方向相互参照）」 |
| 例4 廃止・却下 | `references/transitions.md`「廃止（廃止済み）」「却下（却下）」 |

`transitions.md`「入出力例（上書き遷移）」節と目次行も同時に削除した。同節は例3 と同じ内容を二重に掲載していたものであり、規範側は上記のとおり同ファイルの「上書き（上書き済み・双方向相互参照）」節が持つ。

before→after の並置形式も固有の価値を持たない。before 側は `transitions.md`「遷移前の状態確認（実行ガード）」が期待遷移元を規定し、after 側は各遷移節と `adr-model.md`「状態の型」が規定している。

**保全した契約**: `adr-model.md`「採番衝突時の解消手順」末尾にあった入出力例のうち、`next-adr-id.sh` の終了コードは散文としてリポジトリ内でこの1箇所にしかなかったため、例の本体（入力・手順・期待結果）だけを削除し、終了コードは「発番スクリプトの契約」節として残した。あわせて終了コード `1` の意味を実装準拠（件数ではなく最大連番）へ是正した。

## 移動元→移動先の対応表

各条文について、移動先に1件のみ存在し移動元に残っていないことを照合した。「文言の差分」欄は、移動に伴う参照表現の書き換えのみを許し、判定条件・語順・語彙の変更を許さない。

### `edit-decision.md` → `adr-reference-principle.md`

| 移動した条文 | 移動元 | 移動先 | 文言の差分 |
|---|---|---|---|
| 原則本文（記録は可変文書を現在の参照先として指さない） | `edit-decision.md`「記録の参照原則（参照を書く／直す編集の判定）」 | `adr-reference-principle.md`「原則」 | 「前記の凍結原則」→「`edit-decision.md` の凍結原則」。凍結原則は移動元に残るため、相対語では指せなくなったことによる書き換え |
| 判定テスト（参照先を空にして読み直す） | 同上 | 同上 | なし（逐語） |
| 適用対象の例・対象外の例 | 同上 | 同上 | なし（逐語） |
| 判定テストの位置づけの但し書き | 同上 | 同上 | なし（逐語） |

**参照元の張り替え（3箇所）**

| 参照元 | 変更前の指し先 | 変更後の指し先 |
|---|---|---|
| `SKILL.md`「5遷移」節の本文 | `references/edit-decision.md` の「記録の参照原則」 | `references/adr-reference-principle.md` |
| `SKILL.md`「手順の参照」の一覧 | `edit-decision.md` の行が2責務を併記 | `edit-decision.md`（編集分類と操作分岐）と `adr-reference-principle.md`（記録の参照原則）の2行へ分離 |
| `transitions.md` 廃止手順2 | `edit-decision.md`「記録の参照原則」 | `adr-reference-principle.md` |

### `adr-scoping.md` → `adr-destination.md` / `adr-splitting.md`

| 移動した条文 | 移動元 | 移動先 | 文言の差分 |
|---|---|---|---|
| 判定結果の行き先（リード・ADR 化する側の2分岐・ADR 化しない側の3分岐・警告どまりの扱い・既存 ADR への改訂の但し書き・一般則が最も効く層） | `adr-scoping.md`「判定結果の行き先」 | `adr-destination.md`「判定結果の行き先」 | 「前節の昇格フロー」→「`adr-scoping.md`「起票のタイミングとエスカレーション」の昇格フロー」。移動元に残る節を相対語で指せなくなったことによる |
| 命名規約の ADR 化基準・本節の射程（3類型・ファイル配置規約の扱い・射程外の行き先） | `adr-scoping.md`「命名規約の ADR 化基準 > 本節の射程」 | `adr-destination.md`「命名規約の ADR 化基準 > 本節の射程」 | なし（逐語）。文中の「前掲「判定結果の行き先」」は移動先でも同一ファイル内の前方を指すため書き換え不要 |
| 命名規約の振り分け（構造的／書式的の表・タイブレーカー・強制未実装の例外） | `adr-scoping.md`「命名規約の ADR 化基準 > 振り分け」 | `adr-destination.md`「命名規約の ADR 化基準 > 振り分け」 | なし（逐語） |
| 命名規約の適用順（必要条件 → 射程 → 表の3段） | `adr-scoping.md`「命名規約の ADR 化基準 > 適用順」 | `adr-destination.md`「命名規約の ADR 化基準 > 適用順」 | 「前掲「必要条件」節」→「`adr-scoping.md`「必要条件: 却下代替が在ること」節」。移動元に残る節への参照であるため |
| 新規 ADR の束ねの制約（反転の単位・2条・分割の適用条件への送り） | `adr-scoping.md`「新規 ADR の束ねの制約」 | `adr-splitting.md`「新規 ADR の束ねの制約」 | なし（逐語） |

**移動元に残した参照表現の書き換え（3箇所）**: `adr-scoping.md`「必要条件」節の「行き先は後掲「判定結果の行き先」の…」、および「発見型」節の「次節の一般則」2箇所を、いずれも `adr-destination.md`「判定結果の行き先」を直接指す形へ改めた。判定条件は変えていない。

**責務帰属の記述の是正（2箇所）**: 分割後に偽になる記述を実態へ合わせた。`adr-scoping.md` の冒頭リードから移設した3責務（行き先・束ね・命名規約）を外し、新設2本を指す1文を足した。`SKILL.md`「ADR 化要否の判定」節の「4項目の名称・同点処理・**行き先**に加え…も `adr-scoping.md` が単独で持つ」から行き先を外し、行き先は `adr-destination.md` が単独で持つ旨を別文にした。

**新設文書の冒頭リード**: `adr-splitting.md` の冒頭に、「分割」の語が指す3つの判断（いつ駆動するか＝`SKILL.md`「分割」節／どう実行するか＝`transitions.md`「分割（多決定 ADR の部分上書き）」節／そもそも束ねないためにどうするか＝本ファイル）の射程の違いを明記した。3者を現在の置き場所のまま維持したうえで、正本を引くときに誤った文書へ着地しないようにするためである。

**`adr-destination.md` の H1**: 収容する責務が「判定結果の行き先」と「命名規約の振り分け」の2つであるため、H1 を「判定結果の行き先と命名規約の振り分け」とした。同一ファイル内で H1 と H2「判定結果の行き先」が同名になるのを避ける意図もある。

**`adr-demotion.md` の張り替えは0件**: 同ファイルが `adr-scoping.md` を指すのは `:9`（比較表の列見出し）と `:30`（「必要条件: 却下代替が在ること」節）の2箇所のみで、いずれも移設対象の節を指さない。grep で確認済み。

**観測: 節名の衝突（本再構成が作ったものではない）**: `adr-demotion.md`:112 は自ファイルの `## 判定結果の行き先` を持ち、`adr-destination.md`:7 の同名節と見出し名が衝突する。衝突は再構成前（`adr-scoping.md` と `adr-demotion.md`）から同じ度合いで存在しており、本再構成で増減していない。参照は本再構成の作法どおりいずれもファイル名を伴って指すため一意に解決する。`adr-demotion.md` の帰趨は「維持・変更0件」であり、節名の改名は行わない。

**冒頭リードの整合**: `edit-decision.md` の冒頭リードが持っていた「参照は1段階まで＝本ファイルから他の reference ファイルへ委譲・相互参照しない」という自ファイルの参照方針の括弧書きを、本再構成で採る作法（正本を直接ファイル名で指す・深さの上限2段）に合わせた記述へ改めた。あわせて記録の参照原則が本ファイルの責務でなくなったことを明記した。書き換えは同ファイルの参照方針とその責務の記述に限り、ADR 運用条文の判定条件・語彙には触れていない。

## 判定手続きの分岐の前後突き合わせ

正本の統一を分岐の削減と同一視しない。文書を分けても手続きの分岐数は減らないことを、前後の分岐一覧の件数と文言で示す。

**比較対象**: 再構成前 = `adr-scoping.md`（`398928a` 時点、208行）。再構成後 = `adr-scoping.md` ＋ `adr-destination.md` ＋ `adr-splitting.md`。

**何を1件と数えるか**: 判定手続きの条件分岐1つを1件とする。具体的には、必要条件の成否とその判定材料の限定、粒度判定の各項目の該当／非該当、同点処理、合計のスコア境界、発見型短絡の適用条件、行き先の各分岐、命名規約の射程内／外・構造的／書式的・強制の実装有無・適用順の各段、束ねの制約の各条を数える。分岐を持たない説明文（採用理由・失敗モードの説明）は数えない。

### 逐語性の機械的な照合

行の欠落・追加が無いことを先に示す。再構成前の 208 行は、`1-136`（判定コア）／`137-158`（判定結果の行き先）／`160-167`（束ねの制約）／`169-208`（命名規約）と 2 本の区切りの空行に過不足なく分解でき、それぞれ再構成後の `adr-scoping.md`（判定コア）／`adr-destination.md`（行き先＋命名規約）／`adr-splitting.md`（束ね）へ移した。`diff --ignore-blank-lines` で突き合わせた結果、**変更された行は次の5行だけ**であり、いずれも移動に伴う参照表現の書き換えである。削除された行は0件である。

**この diff が覆っていない範囲**: 比較の対象は移動先ファイルのうち移設ブロックに対応する範囲に限る（`adr-destination.md` は7行目以降、`adr-splitting.md` は11行目以降）。両ファイルの冒頭16行（destination 6行・splitting 10行）は H1・責務リード・読み方規定として本再構成が新規に書いた行であり、比較の対象外である。したがって diff が示すのは「**移設した条文**に追加が無い」までであって、「本再構成が1行も足していない」ではない。外れた16行のうち規範性を持つ記述は次の2件で、いずれも移設ではなく新設である。

- `adr-destination.md`:3 — 本ファイルの分岐ラベルに現れる閾値と値域語が `adr-scoping.md` の定義を指す参照であることの読み方規定（J9 の裁定を条文化したもの）
- `adr-splitting.md`:5-9 — 「分割」の語が指す3つの判断のうち本ファイルが持つのはどれかという射程の切り分け（J1 の裁定を条文化したもの）

| 変更された行 | 変更の内容 | 種別 |
|---|---|---|
| 行き先「単一の変更に閉じる判断」の分岐 | 「前節の昇格フロー」→「`adr-scoping.md`「起票のタイミングとエスカレーション」の昇格フロー」 | 参照表現 |
| 命名規約「適用順」の第1段 | 「前掲「必要条件」節」→「`adr-scoping.md`「必要条件: 却下代替が在ること」節」 | 参照表現 |
| `adr-scoping.md` の冒頭リード | 移設した3責務を外し、新設2本を指す1文を追加（2行） | 責務の記述 |
| `adr-scoping.md`「必要条件」節の末尾 | 「後掲「判定結果の行き先」」→「`adr-destination.md`「判定結果の行き先」」 | 参照表現 |
| `adr-scoping.md`「発見型」節 | 「次節の一般則」2箇所 →「`adr-destination.md`「判定結果の行き先」の一般則」 | 参照表現 |

束ねの制約（`adr-splitting.md` へ移した8行）は差分0件の完全な逐語一致である。

### 分岐一覧（前後で同一）

条文が逐語一致であるため、分岐の一覧は前後で同じ集合になる。件数は **38件**で一致する。

**この一致の位置づけ**: 件数の一致は、上の逐語性の照合から導かれる従属命題であって、独立に反証されうる測定点ではない。逐語一致が成り立つ以上、分岐集合が動かないことは論理的に従う。下の一覧は、分岐を数え落としていないこと（＝逐語一致の主張が分岐の網羅を覆っていること）を人手で確かめるための列挙であり、逐語性とは別の証拠を足すものではない。

| # | 分岐 | 再構成前の所在 | 再構成後の所在 |
|---|---|---|---|
| 1 | 却下代替が在るか（不成立なら点数を数えず ADR 化しない） | 必要条件 | `adr-scoping.md` 必要条件 |
| 2 | 判定対象がこれから記録する決定そのものか（他 ADR・規約文書の却下代替は当たらない） | 必要条件 | 同上 |
| 3 | 却下した選択肢の名指しと理由が対として特定できるか | 必要条件 | 同上 |
| 4 | 意図的に決めなかった面か（射程外の論点は却下代替ではない） | 必要条件 | 同上 |
| 5 | 成立するか迷う場合は不成立へ倒す | 必要条件 | 同上 |
| 6 | 項目1 後戻りコストの該当／非該当 | 粒度判定基準 | `adr-scoping.md` 粒度判定基準 |
| 7 | 項目2 適用先の該当／非該当 | 粒度判定基準 | 同上 |
| 8 | 項目3 理由の揮発の該当／非該当 | 粒度判定基準 | 同上 |
| 9 | 項目4 自動強制の該当／非該当 | 粒度判定基準 | 同上 |
| 10 | 項目単位の同点処理（該当するか迷う項目は0点） | 粒度判定基準 | 同上 |
| 11 | 合計3点以上 → ADR 化を推奨 | 粒度判定基準 | 同上 |
| 12 | 合計3点未満 → ADR 化しない | 粒度判定基準 | 同上 |
| 13 | 判断に迷ったら ADR 化しない側へ倒す | 粒度判定基準 | 同上 |
| 14 | 項目3 の値域(A)(B) に当たる保持先があるか | 判定する入力の値域と定義 | 同上 |
| 15 | 値域に当たるか迷う場合は0点 | 判定する入力の値域と定義 | 同上 |
| 16 | 項目3 の条件1〜3 のいずれかに該当するか | 判定する入力の値域と定義 | 同上 |
| 17 | 採用理由が確かめられない場合の条件1・2 の不成立と条件3 の独立判定 | 判定する入力の値域と定義 | 同上 |
| 18 | 項目4 の阻止の状態が3語のどれか（強制未実装は `検査なし`） | 判定する入力の値域と定義 | 同上 |
| 19 | 確かめられなかった事実を推定で補うか（申告へ回す） | 判定に添える申告 | 同上 |
| 20 | 発見型短絡の適用条件（保持先が実装・テスト資産・操作手順で、かつ現に検出・阻止） | 発見型 | `adr-scoping.md` 発見型 |
| 21 | 短絡に当たらない場合は4項目を通常どおり判定 | 発見型 | 同上 |
| 22 | ADR 化する側: 新規 ADR へ振り分ける条件 | 判定結果の行き先 | `adr-destination.md` 判定結果の行き先 |
| 23 | ADR 化する側: 既存 ADR への非core 改訂へ振り分ける条件 | 判定結果の行き先 | 同上 |
| 24 | ADR 化しない側: ツールで自動強制される規範 → 実装・テスト資産・操作手順 | 判定結果の行き先 | 同上 |
| 25 | ADR 化しない側: 強制できないが複数へ効かせたい規範 → 共有規約文書 | 判定結果の行き先 | 同上 |
| 26 | ADR 化しない側: 単一の変更に閉じる判断 → PR 説明 | 判定結果の行き先 | 同上 |
| 27 | 併記の未了が項目3 の点数を動かすか（同居判定の1点条件に当たる決定に限る） | 判定結果の行き先 | 同上 |
| 28 | 警告どまりで阻止しない規範は第1分岐に置く | 判定結果の行き先 | 同上 |
| 29 | 記録済みの決定へ触れる編集は点数ではなく編集分類が入口 | 判定結果の行き先 | 同上 |
| 30 | 命名規約の射程内／射程外 | 本節の射程 | `adr-destination.md` 本節の射程 |
| 31 | 射程外の類型1: 記述の並び順とレイアウト | 本節の射程 | 同上 |
| 32 | 射程外の類型2: 成果物メタデータの値の形式 | 本節の射程 | 同上 |
| 33 | 射程外の類型3: 識別子の採番形式（区切り記号の帰属を含む） | 本節の射程 | 同上 |
| 34 | ファイル配置規約は射程内／成果物内部の分割の規範は射程外 | 本節の射程 | 同上 |
| 35 | 構造的か書式的かの振り分け（表の2行） | 振り分け | `adr-destination.md` 振り分け |
| 36 | タイブレーカー: 原理的に強制できるか（項目4 に照らす） | 振り分け | 同上 |
| 37 | 例外: 書式的命名規約は強制が未実装でも ADR 外 | 振り分け | 同上 |
| 38 | 適用順の3段（必要条件不成立／成立かつ射程内／成立かつ射程外） | 適用順 | `adr-destination.md` 適用順 |
| — | 独立に反転しうる core を束ねない | 新規 ADR の束ねの制約 | `adr-splitting.md` |
| — | 束ねが必要なら一体で反転する抽象度まで上げる | 新規 ADR の束ねの制約 | `adr-splitting.md` |

束ねの制約の2条は判定手続きの分岐ではなく起票時の構成上の制約であるため、38件の外に置き、所在の移動のみを示す（前後いずれでも2条）。

**結論**: 分岐は前後で38件、いずれも判定条件の文言が一致する。差分は上表の5行に限られ、すべて移動に伴う参照表現の書き換えまたは責務の記述の是正である。文書を3本に分けたことで分岐が減っていないことを確認した。

## 節名を照合キーにしている開発記録の追随

参照面の節名を照合キーにしている開発記録2本は、いずれも**編集していない**。凍結記録・観測記録を参照先の消滅を理由に書き換えないという `adr-judgment-pipeline-removal-2026-08-27.md` の裁定に従う。指し先が移ったことは本節に残す。

- **`docs/development/adr-cross-reference-scoping-judgment.md`:38** が照合キーとして引く「ADR 化しない（2点以下）の行き先」は、現行の節名では `adr-scoping.md`「ADR 化しない（合計3点未満）の行き先」に当たり、本再構成で `adr-destination.md`「判定結果の行き先」配下へ移った。同記録:15 が引く「発見型: 未記録の規範を見つけたとき」は `adr-scoping.md` に残留する。なお:38 の引用は本再構成とは無関係に、閾値の表記が「2点以下」から「合計3点未満」へ改まった時点で既に陳腐化していた
- **`docs/development/adr-cross-reference-lint-crosscheck.md`** が照合キーとして引く `cross-references.md` の節名（関係語彙／`Related:` の書式規約／機械検査の範囲／検査される側（source）／違反となる状態／検査されないこと／是正手段）は、本再構成で1つも改名・移設していない。同記録の照合キーは全件そのまま解決する

`docs/development/adr-destination-branch-measurability-2026-08-23.md`:5 も `adr-scoping.md`「判定結果の行き先」節を対象として名指しするが、同じく日付付きの観測記録であるため編集しない。対象節は `adr-destination.md` へ移っている。

## 受入条件の照合

11件すべてを1件ずつ照合した。未充足は0件である。

| AC | 充足箇所 | 照合に使ったコマンドまたは走査 | 判定 |
|---|---|---|---|
| 1 | 本記録「再構成前の参照面9件の帰趨と責務」 | 帰趨表の行数を数えて 9 を確認。値域（維持／分割／統合／削除）の外のラベルが無いこと、`SKILL.md` と references 8本が過不足なく1行ずつ現れることを目視で照合 | 充足 |
| 2 | `references/adr-model.md`「検査項目と正本の対応」 | 表の行数を数えて 7 を確認。各行の正本（パス＋節名）が実在する見出しへ解決することを見出しの grep で確認（7件すべて OK）。`SKILL.md` の「レイヤ」の出現が 0 件であることを grep で確認 | 充足 |
| 3 | 本記録「3語彙群の出現箇所一覧」 | 参照面11件を走査し、群ごとに値を列挙・規定する箇所を切り出して3分類でラベル付け。各群の「正本」ラベルが1件、ラベルの付かない行が0件、値を再掲するだけの行が0件であることを確認 | 充足 |
| 4 | `references/adr-model.md`「状態の型」「型の制約と機械検査の対応」＋ 本記録「front-matter の型と旧表・レイヤ1違反種別の対応」 | 構成子5件が旧表の5行および `lint-adr.sh`:23-29 の遷移表5行と1対1に対応することを照合。違反種別の対応表の行数 8 と `lint-adr.sh`:31-39 の列挙件数 8 が一致し、対応欄が空の行が0件であることを確認 | 充足 |
| 5 | `references/adr-model.md`「発番スクリプトの契約」 | 終了コード表の行数 3 を確認。3値の発生条件を `next-adr-id.sh` の4本の終了経路（ディレクトリ不在／時刻部の形式不正／連番の上限超過／正常）と1件ずつ突き合わせ。件数ベースの表現（「99件」）が参照面と発番器テストの双方から消えていることを grep で確認（0件） | 充足 |
| 6 | `scripts/tests/lint-adr-surface.bats` の期待ファイルリスト | 期待リスト10件と `references/` の実ディレクトリ10件を `diff` で突き合わせ、片側にしかない項目が0件であることを確認。面①②④を含む同ファイルのテストが全スイート実行で緑 | 充足 |
| 7 | 本記録「判定手続きの分岐の前後突き合わせ」 | 再構成前（`398928a`）と再構成後3ファイルを `diff --ignore-blank-lines` で突き合わせ、変更行が5行・削除行が0件であることを確認。分岐一覧を条件分岐1件単位で列挙し、前後とも38件で一致することを確認 | 充足 |
| 8 | `references/adr-scoping.md`「粒度判定基準」「判定する入力の値域と定義」 | スコア境界の定義（「スコア境界は3点」）と項目3の保持先の値域の定義（「項目3の保持先の値域」）を参照面11件へ grep し、いずれも `adr-scoping.md` に1件のみであることを確認。`adr-destination.md` の分岐ラベル5箇所は定義を持たず、逐語で移設された参照である（前掲の diff が示す） | 充足 |
| 9 | — | `bash scripts/run-tests.sh` → 全6スイート緑（bats 147 tests, 0 failures）、exit 0 | 充足 |
| 10 | — | `bash plugins/adr/scripts/lint-adr.sh docs/adr` → exit 0 | 充足 |
| 11 | 本記録「移動元→移動先の対応表」 | 移動した条文それぞれについて移動元と移動先を1行ずつ記載。移動先に1件のみ存在し移動元に残っていないことを、条文に固有の語（「反転の単位」「構造的命名規約」「書式的命名規約」「記録の参照原則」ほか）の grep で確認 | 充足 |

### 制約の充足

- **プラグイン版の bump**: `plugins/adr/.claude-plugin/plugin.json` と `plugins/adr/.codex-plugin/plugin.json` をいずれも `0.6.0` → `0.7.0` へ同値で更新した
- **配布物へ配布元リポジトリの Issue 番号を書き込まない**: 参照面11件に Issue 番号を書いていない。本記録（`docs/development/` 配下、配布物外）にのみ Issue 番号が現れる
- **行番号固定台帳**: `scripts/tests/next-adr-id.bats` の変更は 112 行目のケースラベル1行のみで、登録行（2行目）より上への行挿入は無い
- **条文の内容改訂（OUT）に触れていない**: 合法な状態の集合、判定条件、値域、閾値、遷移規則、相互参照の書式規約はいずれも変えていない。例外は Issue が IN として明示した2点（発番器の終了コード1 の意味の実装準拠への是正、規約文書を持たない3検査の成文化）に限る

## レビュー指摘への対応（PR #792）

差分 `49fa75a5..d59b5a04` に対するレビューで、要修正3件・テスト網2件・追随漏れ2件・記録精度1件・低優先3件が挙がった。反証できたものは0件である。

### 本 PR で対応した指摘

| 指摘 | 欠陥 | 対応 |
|---|---|---|
| 1 | 「レイヤ1 は型として構成できない状態を違反として報告する」が実装より広い。`superseded-by: ,` のような病的値はレイヤ1 の空判定を通過し、レイヤ3 が報告する（`invalid/11-xref-list-empty-superseded` が現に固定している） | `adr-model.md`「型の制約と機械検査の対応」を「違反種別 → 制約」の一方向へ限定し、逆向きが成り立たないことと既知の経路を明記。あわせて `transitions.md` のレイヤ3 是正手段へ当該分岐を追加し、`SKILL.md` のフィードバックループ（対応表 → 正本の是正手段）が成立する状態にした |
| 2 | `adr-model.md`「識別子の一意性」が、指し先（同ファイル「採番衝突時の解消手順」）に存在しない規則を引いていた。実体は `transitions.md`「採番規則」 | 指し先を実体のある節と文言へ改めた |
| 3 | 対応表がレイヤ5 ファイル名形式の正本と宣言した「採番方式」の slug 規定が「英数字ハイフン区切り」のみで、実装（`ADR_STEM_PATTERN`）が強制する小文字限定・連続ハイフン禁止・先頭末尾ハイフン禁止を持たない | slug 規定を実装準拠へ是正した |
| 4 | 本 PR の中核成果物である「検査項目と正本の対応」表を守る検査が存在しない。表の削除・節の改名・節の削除のいずれの変異でも全テストが緑のまま通る | `scripts/tests/lint-adr-surface.bats` へ面⑤・面⑥ を新設した（下記） |
| 6a | `lint-adr.sh` の違反メッセージ3件が、本 PR が削除した「スキーマ表」を名指ししたまま | 実装自身の語彙で規範を述べる形へ改めた。正本へのポインタは対応表が単一の索引として持つため、メッセージ側で正本の節名を名指しすると第二の索引になる。同じ名を持つ fixture 本文4件（`invalid/14`〜`17`）も同時に改めた |
| 7 | 現行の設計文書2本が、本 PR が消した構成物名「遷移ごとの必須ルール」を実体として名指し | `docs/development/domain-model.md` と `docs/development/event-storming.md` を「合法な front-matter を表す状態の型」へ改めた。`docs/adr/README.md`:20 は ADR-202607261459-01 の当時の内容の記述であるため据え置く |
| 8 | 本記録の「条文の追加0件」が、引いた `diff` の出力ではなく人手の分類結果だった。突き合わせは移動先ファイルの冒頭16行を対象から外しており、そこに規範的な文が含まれる | 「逐語性の機械的な照合」節へ、diff が覆っていない範囲と、外れた16行のうち規範性を持つ2件を明記した。分岐38件の一致が逐語一致からの従属命題であることも同節へ明記した |
| 低優先 | `edit-decision.md` から削除した「参照は1段階まで」の後継規律が配布物側に無い／`adr-model.md` 内で「旧形式」が2つの異なる集合を指す／`SKILL.md` の索引行が本 PR の新設4節と発番スクリプトの契約を挙げていない | 順に、参照の深さ2段の規律を同ファイル冒頭へ明文化、「旧ファイル名形式」と「front-matter を持たない ADR」へ語を分離、索引行へ5項目を追記 |

### 新設した検査（指摘4）

`scripts/tests/lint-adr-surface.bats` へ2ケースを足した。参照面は「正本のファイル名と節名を直接書いて指す」作法で組まれており、節を改名・削除しても指す側は静かに壊れるだけだった。

- **面⑤ 参照面の節名参照がすべて実在の見出しへ解決する**: 参照面11件から `` `<path>.md`「<節名>」 `` 形式の参照を抽出し（現状31件）、参照先が期待リスト内のファイルであり、かつその節見出しが実在することを照合する。あわせて照合件数が下限（`AC5_SECTION_REF_MIN`＝現在の実数31）を満たすことをアサートする
- **面⑥ 検査項目と正本の対応表が7項目を過不足なく持つ**: 「検査項目と正本の対応」表の行を抽出し、行数が期待リストの件数と一致すること・各行の正本セルが節名参照の形式を取ること・期待リストと表の双方向被覆・レイヤ番号の集合が `lint-adr.sh` ヘッダの `# レイヤN` 宣言と一致することを照合する

多バイト文字を awk の文字クラスへ入れると mawk ではバイト単位に分解されて誤判定するため、参照の抽出は bash のリテラル部分文字列操作だけで行っている。

**期待リストを対応表から導出しない**: 面⑥ が照合する検査項目7件は `AC2_CHECK_ITEMS` としてテスト側に固定してある。表から導出すると、行が消えたときに期待側も一緒に縮んで欠落が素通りする。面①② が surface のファイル一覧を glob で組み立てず期待リストを固定するのと同じ理由である。下記のとおり、初版はこの導出を行っており実際に素通りしていた。

### 下限アサートの初版の欠陥（再レビューで検出）

初版の面⑥ は行数の**下限1**とレイヤ番号の**集合**しか見ておらず、行数そのものを見ていなかった。レイヤ5 が3検査を束ねるため、レイヤ5 の行が何行あってもレイヤ番号の集合は `1 2 3 4 5` のまま動かない。対応表から「レイヤ5 識別子重複」「レイヤ5 H1 整合」の2行を削除しても全スイートが緑のまま通ることを実測した。この退行は Issue #722 の AC2「表の行数が7である」を直接破り、しかも消える2行は本再構成が「規約を持たなかった3検査」として成文化した側にあたる。

面⑤ の下限も同じ欠陥を持っていた。名目値1では、抽出が部分的に壊れて数件しか拾えなくなっても緑のまま通る。

是正として、面⑥ は期待リスト `AC2_CHECK_ITEMS`（AC2 が列挙する7項目）との双方向被覆と行数の一致へ、面⑤ の下限は名目値から現在の実数へ改めた。

**変異検査の結果**: 是正後の実装に対し、検出を殺す方向6件がすべて赤、緑のまま残るべき方向2件がすべて緑であることを実測した。

| 変異 | 期待 | 実測 |
|---|---|---|
| 対応表からレイヤ5 の2行を削除（再レビューの報告そのもの） | RED | RED（面⑤ の下限・面⑥ の行数・面⑥ の期待→表 の3経路） |
| 対応表からレイヤ2 の1行を削除 | RED | RED（同3経路） |
| 期待リストに無い行を1件追加 | RED | RED（面⑥ の行数・表→期待 の2経路） |
| 既存行を複製（行数のみずれる） | RED | RED（面⑥ の行数） |
| 節名参照を1件だけ落とす（31→30。節は残す） | RED | RED（面⑤ の下限のみ。下限が単独で効くことの確認） |
| 期待リストの項目名を1件書き換え | RED | RED（面⑥ の双方向。固定リストが照合に効いていることの確認） |
| 節名と参照を同時に改名 | GREEN | GREEN |
| 節名参照を1件増やす | GREEN | GREEN |

初版に対しても同じ枠組みで変異検査を行っており、そこでは検出を殺す方向5件（対応表の節ごと削除／`## 状態の型` の改名／`## index の同期` の改名／表に無いレイヤ6 の宣言／正本セルからの節名の欠落）が赤、正例2件が緑であった。**この5件はいずれも赤であったにもかかわらず、行数の欠陥は残っていた。** 変異の集合が「消したときに赤になるもの」だけで組まれており、レイヤ番号の集合を動かさずに行を減らす方向が入っていなかったためである。変異の網は、検査が見ている量（ここでは集合）を動かさない方向の変異を含めないと、その量の粗さを暴けない。
### follow-up へ切り出した指摘

- **指摘5（負例 fixture の欠落3件）** → #793。レイヤ2 の `index.md` 不在、レイヤ5 の H1 に識別子が現れない場合、レイヤ1 違反種別7 のいずれにも負例 fixture が無い。上2件は本 PR が新設した条文に対応するが、fixture の追加は Issue #722 の IN に含まれない。本記録の「fixture の被覆の欠落」で種別7 について記した事実は #793 が引き取る
- **指摘6b（`lint-adr.sh` 冒頭の遷移表）** → #794。`lint-adr.sh`:20-29 の5行の遷移表が、対応表がレイヤ1 の正本と宣言した `adr-model.md`「状態の型」に対する第二の写しとして残る。本記録「3語彙群の出現箇所一覧」が同梱スクリプトを走査面から除外した根拠は `lint-adr.sh`:31-39 の違反種別の列挙に係るものであり、遷移表 :22-29 が同じ根拠に含まれるかは未判定である。この判定を含めて #794 が扱う

### 見てほしい点への回答の反映

- **`adr-demotion.md`:112 と `adr-destination.md`:7 の見出し名衝突**: 「判定結果の行き先」を含む参照はすべてファイル名を伴っており一意に解決するとの確認を受け、本 PR では現状のままとする。改名するなら別 Issue とする
- **レイヤ1 違反種別7 の fixture**: 申告どおりで、偶発的な被覆も無いことが独立に確認された。#793 が引き取る
