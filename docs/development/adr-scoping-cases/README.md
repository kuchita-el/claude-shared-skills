# 固定題材集合の実行手順書

`plugins/adr/skills/manage-adr/references/adr-scoping.md`（以下「対象文書」）を被テスト対象とする固定題材集合について、何のためのものか・何が置かれているか・どう通すか・結果をどう読むかを定める。

## 1. この題材集合の位置づけ

対象文書の判定手続きは、改訂すると帰結が動く。動いたことに気付けるのは、同じ入力を改訂の前後で通し、出てきた帰結を突き合わせたときだけである。本題材集合は、その「同じ入力」を固定して保持するためのものであり、**対象文書を改訂する編集の時点で帰結の差を見る**ことを目的とする。改訂の是非を決めるものではなく、改訂が何を動かしたかを見えるようにするものである。

本題材集合は Issue #604 が挙げた分割案1（判定仕様の退行検出）の成果物である。同じ Issue の分割案2（対象文書が満たすべき不変条件の点検）とは別系統であり、互いを代替しない。不変条件の点検は文書の記述そのものを検査するのに対し、本題材集合は文書を読んだ判定者が出す帰結を検査する。

## 2. 成果物一覧と各ファイルの責務

### 2.1 ファイルと配布物内／外の別

| ファイル | 責務 | 配布物との関係 |
|---|---|---|
| `docs/development/adr-scoping-cases/cases.md` | 題材文層。判定側へ渡る唯一の入力（`### 題材文` 見出しの下の本文ブロック）を持つ | 配布物外 |
| `docs/development/adr-scoping-cases/expectations.tsv` | 期待帰結層。題材ごとの導出すべきもの・期待値・由来・対の相手ID・出所・備考を持つ。**判定側へは渡らない** | 配布物外 |
| `docs/development/adr-scoping-cases/prompt-template.md` | 判定プロンプトの雛形。入力の限定・参照禁止対象・返却形式を定める | 配布物外 |
| `docs/development/adr-scoping-cases/sources.md` | 題材の出所台帳。各題材が PR #603 のどの巡のどの入力に由来するかを持つ | 配布物外 |
| `docs/development/adr-scoping-cases/defect-ledger.md` | 条文欠陥台帳。`expectations.tsv` の備考列へ散っている「規範側の一意性の欠陥」を題材・欠陥・追跡の対応として集約し、対象文書の改訂で動いた測定対象を持つ | 配布物外 |
| `docs/development/adr-scoping-cases/README.md`（本書） | 実行手順書 | 配布物外 |
| `docs/development/adr-scoping-cases/runs/<STEM>-judgments.tsv` | 判定記録。題材 × 試行 × 項目の粒度で判定を残す | 配布物外 |
| `docs/development/adr-scoping-cases/runs/<STEM>-report.md` | 集計レポート。試行間一致・期待帰結との差・差の帰属を残す（部分集合の走行では一時複製へ出力する） | 配布物外 |
| `docs/development/adr-scoping-cases/runs/<STEM>-raw-returns.md` | 判定側の返却全文。`judgments.tsv` の根拠列・行き先列はこれを人手で符号化した派生物であり、符号化の妥当性を後から確かめるための原文 | 配布物外 |
| `docs/development/adr-scoping-cases/runs/<STEM>-returns/` | スキーマ移行を経た保存済み JSON。判定器が返した当時の形は `raw-returns.md` が保持し、逆変換で復元できる | 配布物外 |
| `plugins/adr/skills/manage-adr/references/adr-scoring-thresholds.json` | 項目閾値とスコア境界の唯一の実体 | **配布物内** |
| `docs/development/adr-scoping-cases/<実行日>-<主題>.md` | `runs/` 配下（題材 × 試行）とは別系統の単発記録。判別記録または移行記録として、結論・根拠・再現手順を残す | 配布物外 |
| `plugins/adr/scripts/adr-scoping-cases.sh` | 実行支援スクリプト。`prompt` / `validate` / `report` / `derive` / `crosscheck` / `validate-returns` を持つ | **配布物内** |
| `scripts/tests/adr-scoping-cases-basic.bats` | 上記スクリプトのテスト（基本系。サブコマンドと fixture の組み合わせ） | 配布物外 |
| `scripts/tests/adr-scoping-cases-edge.bats` | 上記スクリプトのテスト（特殊系。環境操作を伴うもの） | 配布物外 |
| `scripts/fixtures/adr-scoping-cases/` | 上記テストが使う fixture（正常系・異常系の最小の題材集合。本リポジトリのデータを含まない当て馬） | 配布物外 |

`runs/` 配下のファイルは語幹 `<STEM>` で採番する。`<STEM>` は実行日が単独なら `<実行日>` そのもの、同一実行日に2つの走行が在る場合（例: 一方が既存全題材の走行、他方が未実測境界の追加走行）は後発の走行の語幹へ判別子を足した `<実行日>-<判別子>` とし、既存ファイルと衝突させない（例: `runs/2026-08-15-cases25-29-judgments.tsv`）。以降の本書の手順・成果物一覧で `<実行日>` と書く箇所は、この `<STEM>` の読み替えを含む。

実行支援スクリプトだけが配布物内に在る。これは検査器であり、**題材集合ディレクトリと対象文書パスをすべて引数で受けて既定値を持たない**ため、本リポジトリ固有のデータ（ディレクトリ構成・対象文書名・題材の中身）を一切持たない。逆に、題材集合・記録・レポートは本リポジトリ固有のデータであるため配布物外に置く。

**テストと fixture を配布物外へ置く境界は `docs/distribution-boundary.md` が定める。** その根拠は本書に置かない（前段の配布物内／外の別も同文書 §2 の判断軸に従う）。本題材集合に固有の事実として、この一方向性はアサーション42（配布元リポジトリ固有の名前が検査器のコードパスへ現れないことの走査）で機械検査している。

なお検査器は**引数の取り方**において汎用だが、スキーマまで可変という意味ではない。題材集合のスキーマ側は固定値として持っている（規範の担い方の3値、由来の2値、`expectations.tsv` の12列、12列目は必要条件の成否、項目1〜4 の4項目）。期待項目は数値または `-`（不問）で記し、合計は項目から導出できるため期待列に持たない。別の判定手続き文書へ持ち込む場合、パスは引数で差し替えられるが、これらの語彙・列構成は検査器の改修を要する。

このうち**規範の担い方の3値は、配布物内のコード（`adr-scoping-cases.sh` の `case` 文）が事実上の唯一の定義**になっている。値の意味は次のとおり。

| 値 | 意味 |
|---|---|
| `体現・強制` | 資産が規範そのものを体現している（実装本体・スキーマ・雛形・fixture 等）。散文で述べるのではなく、その資産の内容自体が規範になっている |
| `散文のみ` | 資産が規範を文章で述べているだけで、体現も強制もしていない |
| `なし` | 規範を担う資産が現に存在しない（実行者の記憶にのみ在る） |

由来の2値の定義は §10「由来の付け方」と `expectations.tsv` の注記行にある。

判定を担うサブエージェントが読むのは、組み立て済みのプロンプトファイルと対象文書だけである。`expectations.tsv` が別ファイルへ分離されているのはこのためであり、期待帰結が題材文と同居すると、判定側が対象文書を読まずに答えを再生できてしまい退行検出力が失われる。

### 2.2 題材を足すときの契約（題材文の必須要素）

`cases.md` へ題材を足す場合、題材文は次の (a)〜(j) をすべて備えること。いずれも対象文書が判定の入力として名指しする**事実**であり、対象文書が結論として述べる述語（値域を満たす／段1で確定する／条件Nに当たる／○点、およびその言い換え）は書かない。

- (a) 決定の内容
- (b) 規範が現に置かれている資産とその種別
- (c) その資産が規範を体現・強制しているか、散文で述べているだけか
- (d) 採用理由が現に書かれている場所と、その内容を確かめられるかどうか
- (e) 却下した選択肢・受け入れた不利益の有無とその中身
- (f) 反転時に直す必要のあるファイルと、影響が及ぶ単位
- (g) 反転が「配置・分割・命名の規約変更」「スキーマ・データ形式の変更」「既に配布された成果物の利用者側への影響」「蓄積済みデータの移行」のいずれを伴うか（いずれも伴わないならその旨）
- (h) 決定を知らない作業者が独立に別の選択をする箇所が現にいくつあるか（無いならその旨）
- (i) その規範に従う作業者が単独か複数か
- (j) 条件3が入力として名指しする資産の構成（規範が1つの資産に収まるか、複数の資産へ分かれる場合に1つの資産の傍らへ理由を置けるか）

(f)(g) を欠くと項目1・項目2 の期待値が確定しない。対象文書の項目1 は本数の条件に加えて OR 条件（非本数条件4種）を持つため、本数だけを書いた題材文からは OR が成立しないことを言えないからである。項目2 は本数条件のみで決まるため、(f) が述べる「影響が及ぶ単位」だけで確定する。

**(h) はどの項目の点数にも効かない distractor である。** 対象文書の項目2 はかつて非本数条件（決定を知らない作業者が独立に別の選択をする箇所が現に複数あれば1点）を OR で持っており、(h) はその入力として立てられた要素だった。Issue #641 が同条件を削除したため入力としての役割は失われている。それでも必須要素から外さないのは、判定側がこの事実を数えて項目2 を1点にしていないかを測るためである。**(h) を根拠に項目2 が1点になっている判定記録が現れたら、それは退行の徴候として読む。**

(i) は判定結果の行き先の分岐が入力として名指しする事実であり（「複数の作業者・複数の箇所へ効かせたい規範」の分岐）、これを欠くと行き先の期待値が一意に決まらない。**作業者の人数は項目2 の入力ではない**（対象文書の項目2 がそう明示している）。(i) は行き先だけに効く。

(j) は条件3の第1連言（規範が複数の資産へ分かれざるを得ないか）と、第2連言（1つの資産の傍らへ理由を置けるか）を題材文から判定するための事実である。1つの資産に収まる題材では、その資産へ理由を併記できるかも明記する。既存題材への遡及はせず、本Issueで改訂する CASE-02・CASE-09・CASE-11 と新規の CASE-32 から適用する。今後の新規題材にも適用する。#621 が §2.2 へ後続の要素を足す場合も新規題材から適用する（既存題材への遡及は記録済みの判定を無効化して再走行を要するため、観測できる改善が無い限りそのコストを払わないという判断であり、§2.4 も同じ判断を採っている）。

### 2.3 期待値の導出が割れた場合の扱い

`expectations.tsv` の期待値を人手で導出する際、値が一意に決まらないことがある。扱いは割れの原因で分ける。

- **割れの原因が題材文の事実の不足に帰属できる場合**: (a)〜(j) の欠落として `cases.md` へ事実を足し、一意に決まる状態にしてから導出し直す。
- **題材文が (a)〜(j) を備えているのに、規範本文の読みが分かれて割れる場合**: いずれか一方を暫定の期待値として置き、割れた候補値・割れた理由・どちらを採ったかを `expectations.tsv` の備考へ記録したうえで、集計レポートの帰属分析（規範側の一意性の欠陥か、題材文の記述密度の不足か）へ回す。

いずれの場合も、**題材を初期集合から外すことはしない。** また、規範側の欠陥と判断したものは記録に留め、対象文書は改訂しない。

規範側の欠陥と帰属したものは、備考へ記録したうえで `defect-ledger.md`（条文欠陥台帳。題材・欠陥・追跡の対応と、対象文書の改訂で動いた測定対象を持つ）へ1行足す。備考は当該題材の割れの記録であり、複数題材に跨る欠陥の在庫は台帳の側が持つ。

### 2.4 据え置いた題材文の記述（次に `cases.md` を触る担い手向け）

判定へ影響していないと確かめたうえで、記述としては改善余地を残したまま据え置いた箇所がある。同じ指摘が繰り返されるのを避けるため、判断とその根拠をここへ残す。

- **CASE-13・CASE-18・CASE-20 の「この規範に関係する資産は2つある。」（`cases.md:161` / `:221` / `:245`）**
  - 経緯: 3巡目まで `この規範を書くとすれば、置き場所の候補になるのは…` と書いていたが、`書くとすれば` が対象文書の条件3 の判定枠組みを名指しする語であるため落とし、現在の文へ置き換えた。
  - 残る問題: 直前の「規範を体現・強制する資産も、規範を散文で述べた記述も、現に存在しない」と読み合わせにくい。**「現に2資産に置かれている」とも読めてしまう**。CASE-17 / CASE-18 は出所欄が明記するとおり「規範の現在の所在を書き分けた対」なので、読みが所在側へ倒れると対の設計意図が弱まる。
  - 判断: **据え置いた。** 判定結果には表れていない（3題材×独立2試行＝6試行とも項目3=1。`runs/2026-07-29-raw-returns.md` の該当6行——CASE-13 は `:392` / `:408`、CASE-18 は `:536` / `:552`、CASE-20 は `:608` / `:624`——はいずれも置き場所ではなく「分かれざるを得なさ」として読んでいる）。一方 `cases.md` の改訂は記録済みの判定を無効化し、この6試行の再実行を要する。観測できる改善が無いまま再実行コストを払う判断を PR #616 では採らなかった。
  - 次に触るとき: `書くとすれば` を含まずに「関係の種類」（所在ではなく、規範が効く先の構成）を明示する書き方へ直す余地がある。#624 で当該題材を再実行する機会があれば、そこへ相乗りするのが最も安い。#619・#620 では当該題材への相乗りを行わなかった。

## 3. 実行手順

コマンドは題材集合ディレクトリを毎回明示して書く。スクリプトは既定値を持たないため、省略形は使えない。

### 手順1: 題材集合の内部整合を検査する

```bash
bash plugins/adr/scripts/adr-scoping-cases.sh validate docs/development/adr-scoping-cases
```

exit 0（「題材集合の検査に通った」）を確認する。落ちた場合は `[NG]` 行が指す欠落・不整合を直してから先へ進む。**この検査に通らないまま判定を始めない。** 期待帰結が欠けたまま通すと、判定は得られても差の突き合わせができない。

### 手順2: 記録ファイルを用意する

`docs/development/adr-scoping-cases/runs/<実行日>-judgments.tsv` を作り、先頭へ `#` 始まりの注記行を置く。注記には、対象文書のパス、実行日、担い手、および**この2試行が測っているのは同一入力に対する判定の再現性であること**を書く。対象文書と題材集合の commit は行ごとに異なりうるため注記へは書かず、列で持つ（下記13列目・14列目）。続けて列見出し行を置く。列は次の14列（タブ区切り）。

`題材ID` / `試行番号` / `項目1` / `項目2` / `項目3` / `項目4` / `行き先` / `根拠_項目1` / `根拠_項目2` / `根拠_項目3` / `根拠_項目4` / `参照ファイル一覧` / `対象文書commit` / `題材集合commit`

`対象文書commit` に入れるのは、判定を実行した時点の対象文書の commit（短縮ハッシュ）である。試行の途中で対象文書が動かないことを、この列で後から確かめられるようにする。

`題材集合commit` に入れるのは、判定を実行した時点の `cases.md` の commit（短縮ハッシュ）である。改訂前後の2レポートを突き合わせる前提は題材文が同一であることなので、この列が無いと、帰結の差が対象文書の改訂によるものか題材文の変更によるものかを行単位で切り分けられない。題材文を直して一部の題材だけをやり直した場合、その行だけが別の値を持つことになる。**行ごとに実際の値を入れる。実行全体で1つの値を注記行へ書いて済ませない。** 走行ツリーの commit が後からどのブランチからも到達できなくなった場合は、題材文ブロックが同一であることを確認したうえで到達可能な commit を記録し、同一性の確認に用いたコマンドと結果を注記または走行レポートへ残す（例: runs/2026-08-16-case31-report.md:77）。

### 手順3: 題材1件分のプロンプトを組み立てる

題材ID と試行番号の組ごとに1回実行する。

```bash
bash plugins/adr/scripts/adr-scoping-cases.sh prompt \
  plugins/adr/skills/manage-adr/references/adr-scoping.md \
  CASE-01 \
  docs/development/adr-scoping-cases
```

標準出力へ返るのは、組み立て済みプロンプトを書き出した一時ファイルのパス1行だけである。プロンプト本文は標準出力へ流れない。呼び出し側は本文を読まず、返ったパスだけを次の手順へ渡す。

### 手順4: 判定サブエージェントを起動する

手順3 が返したパスを渡し、そのファイルを読んで判定するよう指示する。返却は契約仕様が定める実測事実 JSON のみを受け取る。点数・合計・行き先は判定器に申告させず、`derive` が閾値設定から導出する。

### 手順5: 事後検査を行い、通った返却だけを記録へ書く

§7 の事後検査を行う。返却の全文を `runs/<実行日>-raw-returns.md` へ転記したうえで（§7.2）、通った返却を1行として手順2 の記録ファイルへ追記する。落ちた試行は無効として破棄し、破棄の事実を記録の注記行へ残したうえで、その題材・その試行をやり直す。ただし実行基盤の自動注入による逸脱は破棄しない（§7.1）。

### 手順6: 一時ファイルを削除する

`prompt` は呼ぶたびに新しい一時ファイルを作る。使い終えたファイルは削除する。放置すると題材数 × 試行数だけ溜まる。

### 手順7: 全題材 × 2試行が揃ったら集計する

```bash
bash plugins/adr/scripts/adr-scoping-cases.sh report \
  docs/development/adr-scoping-cases/runs/<実行日>-judgments.tsv \
  docs/development/adr-scoping-cases
```

出力は4区画（カバレッジ／試行間一致／各項目の1点率／期待帰結との差）である。カバレッジ検査に落ちる（未カバーの題材・題材集合に無い題材ID・重複行のいずれかがある）と非0で終わる。exit 0 を確認したうえで、出力を集計レポート `runs/<実行日>-report.md` へ貼り、§9 に従って読む。

分子・分母を機械可読な値で取得する場合は、同じ引数の末尾に `--stats` を付ける。標準出力には `キー=値` の行だけが出て、人が読む4区画は標準エラーへ出る。通常のレポートを保存しながら値も保存する場合は、標準出力と標準エラーを別々にリダイレクトする。

一部の題材だけを走らせた場合は、リポジトリの題材集合へ直接 `report` を掛けない。カバレッジの分母は `cases.md` の見出しから作られるため、未実測の題材を含む本体へ掛けると全題材の再走行を要求するからである。走行対象だけを一時ディレクトリへ複製し、複製側の `cases.md` から対象外の見出しを除いてから `report` を掛ける。複製はコミットせず、「期待帰結との差」の件数と各差の切り分けだけを走行記録へ残す。

実測事実から点数を得るには、配布物内の設定を明示して `derive` を実行する。設定値を手で写さない。

```bash
bash plugins/adr/scripts/adr-scoping-cases.sh derive \
  docs/development/adr-scoping-cases/runs/<実行日>-returns/<題材ID>-<試行番号>.json \
  --thresholds plugins/adr/skills/manage-adr/references/adr-scoring-thresholds.json \
  --doc-commit "$(git log -1 --format=%h -- plugins/adr/skills/manage-adr/references/adr-scoping.md)"
```

全件の JSON が揃ったら、`bash plugins/adr/scripts/adr-scoping-cases.sh validate-returns <返却ディレクトリ>` で全件型検査を行い、その後に台帳と導出値を一致検査する。`--allow-missing` は返却全文が存在しない据え置き6組だけに指定する。

```bash
bash plugins/adr/scripts/adr-scoping-cases.sh crosscheck \
  docs/development/adr-scoping-cases/runs/2026-07-29-judgments.tsv \
  docs/development/adr-scoping-cases/runs/2026-07-29-returns \
  --thresholds plugins/adr/skills/manage-adr/references/adr-scoring-thresholds.json \
  --doc-commit "$(awk -F '\t' '!/^#/ && $1 != \"題材ID\" { print $13; exit }' docs/development/adr-scoping-cases/runs/2026-07-29-judgments.tsv)" \
  --allow-missing CASE-01:1 --allow-missing CASE-01:2 \
  --allow-missing CASE-02:1 --allow-missing CASE-02:2 \
  --allow-missing CASE-17:1 --allow-missing CASE-17:2
```

返却全文が残らない6組（CASE-01:1、CASE-01:2、CASE-02:1、CASE-02:2、CASE-17:1、CASE-17:2）は保存済み JSON にも存在しない。免除は欠落を隠すものではなく、この6組に限って明示するための運用である。

この保存済み記録を検査するときは、台帳13列目に記録された対象文書 commit を `--doc-commit` へ渡す。現行対象文書の commit を渡す場合、歴史的記録は版ずれとして全件スキップされ、照合件数が0になるため常設ゲートには使わない。台帳13列目が現行対象文書 commit と一致する走行では版ずれは起きないが、その走行でも台帳13列目を渡す。照合を走行当時の版へ固定するためであり、対象文書が後に進んでも渡す版が動かない。各常設ゲートは、当該走行の判定記録が持つ総試行数と一致する照合件数・スキップ件数0を要求する（`scripts/tests/adr-scoping-cases-basic.bats` の面⑯は照合件数42、面⑯bis は48、面⑯ter は10、面⑯quater は2、面⑯quinquies は2、面⑯sexies は8、面⑯septies は6、面⑯octies は2）。

## 4. 実行主体

起動するのは人間である。判定は、題材1件につき1つの**汎用サブエージェント**が行う。本題材集合のために新規のサブエージェント定義は作らない。判定の内容は対象文書の現行本文とプロンプト雛形だけで決まるべきものであり、エージェント定義側に判定の作法を持たせると、対象文書を改訂しても帰結が動かない余地が生まれるためである。

スクリプトが担うのは入力の組み立て・題材集合の内部整合の検査・判定記録の集計の3つだけであり、判定の真偽をシェルで決めることはしない。

## 5. 入力の限定と参照禁止対象

判定側が読んでよいのは次の3つだけである。

1. 組み立て済みのプロンプトファイル（題材文1件分と対象文書パスを含む）
2. 対象文書
3. 対象文書が名指しする `adr-judgment-contract.md`

**判定の入力は、題材文に書かれた事実と対象文書の現行本文に限る。改訂の経緯・レビュー履歴は入力に含めない。** これらを読めば、判定者は対象文書の現行本文が導く帰結ではなく、過去にそう判定された結果を再生できてしまい、対象文書を改訂しても帰結が動かなくなる。

参照禁止対象は次のとおり（雛形が判定側へ提示するものと同一）。

- 対象文書の変更履歴、過去のコミット、差分
- Pull Request や Issue の本文・コメント・レビュー記録
- 過去の判定記録、集計レポート、期待帰結を記した資料（`expectations.tsv` を含む）
- プロンプトファイルが置かれているディレクトリの他のファイル
- リポジトリ内の他の文書（契約仕様文書を除く）

題材文に必要な事実が書かれていないと判定側が感じた場合も、事実を推測で補わせない。対象文書が定める同点処理に従わせ、補えなかった事実を根拠欄へ書かせる。

## 6. 独立2試行の作り方

題材は**1件ずつ**渡す。試行ごとに**新規コンテキストのサブエージェントを起動し、前試行の判定・記録・他題材の判定を一切引き継がせない**。独立性の根拠はこの点だけに置く。

**この設計で測っているのは、同一入力に対する判定の再現性である。隣接題材の文脈効果は測っていない。** 1件渡しでは判定側の入力に他の題材が現れないため、文脈効果は最初から発生せず、測定の対象にもならない。集計レポートと判定記録の注記行にも同じ射程を明記する。

先例（`docs/development/adr-scope-observation.md` §1.3）は2回目の試行でバッチの切り方を変えているが、これはバッチ内の隣接文脈を消すための操作である。1件渡しではバッチが存在せず、投入順は判定側の入力に一切影響しないため、投入順を変えても2つの試行は同一入力の再実行にしかならない。したがって本手順では投入順の変更を独立性の根拠に用いない。

担い手（モデル・エージェント種別・プロンプト）を変えた試行を追加することはできるが、それが測るのは**判定者間信頼性**であり、同一入力に対する再現性とは別の量である。追加しても本手順の2試行には数えない。追加する場合は、担い手が異なる旨を記録の注記行へ明記して区別する。

## 7. 事後検査

各試行の返却について、`参照ファイル` 欄を検査する。**対象文書とプロンプトファイル以外のパスが挙がっていないこと**を確かめる。挙がっていた場合、その試行は入力の限定に違反しているため**無効として破棄**し、破棄した事実（題材ID・試行番号・読まれてしまったファイル）を判定記録の注記行へ残したうえで、当該題材・当該試行をやり直す。

この検査は入力の限定が守られたかを事後に確かめる唯一の手段であり、省略すると、参照禁止対象を読んだ判定が記録へ紛れ込んだまま集計へ入る。

### 7.1 破棄では解消しない逸脱（実行基盤による自動注入）

判定側が**自分で開いていないのに入力へ混入するファイル**がある。Claude Code は判定サブエージェントの起動時に、リポジトリルートおよびユーザースコープの `CLAUDE.md` を自動でコンテキストへ載せる。これは判定側の操作ではないため、試行をやり直しても同じことが起きる。**この種の逸脱は破棄・再実行では解消しない。**

したがって事後検査の扱いを2つに分ける。

- **判定側が能動的に開いたファイル**が対象文書とプロンプトファイル以外に挙がった場合 — 従来どおり破棄してやり直す。
- **実行基盤が自動注入したファイル**が挙がった場合 — 破棄しない。事実をレポートの「入力の限定に関する限界」へ記し、混入した文書が当該判定へ効きうるかを別途評価する。

`参照ファイル` 欄には、自動注入されたものも含めて挙げさせる（プロンプト雛形は「読んでしまった場合も隠さず挙げること」と指示している）。自動注入か能動的な参照かの区別は、判定側の申告と `参照ファイル` 欄の記述で行う。

### 7.2 返却全文の保全

事後検査に用いた**返却の全文**を `runs/<実行日>-raw-returns.md` へそのまま残す。これは判定器が返した当時の原文である。`returns/` の構造化 JSON は機械変換を経た派生である。`judgments.tsv` の点数列は導出値を保持し、一致検査で二重保持の差を検出する。

原文と `returns/` の一致検査に `scripts/migrate-return-schema.sh --reverse` を掛けるのは、**返却スキーマの分離（`欠測` キーの追加）より前に保存された走行**に限る。分離より後のスキーマで保存された走行へ掛けてはならない。掛けると `returns/` 側から `欠測` が落ち、原文と食い違う。

**どちらに当たるかは、原文（`runs/<実行日>-…-raw-returns.md`）が JSON キーとしての `欠測` を持つかで判別する。** 持たない走行が適用側、持つ走行が非適用側である。走行ごとの個別列挙にしない。`returns/` の側は判別に使えない——スキーマ分離の commit が既存の `returns/` を前方移行したため、分離より前の走行の `returns/` も後の走行の `returns/` も等しく `欠測` を持ち、判別子として機能しないためである（実測: 分離前の6走行の `returns/` はいずれも `欠測` を持ち、その原文6本はいずれも持たない。分離後の #749 の2走行は原文・`returns/` の双方が持つ）。補助の判別として、当該走行の保存時点が返却スキーマ分離の commit `5c68d98`（2026-08-20）より前か後かを併記できる。

## 8. メイン context へ題材集合本文を載せない運用

題材集合の本文（`cases.md` の題材文）は、呼び出し側のセッションへ載せない。

- **判定を実行する作業**では、呼び出し側が扱うのは題材ID・ファイルパス・サブエージェントが返す構造化サマリだけである。`prompt` がプロンプト本文をファイルへ書き出してパスだけを返す設計は、この規律を満たすためのものである。題材文を読んで確認したくなっても、呼び出し側で `cases.md` を開かない。
- **題材集合を作成・改訂する作業**では、本文の読み書きをサブエージェントへ委譲する。呼び出し側へ返させるのは、変更したファイルのパスと構造化サマリ（何をどう変えたかの要約・件数・検査の実行結果）だけであり、本文そのものは返させない。

委譲する際は、プロンプトへ次の2点を必ず含める。

1. ユーザースコープとプロジェクトルートの CLAUDE.md を読み、そのルールに従うこと
2. 実行した全ての Bash コマンドとツール呼び出しを、実行順に「実行ログ」セクションとして最終出力に含めること

## 9. 記録の見方

`report` の出力は4区画に分かれる。読む順序と見るべき点は次のとおり。

1. **カバレッジ** — 題材数に対して記録が覆う件数。全題材 × 2試行が揃っていない状態で一致率を読まない。
2. **試行間一致** — セル単位（項目1〜4 を1セルと数える）の一致件数と、項目別・合計・行き先の一致率。
3. **各項目の1点率（周辺分布）** — 試行ごとに、その項目が1点となった割合。
4. **期待帰結との差** — 題材 × 試行 × 項目の粒度で、期待値と判定が食い違った箇所の一覧。

κ を計算するための分子・分母や、カバレッジ、差の件数を機械的に取り込むときは `report ... --stats` を使う。契約表に列挙した `キー=値` が標準出力へ出るため、表示文言の変更に依存せず値を検査できる。人が読む区画と診断は標準エラーに残る。

`--stats` のキー契約は次の集合に閉じる。`coverage.cases` / `coverage.covered` / `trials.count` / `agreement.cells.{matched,total}` / `agreement.item{1..4}.{matched,total}` / `agreement.sum.{matched,total}` / `agreement.dest.{matched,total}` / `diff.count` は整数を出す。`agreement.trial.a` と `agreement.trial.b` は比較対象の試行ラベルを出し、試行が2つ未満なら空値になる。`ones.trial{試行ラベル}.item{1..4}.{count,total}` は記録に現れた各試行について整数を出す。これら以外のキーは消費側の必須契約に含めない。

終了状態が1でも、カバレッジ違反なら統計値を出力する。期待帰結を1件も読めない場合は統計値を出さず、診断を標準エラーへ出すため、消費側は終了状態と標準出力の両方を確認する。

**κ と該当率（1点率）は両方を見る。生の一致率は単独では使わない。** `adr-scope-observation.md` §4.3 が示すとおり、両者は別の失敗モードを検出する。該当率が高い項目は「安定しているが無情報」（常時点灯して判定を分けていない）であり、処方は項目の削除・統合・条件の細分化である。κ が低い項目は「解釈が試行ごとに割れる」であり、処方は操作的定義の付与である。κ だけを見ると前者を最良の項目として見逃し、生の一致率だけを見ると該当率の偏りによる上振れを一致と読み違える。

**κ はスクリプトが算出しない。** `report` が出す周辺分布（各項目の1点率）から手計算して併記する。二値の項目については、試行1 の1点率を p1、試行2 の1点率を p2 として、偶然一致率 Pe = p1 × p2 + (1 − p1) × (1 − p2)、実測一致率を Po として κ =（Po − Pe）/（1 − Pe）で求める。

**期待帰結との差には、人が帰属を付ける。** 差1件ごとに、規範側（対象文書）の一意性の欠陥か、題材文の記述密度の不足かの見立てを書く。この帰属は機械的には決まらない。題材文の記述密度が原因と判断した題材は、題材文を修正したうえで当該題材のみ独立2試行で再実行し、修正の事実と前後の帰結を記録する。規範側の欠陥と判断したものは記録に留め、対象文書は改訂しない。

**標本が小さいことは限界として注記する。** 題材数が数十件の水準では、κ も一致率も単独では読めない。数値は差の在処を指す手掛かりとして扱い、結論は差の内容と帰属で述べる。

## 10. 対象文書を改訂するときの使い方

1. **改訂前に1度通す。** 現行本文に対して §3 の手順を最後まで実行し、判定記録と集計レポートを残す。これが基準（改訂前の帰結）になる。
2. **改訂する。**
3. **改訂後に同じ手順で通す。** 題材集合を変えず、同じ題材ID・同じ雛形で §3 を再実行する。
4. **2つの集計レポートを突き合わせる。** 帰結が動いた題材を列挙し、動いたことが改訂の意図どおりかを1件ずつ判断する。意図しない箇所で動いていれば、それが退行である。

### 由来の付け方

`expectations.tsv` の由来列は `改訂前から在る` / `改訂の結果として追加` の2値であり、割り当て基準は次のとおり（`expectations.tsv` の注記行と同じ基準）。

- **`改訂前から在る`** — 初期集合、すなわち Issue #605 完了時点で `cases.md` に在った題材。
- **`改訂の結果として追加`** — Issue #605 の完了後に対象文書を改訂し、その改訂に伴って新たに足した題材。

ここでいう「改訂」は**今後行う対象文書の改訂**を指す。PR #603 による改訂を指すのではない。

したがって **Issue #605 完了時点では由来が `改訂前から在る` 側へ縮退し、`改訂の結果として追加` の行は0件だった。これは記入の欠落ではなく、上記基準による設計上の帰結である。** 0件であることを不足と読んで埋めない、というのがこの基準の趣旨である。2値が実際に分かれたのは、対象文書を1度改訂し、その改訂に伴って題材を足した時点からである。

## 11. 現時点で検出力が及ばない範囲（既知の穴）

本節が扱うのは**題材集合の側の穴**（対象文書は一意に決まるが、それを測る題材が無い範囲）である。対象文書の側の欠陥（題材は足りているのに条文の読みが割れる箇所）と、対象文書の改訂で動いた測定対象は `defect-ledger.md` が持つ。

題材集合32題材は、対象文書のすべての規定に等しく効くわけではない。**次の箇所は改訂しても帰結が動かない**ことが分かっている。退行検出の網としては穴であり、題材を足すまで塞がらない。「通ったから安全」と読まないこと。条件2の陽性題材追加と項目4の境界題材解消の経緯は表の後に別記する。

下表は32題材へ必要条件を織り込んだ後の観測である。Issue #632 で足した5題材（CASE-25〜29）は判定手続きの3つの解釈分岐と、本数条件が0点のときの非本数条件の評価、および規範が ADR 文書へ適用される類型を対象としている。CASE-30 は受け入れた代償を含む類型、CASE-31 は違反が現に止まる境界類型、CASE-32 は条件3の陽性類型を対象とする。必要条件の不成立行は点数を不問化するため、項目1〜4の陽性件数は走行記録では別途切り分ける。

| 穴 | 現状 | 帰結 | 追跡 |
|---|---|---|---|
| **ADR 化する側の2分岐が潰れている** | 必要条件成立後に `ADR化する` として残るのは CASE-04 / CASE-26 / CASE-30 / CASE-32 の4題材で、いずれも単一の `ADR化する` 値である（2026-08-23 に `expectations.tsv` を実測。#624 が足した CASE-32 は本行の件数へ反映されていなかった）。分岐（新規 ADR / 非core 改訂）の入力は「既存 ADR が扱っていない枠組みか」であり、判定側は他文書の参照を禁じられ（§5）、必須要素 (a)〜(j) にも該当する事実が無い。さらに前段の事実として、**行き先はそもそも判定器が返さない**——§5 手順4 と契約仕様「直列化形式」が点数・合計・行き先をペイロードから外し、`derive` が閾値設定から導出する。`derive` の成立側は `ADR化する` の単一値で下位分岐を持たない | この4行は**原理的に決定できない**。題材文へ事実を足すだけでは帰結が1件も動かず、塞ぐには返却契約と `derive` を含む測定機構の側の改訂が要る。**この分岐を測らないことを #621 で確定した**（射程外を確定した扱いとする）。判断の根拠は `../adr-destination-branch-measurability-2026-08-23.md`（測る側の案が測定機構の改訂を要すること、実害は誤りの型で分かれ第2選言型のみ下流の人間ゲートが吸収すること、を含む結論と問い5件分の答え） | #621（決定済み。2026-08-23）／第1選言型の捕捉漏れは #774 |
| **項目1 の測定対象が消滅した** | CASE-09・CASE-11・CASE-32 が必要条件成立後に項目1=0となることを #624 の独立2試行で確認した（3題材×2試行、6/6一致）。CASE-02 と CASE-03 の 期待_項目1 は #749 で確定値 0 へ充填し、独立2試行で確認した（両題材の他の欄の充填は本行の射程外。項目2 は下の行、項目3・項目4 は #749 の走行記録が持つ） | 3題材の測定点は #624 の走行で回復したが、**回復したのは 項目1=0 側のみである**。2026-08-27 に `expectations.tsv` を列指定で実測すると（12列目が `成立` の行に限り、3列目が `-` でない行を数える。素の文字列一致では母集団がずれる）、`期待_項目1` に数値を持つのは CASE-02 / CASE-03 / CASE-09 / CASE-11 / CASE-32 の5件で、いずれも 0 である。**陽性側（1）の測定点は現に0件**であり、判定器が項目1 を常に 0 へ潰す退行を32題材のどれも赤にしない。下の項目2 行と同じ形の文だが、**成否は項目1 と項目2 で分かれる**。項目1 では合計経由の間接経路も残っていない——項目1=1 を返す成立側は CASE-04 のみで、合計が 4→3 でも `adr_score_boundary`（3）以上のため行き先が動かず、`期待_項目1` は `-` でセル照合からも外れるためである。項目2 の側は間接経路が2題材だけ残る（下の行を参照）。陽性側を担っていたのは `936c096` 時点の CASE-01 / CASE-28 / CASE-29 で、必要条件層の織り込みにより3件とも `期待_必要条件=不成立` へ移り点数欄が全 `-` へ不問化された。陽性側の回復（題材の追加・改訂と再走行）は #747 が担う。CASE-02 と CASE-03 の 項目1 の充填と再走行は #749 で実施した（2026-08-27。両題材の残る欄も同時に充填している）。充填後も5件はいずれも 0 であり、**陽性側の測定点は0件のままである**（充填が回復させたのは陰性側の測定点の件数だけであり、この穴は塞がっていない）。期待欄に `-` を持つ成立側それぞれの帰趨は、下記「Issue #761 の判定記録」の受入条件3 の表が持つ（同表は 2026-08-25 時点の12件を列挙する。#749 で CASE-02・CASE-03 を充填したため、2026-08-27 時点の実測は10件である）。#724 は個別実施先ではなく、条文改訂で測定対象が動いた場合の帰趨規則を担う | #624（0 側の回復） / #747（項目1 陽性側の回復） / #749（CASE-02・CASE-03 の点数欄の充填。実施済み） |
| **項目2 の測定対象が1件へ縮退した** | CASE-02・CASE-09・CASE-11・CASE-32 の4題材が必要条件成立後に項目2=1となることを #624 の独立2試行で確認した（4題材×2試行、8/8一致）。CASE-03 の 期待_項目2 は #749 で確定値 1 へ充填し、独立2試行で確認した | 項目2 の測定点は #624 の走行で回復したが、**回復したのは 項目2=1 側のみである**。2026-08-27 に `expectations.tsv` を列指定で実測すると（12列目が `成立` の行に限り、4列目が `-` でない行を数える）、`期待_項目2` に数値を持つのは CASE-02 / CASE-03 / CASE-09 / CASE-11 / CASE-32 の5件で、いずれも 1 である。**陰性側（0）の測定点は現に0件**である。ただしセル単位で0件であることは、判定器が項目2 を常に 1 へ潰す退行が32題材を素通りすることを意味しない。`report` の「期待帰結との差」で期待欄 `-` による除外が掛かるのは項目1〜4のセルだけであり、行き先の照合には掛からないためである。合計（`derive` が算出し `adr_score_boundary`＝3 以上で `ADR化する` へ倒す）が閾値を跨ぐ CASE-08・CASE-23——現行の走行記録（`runs/2026-08-15-judgments.tsv`）で2試行とも `0/0/1/1`＝合計2、`期待_必要条件` は成立、`期待_行き先` はそれぞれ `ADR化しない/共有規約文書`・`ADR化しない/PR説明`——では、項目2 を 1 へ潰すと合計が 3 になって行き先が `ADR化する` へ反転し、差として現れる。他方、合計が閾値へ届かない CASE-15 / 16 / 21 / 22 / 31 では潰しても差が出ない。すなわち**セル単位の測定点は0件であり、検出はこの2題材の合計経由の間接経路にしか残っていない**。陰性側を担っていたのは `936c096` 時点の CASE-25 で、必要条件層の織り込みにより `期待_必要条件=不成立` へ移り点数欄が全 `-` へ不問化された。陰性側の回復（題材の追加・改訂と再走行）は #747 が担う。残る #747・#724 の射程は統合せず、それぞれの行で追跡する | #624（1 側の回復） / #747（項目2 陰性側の回復） / #749（CASE-03 の点数欄の充填。実施済み） |
| **必要条件の織り込みで `ADR化する` 側が縮退した** | 現行本文の必要条件を題材文へ適用すると、過去の縮退結果は `ADR化する` は CASE-04 / CASE-26 の2件であり、CASE-12 / 13 / 18 / 19 / 20 / 28 は不成立へ移る。本走でCASE-30が追加され3件になり、その後 #624 の CASE-32 で4件になったが、縮退で失われた CASE-04 ↔ CASE-12 の対は回復していない | 必要条件の成否を測ると、対の検出力が維持できない。**分岐の側は #621 が測らないと確定したため本行の射程から外れた**（上の行を参照）。本行の穴について #747 が担うのは CASE-04 ↔ CASE-12 の対の回復であり、題材の追加・改訂と走行を伴う（#747 は 2026-08-25 以降、上の項目1 行・項目2 行が送る極性回復の射程も並置して持つ。本行の射程がそちらへ広がるわけではない）。この行の穴の境界は #724 の条文改訂帰趨の決定とは異なる | #747 |
| **圧縮で測定対象を失った題材** | CASE-08 / 09 / 19 は、B-04 で消滅した規定を測る題材だったため、現行本文へ期待値を合わせると当該規定の退行検出対象を失う | 対象文書の改訂で動いた測定対象の帰趨を、題材集合だけからは決定できない。#724 は4つの問いへの規則・記録先を決める | #724 |
| **CASE-09・CASE-11 の条件3入力が欠けていた** | CASE-09・CASE-11へ、規範が1つの資産に収まる構成で複数資産へ分かれざるを得ないものではないという事実を追加した。CASE-11の導出すべきものに含まれる条件3も題材文から一意に導ける | 期待値は両題材とも0/1/0/1へ更新し、必要条件成立のもとで独立2試行を実施した。4試行すべてで3セルが一致し、条件3=0となった。陽性測定点はCASE-32で別に立てた。詳細は runs/2026-08-16-case624-discrimination.md | #624 |
| **命名規約節の例外を測る題材が0件になった** | 2026-08-27 に、改訂後の条文（`4abad2a`）の射程の述語を32題材へ1件ずつ当てて実測した。「射程内（識別子にどの名前を与えるか・どう綴るか）かつ書式的かつ強制が未実装」に当たる題材は**0件**である。CASE-02 は記述の並び順、CASE-03・CASE-10 は成果物メタデータの値の形式、CASE-14・CASE-24 は識別子の採番形式であり、いずれも改訂で射程外として名指しされた3類型に落ちる。残る27題材は命名規約そのものを主題としていない | 命名規約節末尾の例外（`書式的命名規約は、強制がまだ実装されていない場合も ADR 外とする`）の行き先が**退行検出網の外へ出た**。この例外を削る・反転する改訂を32題材のどれも赤にしない。#749 が案A（識別子の採番形式は射程外）を条文へ固定した代償として生じた穴である。加えて、**題材を足すだけでは塞がらない**——`cmd_derive` の「ADR 化しない」側は一般則3分岐しか持たず、命名規約節の例外に対応する分岐を持たないため、例外側の行き先を導出器が出せず期待値として置けない。塞ぐには題材の追加と、返却へ命名規約の類型を表すキーを足す測定機構の改訂（`adr-judgment-contract.md` と `derive` と `expectations.tsv` の値域を同時に改める）の両方が要る。題材の追加は #749 では行わない（OUT） | #747（題材の追加・改訂と再走行） / #724（条文改訂で測定対象が動いた場合の帰趨規則） |

**条件3 の測定点が0件である** は、必要条件成立・条件1不成立・条件2不成立・条件3成立の CASE-32 を追加し、条件3単独で項目3=1となる測定点を立てることで塞いだ。CASE-09・CASE-11は条件3不成立側の入力事実を持つ。4題材×2試行の実測は runs/2026-08-16-case624-report.md に記録した。

**ADR 化する側の2分岐が潰れている** は題材を足すだけでは塞がらない。判定側へ既存 ADR の枠組みを事実として与える経路（必須要素への追加、または入力の限定の見直し）が要る。**#621 でこの分岐を測らないと確定した（2026-08-23）。射程外は暫定の扱いではなく確定した扱いである。** 期待_行き先を `ADR化する` の単一値に留めているのはこのためであり、記入の欠落ではない。今後この単一値を「書き分けの漏れ」と読んで埋めない。

**ただし、この規約を守る機械検査は無い。** `validate` は成立側の `期待_行き先` について非空しか見ておらず、語彙の固定は `期待_必要条件=不成立` の行にしか掛かっていない。`crosscheck` も成立側の行き先を照合していない（除外理由として置かれている「台帳が下位分岐を自由記述で保存しており正規化できない」は現行台帳では失効しているが、照合は無効のままである）。したがってこの規約は読解にのみ依存する。

逐語一致の検査を足す案は採らない。`期待_行き先` は値域が型として定まっていない自由文字列であり、その上に語の一致を積んでも検査として意味を持たないためである。値域の型付け（真偽値・列挙などの表現へ移すこと）と、それに伴う `crosscheck` の成立側照合の有効化は、既存の走行記録の移行コストと併せて判断する必要があるため**保留とする（2026-08-23）**。この段落が、その保留の記録である。

確定の根拠は `../adr-destination-branch-measurability-2026-08-23.md`（Issue #621 の調査メモ。問い5件への答えと却下した選択肢の理由を持つ）に置く。要点は次の4つである。

- **測る側の案は題材の追加では届かない。** 行き先は判定器の返却に含まれず（§5 手順4・契約仕様「直列化形式」）、`derive` が閾値設定から導出する。成立側の `derive` は `ADR化する` の単一値であり下位分岐を持たない。必須要素へ記号を足しても、判定器はその事実を用いた行き先を返す口を持たず、`derive` はそれを受け取らない。塞ぐには返却契約・`derive`・`expectations.tsv` の値域・`crosscheck` の照合を同時に改める測定機構の改訂が要る
- **実害は誤りの型で分かれる。** 2分岐の出力は対象文書自身が「振り分け先の提示であって確定ではない」と宣言している。第2選言型（既存 ADR の結論・方向・前提を反転・置換する決定を非core 改訂の側へ振る誤り）は、`edit-decision.md` の**必須の人間ゲート**が同一の述語を問い直し、既定選択肢が core（＝新規 ADR）であるため吸収される。軽い方向も起票直後は `status: 提案中` で有効 ADR index に載らず、却下遷移で終端できる。他方、第1選言型（既存 ADR が扱っていない枠組みを立てる決定を、枠内に収まると読んで非core 改訂の側へ振る誤り）に対応する問いは下流に無く、ゲートを素通りする（`edit-decision.md` の第1問は既存 ADR の結論を問うため答えは NO になり、この型を捕まえる「先行 ADR が決めていない facet」の規定は経路選択の時点で対象外化される）。この型の実害は小さいとは言えないが、測る側の案が上の障害を越えられないため結論は動かない。塞ぐには `edit-decision.md` 側の規範の改訂が要るため、規範側の捕捉漏れとして #774 で追跡する
- 必須要素へ (k) を足す案は §2.2 の契約とは両立しうるが、分岐の述語「枠組みの中に収まるか」の一意性を**対象文書の側が保証していない**（`edit-decision.md` が機械検査の不在を既知の限界として明記している）。一意でない述語に期待値を置いても、割れは §2.3 の経路で規範側の欠陥として `defect-ledger.md` へ積まれるだけで、退行検出の網にならない
- 既存 ADR の index を追加入力として渡す案は、§5 と `prompt-template.md` の二重の入力制限と §7 の許容パス集合を同時に改訂したうえ、時点によって変わる入力（index は ADR の増減で変わる）を持ち込む。§6 が測ると宣言している「同一入力に対する再現性」という測定の性格そのものが変わるため採らない

**対象文書（`adr-scoping.md`）側へ射程外である旨の注記は行わない。** 「この2行が題材集合の網に入っていない」は題材集合の側の属性であって規範としての属性ではなく、対象文書の読者（`manage-adr` の利用者）にとって判定の手順を変えない。加えて `adr-scoping.md` は配布物であり、リポジトリ内部の実験装置の事情を書くのは配布物と利用者側の境界を越える。記録の読者は題材集合を触る担い手であり、本節と上記メモで届く。したがって注記のための後続 Issue も起票しない。

なお、2分岐の誤りが将来実害を持つと判明した場合、網を張るべき対象は `adr-scoping.md` ではなく確定を担う `edit-decision.md` の側である（別の被テスト対象として題材集合を立てることになる）。この見立ては上記メモの案D として記録してあり、測る側の装置を立てること自体は現時点では Issue にしない。ただし第1選言型（既存 ADR が扱っていない枠組みの新設）の捕捉漏れは、題材集合を立てても塞がらず規範の改訂を要するため、#774 で別に追跡する。

**条件2 の陽性が0件** はCASE-30で解消した。項目4の境界題材はCASE-31で解消したが、確認は2試行・担い手1系統に限られる。

**CASE-09・CASE-11 が条件3 に反応しない** は、条件3の入力事実を追加し、当該2題材を独立2試行で再実行して解消した。CASE-32で条件3成立側も追加し、4題材×2試行の記録・レポート更新まで完了した。本PRで移したCASE-09・CASE-11・CASE-02の入力事実と条件3の測定点は #624 が担い、#747 は別行の CASE-04 ↔ CASE-12 の対の回復を担う（2026-08-23 に #621 の確定でスコープを改訂した。改訂前は「ADR 化分岐に必要な題材追加・改訂」を担っていた）。#761 で行った追跡欄の整理は、解消済みの2行を #624 に戻し、CASE-02項目1・3の残余を実施先 #749 へ申し送ることで完了した。その後の第2ラウンド（2026-08-25）で、#624 による回復が 項目1=0 側・項目2=1 側の片側に留まる事実を上表の当該2行へ記載し、陽性側・陰性側の測定点の回復の追跡先として #747 を両行の追跡欄へ加えた。あわせて #749 本文へ点数欄を要求する受入条件を追加したため、項目1 行および `defect-ledger.md` §7 が置いていた「受入条件の追加が前提」という趣旨の注記は除いた（項目2 行はこの注記を持っていなかった）。

**CASE-09・CASE-11 の3層の整合** は、条件3の入力事実を補い、CASE-11の必要条件成立を確認した独立2試行を記録することで回復した。CASE-09・CASE-11の4試行は条件3=0で一致し、CASE-32の2試行は条件3=1で一致した。

**契約側（§2.2）にも同型の穴があった。** 必須要素へ (j) を追加し、条件3が入力として名指しする資産構成（規範が1つの資産に収まるか、複数の資産へ分かれる場合に1つの資産の傍らへ理由を置けるか）を明記する契約へ改訂した。CASE-02・09・11・32の要素チェックは runs/2026-08-16-case624-element-check.md に記録した。

### Issue #761 の判定記録

#### 回復経路（AC1）

`8c3c56b` と現行の `expectations.tsv` を突き合わせると、回復は同じ「必要条件の反転」ではなく、次の3経路に分かれる。

- **CASE-11** は、条件3の入力事実（却下選択肢と却下理由の対を特定できる事実）を題材文へ追加したことで、`期待_必要条件` が不成立から成立へ反転し、点数欄の一括不問化が解消した。
- **CASE-09** は、`期待_必要条件` が #624 前から成立であり、反転していない。必要条件成立のまま `-` だった項目1・項目2・項目4を確定した。
- **CASE-32** は `8c3c56b` に存在せず、条件3陽性側を測る新規題材として追加した。

したがって、CASE-09をCASE-11とともに必要条件の反転側へ置く記述は採らない。この組み合わせは「新規題材だけを足す」経路を採らず、既存題材の入力事実の補完・据え置き点の確定・陽性題材の追加を分けて行う意図的な設計変更である。根拠は `runs/2026-08-16-case624-report.md`、`runs/2026-08-16-case624-discrimination.md`、および `runs/2026-08-16-case624-judgments.tsv` である。

#### #747・#724 の再編要否（AC2）

#747 の受入条件は、2分岐を撃ち分ける題材の追加、CASE-04 ↔ CASE-12 の再比較、独立2試行と期待値・走行記録の更新であり、題材追加・改訂と走行を伴う。一方、#724 の受入条件は4つの調査の問いへの決着、決定と記録先の選択、#619との整合、およびADR化要否の判定であり、規則の決定と文書化を主とする。成果物と作業型が同一ではないため、**再編しない**。

原因がどちらも必要条件の織り込みに関係することは、作業を一体化する根拠にしない。統合する案を採らない理由は、#747が実測可能性を回復する実装・走行Issueで、#724が条文改訂で測定対象が動いた場合の規則を決めるIssueだからである。#747の担い手はADR化する側の2分岐、#724の担い手は圧縮で失われた測定対象の帰趨規則、#749の担い手はCASE-02 / 14 / 24の命名規約に関する期待値更新と走行であり、表の各行にも境界を残した。

**追記（2026-08-23、#621 の確定による）**: 上の2段落は #761 の判定時点の記録であり、当時の #747 が持っていた「2分岐を撃ち分ける題材の追加」を前提に置いている。#621 が当該分岐を測らないと確定したため、この受入条件は #747 本文から削除済みであり、**#747 の担い手は CASE-04 ↔ CASE-12 の対の回復である**。再編しないという結論は、残る受入条件（題材の追加・改訂と走行）と #724（規則の決定と文書化）の作業型が依然として異なることで保たれる。次の担い手は、上の記述から #747 の射程を2分岐の側へ復元しないこと。

**追記（2026-08-25、#761 第2ラウンドによる）**: 上記の射程に**並置する形で**、項目1 の陽性側・項目2 の陰性側の測定点の回復を #747 の射程へ追加した（上表の当該2行を参照）。原因（必要条件層の織り込みによる不問化）と作業型（題材の追加・改訂＋再走行）が既存射程と一致するためである。#724 との再編要否の結論（再編しない）は、この追加によって変わらない。あわせて #749 の担い手も動いた——上の2段落は「CASE-02 / 14 / 24 の命名規約に関する期待値更新と走行」と述べているが、本ラウンドで CASE-02・CASE-03 の**点数欄**の確定が #749 の受入条件へ加わり、CASE-14 / CASE-24 は点数欄更新の対象に入らないことが確定した（下記「CASE-02 項目1・3の帰趨（AC4）」を参照）。上の2段落の #747・#749 の担い手の記述は、いずれも判定時点の記録として読むこと。なお #747 へ追加した受入条件は、当初「項目1 を常に 0 へ・項目2 を常に 1 へ潰す退行が少なくとも1題材で差として現れる」という形で書いていたが、項目2 側は CASE-08・CASE-23 の合計経由の間接経路により題材を足さずとも既に充足される。本PRのレビュー指摘を受け、**セル単位の差**として現れることを求める形へ改めた（2026-08-25。上表の項目2 行を参照）。

#### 必要条件不成立17件の棚卸し要否（AC3）

`expectations.tsv` の `不成立` は、注記行を含む素の文字列一致が25件、注記行を除く行全体一致が22件、12列目 `期待_必要条件` の列指定が17件である。差分5件は CASE-08 / CASE-11 / CASE-23 / CASE-30 / CASE-32 で、`不成立` の出現列はそれぞれ CASE-08・23・32が2列目 `導出すべきもの`、CASE-11・30が11列目 `備考` である。したがって棚卸しの母集団は17件（CASE-01 / 05 / 06 / 07 / 10 / 12 / 13 / 14 / 17 / 18 / 19 / 20 / 24 / 25 / 27 / 28 / 29）に限る。

この17件は項目1〜項目4の全欄が `-` で例外がなく、必要条件層による一括不問化である。他方、必要条件成立15件のうち12件にも `-` はあるが、これは各題材が測る項目だけに値を持つ設計上の非測定であり、現象が異なる。よって17件を新たな一括棚卸しのIssueへ広げず、既存の必要条件一般則に従って不問のまま確定する。新規Issueは起票しない。ただし成立15件の側には設計上の非測定に収まらない残余があり、その切り分けを下記の判別子で行う。CASE-02 の項目1・項目3 はこの残余にあたるため17件の結論の対象外とし、次項で扱う。

**この不問確定は識別力の退役を含む。** `936c096` 時点で `期待_項目1=1` を持っていたのは CASE-01 / CASE-28 / CASE-29、`期待_項目2=0` を持っていたのは CASE-25 であり、4件とも現在は `期待_必要条件=不成立` で点数欄が全 `-` である。したがって上記17件の不問確定は、**項目1 の陽性側と項目2 の陰性側の識別力を退役させることを含む**（回復後の測定点は 項目1 が CASE-09 / 11 / 32 の3件すべて 0、項目2 が CASE-02 / 09 / 11 / 32 の4件すべて 1 であり、反対の極は0件である）。不問のまま確定するという結論は、この識別力の回復を #747 が追跡することを前提として成り立つ（上表の項目1 行・項目2 行を参照）。

##### 成立側の残余を切り出す判別子

残余の判別子は、§11 の表が当該題材を名指ししているかどうかではなく、**題材の状態**で定義する。次の3つをすべて満たす題材を残余とする。

1. `expectations.tsv` 12列目 `期待_必要条件` が `成立` である
2. 3〜6列目（`期待_項目1`〜`期待_項目4`）のいずれかが `-` である
3. 当該題材の**4項目分**の確定値が、11列目 `備考` に在る。**走行記録は確定値源に数えない**——走行記録の2試行一致値は、備考の確定値を再確認する材料として数えるに留める

条件3 が求めるのは「4項目分の確定値が備考に在る」ことであって、特定の値列ではない。合致2件はいずれも備考が逐語「確定値は 0/1/0/1」で始まるが、これは実例であって要件ではなく、別の値列を持つ題材が将来現れても4項目分が揃っていれば合致とする。値列そのものを要件に読むと、`期待_行き先` の逐語一致検査を退けた §11 前段の判断（値域が型として定まっていない自由文字列の上に語の一致を積んでも検査として意味を持たない）とも揃わない。

条件3 が確定値源を備考に限る理由は2つある。§2.3 が「`expectations.tsv` の期待値を人手で導出する際」と述べ、期待値の導出を人手によるものとして前提していること。および判定値をそのまま期待値へ昇格させると期待値層が判定の写しになり、判定器の退行を期待値層が検出できなくなる（自己言及になる）ことである。数える立場を採ると、下表の12件は各題材の現行の走行記録ではいずれも独立2試行が一致しているため残余が12件へ広がり、判別子が残余の切り出しとして機能しなくなる（CASE-02 は 2026-08-15 の走行では項目2 が試行間で割れており、一致したのは題材文改訂後の #624 の再走行である）。

条件3 が**4項目分**を求めるのは、期待欄の `-` を埋める作業が4項目を一組として確定することを要するためである。1項目だけの確定値（例: 「項目3 の確定値は0点」）は、その項目が既に期待欄へ入っていれば残余を生まず、`-` のまま残る他の欄については何も定めない。したがって部分的な確定値を持つ題材は残余に含めない。

**判別子の正本は本項の3条件である。** 後段の「CASE-02 項目1・3の帰趨（AC4）」と `defect-ledger.md` §7 が述べる判別子は本項の要約であり、正本ではない。第2ラウンドの初期案では条件3 を「備考または `runs/` の走行記録に在る」という選言で書いていたが、当時の但し書きが走行記録の2試行一致値を単独では数えないと定めていた以上、この選言肢を満たす題材は原理的に0件である（`runs/` 配下に「確定値」の記録は1件も無い）。要約の2箇所は当初から選言なしで述べており、3箇所で形が割れていた。正本を1箇所に定めるため選言を落とし、あわせて #761 受入条件9 の「第3項は選言のまま残す」という定めも 2026-08-25 に改訂した（本PRのレビュー指摘による）。

2026-08-25 の実測では、`期待_必要条件=成立` の15件のうち期待欄に `-` を持つのは次の12件であり、判別子に合致するのは **CASE-02 と CASE-03 の2件**である。

| 題材 | `-` の欄 | 備考が持つ確定値 | 判別子 | 帰趨 | 理由 |
|---|---|---|---|---|---|
| CASE-02 | 項目1・項目3 | 4項目分（逐語「確定値は 0/1/0/1」） | 合致 | #749 への申し送り（#749 で実施済み。2026-08-27） | 確定値が備考に在るのに期待欄が `-` のまま残っている。期待値層の更新と再走行を要するため実施先へ送る（#749 本文へ受入条件を追加済み） |
| CASE-03 | 項目1・項目2・項目4 | 4項目分（逐語「確定値は 0/1/0/1」） | 合致 | #749 への申し送り（#749 で実施済み。2026-08-27） | CASE-02 と同一の状態にあり、作業型（期待値層の更新＋再走行）も一致するため同じ実施先へ送る（#749 本文へ受入条件を追加済み） |
| CASE-04 | 項目1・項目2・項目4 | 項目3 のみ（期待欄に反映済み） | 非合致 | 不問のまま確定 | 4項目分の確定値を持たない。備考は `-` の3欄を「未特定」と明示する |
| CASE-08 | 項目1・項目2・項目4 | 項目3 のみ（期待欄に反映済み） | 非合致 | 不問のまま確定 | 同上 |
| CASE-15 | 項目1・項目2・項目4 | 項目3 のみ（期待欄に反映済み） | 非合致 | 不問のまま確定 | 同上 |
| CASE-16 | 項目1・項目2・項目4 | 項目3 のみ（期待欄に反映済み） | 非合致 | 不問のまま確定 | 4項目分の確定値を持たない。備考は `-` の3欄を「一次資料に無く」と明示する |
| CASE-21 | 項目1・項目2・項目3 | 無し | 非合致 | 不問のまま確定 | 4項目分の確定値を持たない。備考は「全項目が未特定」と明示する |
| CASE-22 | 項目1・項目2・項目3 | 無し | 非合致 | 不問のまま確定 | 同上 |
| CASE-23 | 項目1・項目2・項目4 | 項目3 のみ（期待欄に反映済み） | 非合致 | 不問のまま確定 | 4項目分の確定値を持たない。備考は `-` の3欄を「一次資料に無く」と明示する |
| CASE-26 | 項目1・項目2・項目4 | 無し | 非合致 | 不問のまま確定 | 4項目分の確定値を持たない。備考は `-` の3欄に触れない |
| CASE-30 | 項目1・項目2・項目4 | 無し | 非合致 | 不問のまま確定 | 同上 |
| CASE-31 | 項目1・項目2・項目3 | 項目3 のみ（期待欄は `-` のまま） | 非合致 | 不問のまま確定 | 4項目分の確定値を持たない。備考は `-` 欄のうち項目3 について「段1で0に確定する設計」と述べるが、条件3 の入力事実を題材文へ置かない設計のため期待欄は `-` に留めており、項目1・項目2 については何も述べない |

非合致10件は、**4項目分の確定値が備考に記録されていない**という状態によって除外している。除外の述語は「題材の側に記録が無い」ではなく備考に限る——上表の12件はいずれも現行の走行記録では独立2試行が一致しており（条件3 が確定値源を備考に限る理由で述べたとおり）、走行記録まで含めた「題材の側」では除外が成り立たないためである。**非合致10件のうち** `-` 欄について備考が値を述べているのは CASE-31 の項目3 のみであり、これが条件3 の「4項目分」という要求が現に帰趨を分ける唯一の箇所である。合致2件（CASE-02・CASE-03）の備考は `-` 欄の値をまさに4項目分述べているため、この母集団には入らない。「§11 が名指ししていない」といった記録の置き場所に由来する理由では除外していない。合致2件のうち CASE-02 の帰趨は第1ラウンドで #749 への申し送りに確定しており、CASE-03 は本ラウンドで同じ実施先へ送ると判定した（同一の判別子に合致する2件が別々の帰趨を持つと、切り分けの根拠が題材の性質ではなく実施先の有無になるため）。

なお、照合器 `plugins/adr/scripts/adr-scoping-cases.sh` は期待欄が `-` の欄を差の判定から除外する。したがってこの残余は `report` の「期待帰結との差」を0件に保ったまま残り続け、走行だけでは表面化しない。

#### CASE-02 項目1・3の帰趨（AC4）

CASE-02は必要条件成立側なので上記17件に含まれず、成立側の設計上の非測定とも異なる。切り出しの根拠は前項の判別子である（正本は前項。以下はその要約）——`期待_必要条件=成立`・備考に4項目分の確定値が在る・それでも期待欄に `-` が残る、の3条件を満たす。CASE-02 の備考はその確定値を「確定値は 0/1/0/1」と述べている。§11 の表が「期待_項目1・項目3を不問のまま据え置いた」と述べているのは、この状態を記した記述であって切り出しの根拠ではない。一次情報を再確認すると `期待_項目3` も `-` のままで、項目1と同じく確定値の期待値未反映であるため、同じ残余に含める。同じ判別子には CASE-03 も合致し、帰趨は前項の表のとおり同じ実施先へ送る。

一次情報では、`runs/2026-08-16-case624-judgments.tsv` の独立2試行が現行題材集合commit `4d35d86` に対して、いずれも `項目1=0、項目2=1、項目3=0、項目4=1` を導出している。`expectations.tsv` 11列目の備考も同じ再確認を記録し、題材集合の最新変更も `4d35d86` で一致する。したがって「現行題材文のまま2試行で導出済み」という事実は判定入力になる。

§11の「3題材×2試行」は、測定点消滅の解消を示すCASE-09・CASE-11・CASE-32の範囲を数えた記述であり、CASE-02の走行実績を否定する記述ではない。一方、CASE-02を期待値側で不問のままにした個別帰趨は残っている。この食い違いは走行記録の取りこぼしではなく、測定点の解消を示す母集団と期待値側の先送りを別の範囲で記述したものと判定した。

帰趨は「既存Issueへ申し送る」とし、実施先を **#749** に確保する。第1ラウンドの時点では #749 の受入条件が `期待_行き先` の更新と行き先の走行差だけを明示しており、`期待_項目1`・`期待_項目3` の更新を保証していなかった。第2ラウンド（2026-08-25）で **#749 本文へ受入条件を追加済みであり**、CASE-02 と CASE-03 の点数欄の確定と、それを含む独立2試行の差が点数欄まで0件であることが #749 の完了条件に入っている。したがって本節の申し送りは、未実行の前提ではなく実在する受入条件に立つ。

追加した受入条件の対象は **CASE-02 と CASE-03 の2題材**である。本節の従前の記述は対象を「CASE-02を含む3題材」としており、#749 の従来スコープ（CASE-02 / CASE-14 / CASE-24 の `期待_行き先` の更新）に照らせば残り2件は CASE-14 / CASE-24 を指す。しかし CASE-14 / CASE-24 は `期待_必要条件=不成立` であり、`expectations.tsv` 冒頭注記の定めにより点数欄が全 `-` であることが規約上正しい状態である。#749 が改訂するのは命名規約節であって必要条件ではないため、改訂後も両題材の点数欄は `-` のままが正しい。両題材の点数欄更新を要求すると原理的に満たせない受入条件になるため、対象に含めない（両題材の `期待_行き先` の更新は #749 の従前の受入条件が引き続き担う）。#724は個別題材の実施をOUTとしており、ここでは条文改訂で測定対象が動いた場合の規則決定までを参照する。期待値更新・再走行は本Issueの範囲外とし、`defect-ledger.md` §7にも #749 を記録する。

## 12. 出所の巡番号の呼称

`cases.md` と `expectations.tsv` の出所列が使う巡番号は、`sources.md` が定めた数え方に従う。`sources.md` は PR #603 のコミットハッシュと投稿順の通番を一次キーとして巡を数え直しており、これを正とする。

PR #603 のレビュー往復では巡4 の応答が2件あるため、レビュー側コメントの通番と巡番号が巡5 以降で1つずれる。題材集合の中では `sources.md` の呼称で統一する。出所を辿る際は `sources.md` の対応表からコミットハッシュと投稿順通番へ落として確認する。

Issue #605 の実装プランはずれた側の呼称を使っている箇所があるが、実装プランは追跡対象外（`docs/plans/` はリポジトリへ含めていない）であるため、呼称の一次出典としては扱わない。
