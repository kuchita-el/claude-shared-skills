# 判定記録の集計レポート（2026-08-15、CASE-25〜29 / Issue #703）

## 本レポートが測っているもの

本レポートが測っているのは、**同一入力に対する判定の再現性**である。隣接題材の文脈効果は測っていない。題材は1件ずつ判定側へ渡しており、判定側の入力に他の題材は現れない。独立性の根拠は、試行ごとに新規コンテキストの起動（`claude -p` の別起動）とし、前試行の判定・記録・他題材の判定を一切引き継がせていないことに置く。

本レポートは2本の集計を持つ。**部分集計**は本走行5題材（CASE-25〜29）だけを対象とし、走行そのものの健全性を見る。**全体集計**は既存 `runs/2026-08-15-judgments.tsv`（CASE-01〜24）と本走行の判定記録を連結し、Issue の「対象」欄第2項が求める現行対象文書に対する29題材の全体ベースラインを取る。

## 実行条件

| 項目 | 値 |
|---|---|
| 対象文書 | `plugins/adr/skills/manage-adr/references/adr-scoping.md` |
| 対象文書の commit | `03a5b79`（本走行10行、既存48行のいずれも一貫。連結した58行の13列目が単一の値であることを確認済み） |
| 題材集合（`cases.md`）の commit | `10fb0f7`（本走行10行） |
| 実行日 | 2026-08-15 |
| 題材数（本走行） | 5（`CASE-25`〜`CASE-29`） |
| 試行数 | 2（題材ごとに独立） |
| 判定の担い手 | `claude -p --model opus --effort high`。実効モデルは各起動の `modelUsage` で `claude-opus-5`（10件全件で一致）。題材1件・試行1件につき1起動、起動間で文脈を引き継がせていない |
| 判定記録（本走行） | `runs/2026-08-15-cases25-29-judgments.tsv`（10行） |
| 返却全文（本走行） | `runs/2026-08-15-cases25-29-raw-returns.md`（10件） |
| 破棄した試行 | 3件（`原文列挙_N` の不使用。詳細は `runs/2026-08-15-cases25-29-raw-returns.md` の「破棄した試行」節） |
| 既存走行（CASE-01〜24） | `runs/2026-08-15-judgments.tsv`（48行、実行日 2026-08-15、Issue #721 AC6 の走行。担い手条件は本走行と同一） |

事後検査として、本走行10行の参照ファイル一覧を確かめた。判定側が**能動的に開いた**ファイルは、いずれも対象文書 `adr-scoping.md` と契約仕様 `adr-judgment-contract.md` の2つに限られており、改訂の経緯・レビュー履歴・過去の判定記録・期待帰結を記した資料を自ら読んだ試行は無い。10件全件が組み立て済みプロンプトファイル自身を `参照ファイル` 欄へ挙げていないが、読まずに判定することは不可能であるため申告の欠落と扱う（既存走行でも同型の申告欠落が5件ある）。ただし実行基盤による自動注入があり、入力の限定は完全には守られていない（後述「入力の限定に関する限界」）。

## 部分集計（本走行5題材）

一時ディレクトリ（リポジトリ外）へ `expectations.tsv` を複製し、`cases.md` は CASE-25 の見出し以降だけを残した形で複製して、本走行の判定記録単独に対して掛けた。

```
== カバレッジ ==
  題材 5 件中 5 件を記録が覆う

== 試行間一致（セル単位。項目1〜4 を1セルと数える） ==
  試行 1 と 試行 2: 一致 4 / 4 セル (100.0%)
    項目1: 一致 1 / 1 (100.0%)
    項目2: 一致 1 / 1 (100.0%)
    項目3: 一致 1 / 1 (100.0%)
    項目4: 一致 1 / 1 (100.0%)
    合計: 一致 1 / 1 (100.0%)
    行き先: 一致 5 / 5 (100.0%)

== 各項目の1点率（周辺分布。試行ごと） ==
  試行 1:  項目1 0.0% (0/1)  項目2 100.0% (1/1)  項目3 100.0% (1/1)  項目4 100.0% (1/1)
  試行 2:  項目1 0.0% (0/1)  項目2 100.0% (1/1)  項目3 100.0% (1/1)  項目4 100.0% (1/1)

== 期待帰結との差 ==
  差は無い
```

必要条件が成立したのは CASE-26 の1件のみ（CASE-25/27/28/29 はいずれも両試行とも不成立で、`expectations.tsv` の期待_必要条件と一致）。数値セル（項目1〜4）の分母は CASE-26 の1件へ縮退している。

**κ（部分集計）は数値として掲げない。** 分母が1件のため、4項目とも試行1・試行2 の1点率が完全に一致している（項目1: p1=p2=0.0、項目2〜4: p1=p2=1.0）。周辺分布が両試行とも同一値であるため偶然一致率 Pe = p1×p2 + (1−p1)×(1−p2) = 1 となり、κ =（Po − Pe）/（1 − Pe）は分母0で**未定義**である。4項目すべてがこの型（Pe=1 による未定義）に当たり、分母1で解釈不能の型（p1≠p2 で形式上 κ=0 が出る型）は本走行には現れなかった。

**期待帰結との差は0件。** J5（期待帰結との差が出た場合の帰属先）の判断は、差が現れなかったため発動しない。

## 全体集計（29題材。現行全体ベースライン）

一時ファイル（リポジトリ外）へ `runs/2026-08-15-judgments.tsv`（CASE-01〜24、48行）と `runs/2026-08-15-cases25-29-judgments.tsv`（CASE-25〜29、10行）を連結し、リポジトリ本体の題材集合ディレクトリ（`docs/development/adr-scoping-cases`）へ掛けた。既存3ファイル（`2026-08-15-judgments.tsv` / `2026-08-15-raw-returns.md` / `2026-08-15-returns/`）は改変していない。

```
== カバレッジ ==
  題材 29 件中 29 件を記録が覆う

== 試行間一致（セル単位。項目1〜4 を1セルと数える） ==
  試行 1 と 試行 2: 一致 43 / 44 セル (97.7%)
    項目1: 一致 11 / 11 (100.0%)
    項目2: 一致 10 / 11 (90.9%)
    項目3: 一致 11 / 11 (100.0%)
    項目4: 一致 11 / 11 (100.0%)
    合計: 一致 10 / 11 (90.9%)
    行き先: 一致 29 / 29 (100.0%)

== 各項目の1点率（周辺分布。試行ごと） ==
  試行 1:  項目1 9.1% (1/11)  項目2 45.5% (5/11)  項目3 36.4% (4/11)  項目4 100.0% (11/11)
  試行 2:  項目1 9.1% (1/11)  項目2 36.4% (4/11)  項目3 36.4% (4/11)  項目4 100.0% (11/11)

== 期待帰結との差 ==
  CASE-02 試行2 項目2: 期待 1 / 判定 0
  CASE-21 試行1 行き先: 期待 ADR化しない/実装・テスト資産・操作手順 / 判定 ADR化しない/PR説明
  CASE-21 試行2 行き先: 期待 ADR化しない/実装・テスト資産・操作手順 / 判定 ADR化しない/PR説明
  差 3 件
```

「未知の題材ID」「重複した行」「commit 列の未確定」の報告はいずれも出力に現れず、`report` は exit 0 で終わった。カバレッジが29題材中29題材であることとあわせて、連結が両走行を過不足なく取り込んでいることを確認した。連結した58行（見出し行2行を含む60行から算出）の13列目はすべて `03a5b79` で単一であり、この全体集計を単一版のベースラインとして読める（J2 と同一の条件）。

**κ（全体集計）は項目によって算出できる。** 既存24題材の数値セルが分母へ入るため、周辺分布が0にも1にも振り切れていない項目は README §9 の式（Pe = p1×p2 + (1−p1)×(1−p2)、κ =（Po − Pe）/（1 − Pe）、Po は実測一致率）で手計算した。

| 項目 | 試行1の1点率 | 試行2の1点率 | Pe | Po | κ |
|---|---|---|---|---|---|
| 項目1 | 9.1%（1/11） | 9.1%（1/11） | 0.835 | 1.000 | 1.000 |
| 項目2 | 45.5%（5/11） | 36.4%（4/11） | 0.512 | 0.909 | 0.813 |
| 項目3 | 36.4%（4/11） | 36.4%（4/11） | 0.537 | 1.000 | 1.000 |
| 項目4 | 100.0%（11/11） | 100.0%（11/11） | **1.000（未定義）** | 1.000 | **未定義** |

項目4 は両試行とも1点率100%で周辺分布が1へ振り切れており、Pe=1 のため κ は未定義である。README §9 の「安定しているが無情報」（該当率が高いが判定を分けていない）に当たる項目であり、2026-08-15 走行（24題材）の時点で既に同じ状態だった。項目1・項目3 は κ=1.000 で実測一致も完全である。項目2 だけが κ=0.813 で1未満であり、CASE-02 試行2 の1件差（後述）に対応する。標本が29件・11件（数値セル分母）の水準である以上、κ も一致率も単独では読めない旨は README §9 のとおりである。

## 期待帰結との差の帰属

全体集計に現れた差3件はいずれも CASE-02・CASE-21（既存の CASE-01〜24 走行に由来）であり、**本走行（CASE-25〜29）に由来する差は0件**である。新たな帰属付けの対象は本走行の5題材に由来する差に限られるため、本Issueの範囲では帰属作業は発生しない。

参考として、既存の3件は `defect-ledger.md` の A-02・A-03 で既に帰属済みである。

- **CASE-02 試行2 項目2、CASE-21 行き先2件**: `defect-ledger.md` 50行目（A-02）が「残るCASE-02試行2の項目2差とCASE-21の行き先差は別原因として記録する」と明記し、CASE-21 の行き先差は同台帳51行目（A-03、「判定結果の行き先」節の但し書きが検査皆無の場合を明示しない一意性の欠陥）に帰属済み。CASE-02 の項目2差は Issue #641 による非本数条件削除後の再導出（B-02、64行目）に伴う既知の差である。

本走行（CASE-25〜29）が新たに動かした差は無いため、`defect-ledger.md` への追記は行っていない。

## 入力の限定に関する限界

判定側は対象文書と契約仕様のみを読む建て付けだが、既存走行と同様に**実行基盤の水準でこれが守られていない**。Claude Code は起動時にリポジトリルートおよびユーザースコープの `CLAUDE.md` を自動でコンテキストへ載せる。本走行10件のいずれも `CLAUDE.md` を挙げた申告は無いが、申告が無いことは注入が無かったことの証拠にならない。この逸脱は判定側の操作ではないため破棄していない（README §7.1）。

本リポジトリの `CLAUDE.md` は ADR の粒度判定基準の実体を持たず、対象文書そのものを指すポインタに留まるため、判定の入力として新たな規定を与えてはいない。ただし ADR 運用の存在自体は伝わるため、行き先の分岐に対する事前分布を動かした可能性は本走行でも排除できていない（2026-08-15 走行の記録と同一の限界）。

## 本走行が動かさなかった既知の穴

`README.md` §11 の既知の穴のうち、本走行の着手前に追跡列に #703 を持っていたのは「項目1 の測定対象が消滅した」「項目2 の測定対象が1件へ縮退した」の2行のみである。CASE-25/27/28/29 の必要条件が両試行とも不成立（期待どおり）であったため、この2行の測定対象はいずれも本走行では復活していない。本PRはこの実測結果に基づき §11 の当該2行から追跡列の #703 を外した（`README.md` §11 を参照）。

残る6行（条件2 の陽性が0件・項目4 の境界題材の消滅・ADR化する側の2分岐の潰れ・必要条件の織り込みによる縮退・圧縮での測定対象喪失・CASE-09/11 が条件3に反応しない）は本走行が動かさない。

## 測定の限界

- **標本が小さい。** 部分集計は題材5件・数値セルの分母は1件に縮退しており、κ も一致率も単独では読めない。全体集計も題材29件・数値セルの分母11件の水準であり、同様の限界を持つ（2026-08-15 走行時点の限界がそのまま引き継がれる）。
- **担い手が1系統である。** 本走行の判定はすべて `claude -p --model opus --effort high` の同一担い手が行った。測れているのは同一の担い手における再現性であり、判定者間信頼性ではない。
- **記録の一部が人手の再符号化である。** 根拠4列・参照ファイル一覧列は、判定側の返却を記録者が圧縮・写像した派生物である。返却全文は `runs/2026-08-15-cases25-29-raw-returns.md` に10件全件を保持しており、符号化の妥当性を原文へ突き合わせて確かめられる（既存24題材の一部にはこの原文保全が無い。2026-07-29-report.md の限界節を参照）。
- **改訂前の基準としてのみ意味を持つ。** 本レポートの数値は、対象文書を改訂したときに同じ手順で通して差を見るための基準値である。単独では対象文書の品質を評価しない。

## 対象文書への差分

本Issueでは対象文書を改訂しない（判断依頼 J5 は差0件のため発動せず、判断依頼 J2 の前提どおり走行完了まで対象文書は `03a5b79` のまま動いていない）。`git diff` および `git log main..HEAD -- plugins/adr/skills/manage-adr/references/adr-scoping.md` のいずれも出力が空であり、本ブランチで対象文書を変更したコミットは無い。
