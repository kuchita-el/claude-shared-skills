# #749 部分走行レポート（2026-08-27）

## 実行条件

- 対象: CASE-02 / CASE-14 / CASE-24、各2試行（計6試行）
- 対象文書: plugins/adr/skills/manage-adr/references/adr-scoping.md（`2aaed06`。命名規約節へ射程と適用順を条文化した改訂と、そのセルフレビュー指摘・PRレビュー指摘の反映の後）
- 題材集合: `4d35d86`（題材文は本Issueで触っていない）
- 担い手: `claude -p --model opus --effort high`、題材1件・試行1件につき別起動
- 判定記録: runs/2026-08-27-issue749-judgments.tsv
- 返却全文: runs/2026-08-27-issue749-raw-returns.md
- 構造化返却: runs/2026-08-27-issue749-returns/
- 破棄した試行: なし（本走行の6試行はいずれも返却型検査と事後検査に通った）。過去3回の走行では各1件を破棄している（「事後検査」節・「返却の必須フィールドの欠落」節を参照）

### 走行を採り直している

本記録は**4回目の走行**である。1回目は条文化の改訂（`4abad2a`）、2回目はセルフレビュー指摘の反映（`54f8755`）、3回目は PRレビュー指摘への対応（`d1c8d83`。ファイル配置規約の射程内明示と複合識別子の区切り記号の帰属）に対して、いずれも同一手順で採った。3回ともその後に命名規約節を再度改訂したため、前3回の記録は対象文書の現行本文と食い違う。**改訂後の対象文書に対する走行であることが AC5 の要件であるため、`2aaed06`（射程の説明が3類型を上書きしうる読みを塞ぎ、区切り記号の帰属規則を全域化した改訂）に対して採り直し、前3回の記録は残していない。** 4走行の帰結は8試行とも一致した（点数・行き先・必要条件の成否のすべて）。

## report の結果

3題材の一時複製（走行対象だけを持つ題材集合の複製。コミットしていない）へ `report` を実行した。終了状態は0であった。

- 3題材、6試行、カバレッジ 3/3
- 試行間一致: セル単位 4/4、行き先 3/3
- **期待帰結との差: 0件**

差が0件であるため、帰属（規範側の一意性の欠陥か題材文の記述密度の不足か）を記す対象は無い。

## 判定結果

| 題材 | 試行 | 必要条件 | 項目1〜4 | 行き先 | 期待_行き先 |
|---|---|---|---|---|---|
| CASE-02 | 1 | 成立 | 0/1/0/1 | `ADR化しない/共有規約文書` | 一致 |
| CASE-02 | 2 | 成立 | 0/1/0/1 | `ADR化しない/共有規約文書` | 一致 |
| CASE-14 | 1 | 不成立 | 数えない | `ADR化しない/PR説明` | 一致 |
| CASE-14 | 2 | 不成立 | 数えない | `ADR化しない/PR説明` | 一致 |
| CASE-24 | 1 | 不成立 | 数えない | `ADR化しない/PR説明` | 一致 |
| CASE-24 | 2 | 不成立 | 数えない | `ADR化しない/PR説明` | 一致 |

CASE-02 は点数欄を含めて期待値と一致した（本Issueで `期待_項目1`・`期待_項目3` を確定値 0 へ充填したため、点数欄も比較対象に入っている）。CASE-14 / CASE-24 は `期待_必要条件=不成立` であり点数欄が全て不問記号のため、行き先のみを比較した。

## 改訂前の走行との突き合わせ

条文を改訂した時点で判定が動きうるため、改訂前後で帰結が動かないことを確かめるのが本走行の目的である。担い手の条件（`claude -p --model opus --effort high`、題材1件・試行1件につき別起動）は 2026-08-15 以降の走行と同一であるため、2つの走行の差は条文改訂へ帰属させられる。2026-07-29 の走行は担い手が汎用サブエージェントで model / effort の記録が無いため、担い手の同一性を根拠にした比較対象には採っていない。

| 題材 | 改訂前（`03a5b79`）の判定 | 改訂後（`2aaed06`）の判定 | 動いたか |
|---|---|---|---|
| CASE-02 | `runs/2026-08-16-case624-judgments.tsv` の2試行とも 0/1/0/1・`ADR化しない/共有規約文書` | 2試行とも 0/1/0/1・`ADR化しない/共有規約文書` | 動いていない |
| CASE-14 | `runs/2026-08-15-judgments.tsv` の2試行とも不成立・`ADR化しない/PR説明` | 2試行とも不成立・`ADR化しない/PR説明` | 動いていない |
| CASE-24 | `runs/2026-08-15-judgments.tsv` の2試行とも不成立・`ADR化しない/PR説明` | 2試行とも不成立・`ADR化しない/PR説明` | 動いていない |

CASE-02 の比較対象に `runs/2026-08-15-judgments.tsv` ではなく `runs/2026-08-16-case624-judgments.tsv` を採ったのは、前者の試行2 が改訂前の題材文に対する判定であり（項目2=0。題材文の記述密度に帰属する差として #624 が扱った）、題材文が現行と異なるためである。後者は現行題材文（`4d35d86`）に対する走行であり、題材文を揃えた比較になる。

### この走行が示すことと、示さないこと

**示すこと（退行検査）**: 条文改訂の前後で3題材とも帰結が動いていない。

**示さないこと（案A と案B の判別）**: 本走行は、識別子の採番形式を射程外とする読み（案A）が射程内とする読み（案B）より正しいことの実測ではない。3題材とも、走行では両案が同じ値を出すためである。

- **CASE-14 / CASE-24**: 返却は `必要条件_成立: false` である。改訂後の「適用順」1項により、必要条件が不成立なら点数を数えず本表も末尾の例外も掛からず、行き先は一般則3分岐が決める。射程の判定へ到達する前に決着しており、`ADR化しない/PR説明` は射程の記述と独立に出る値である
- **CASE-02**: `cmd_derive` の「ADR 化しない」側は一般則3分岐しか持たず、命名規約節の例外に対応する分岐を持たない。案B を採ったときの候補値 `ADR化しない/実装・テスト資産・操作手順` は導出器が原理的に出せず、`項目4_阻止状態=検査なし` かつ `複数作業者=true` から `ADR化しない/共有規約文書` が出る経路は両案で同じである

これは `defect-ledger.md` の A-02 行が「返却の実測事実が命名規約の類型を表すキーを持たず導出器が例外側の分岐を出せない」と記す事実の帰結である。案A の射程の述語が識別子の採番形式を射程外と導けることを確かめた根拠は、改訂後の射程の述語を32題材へ1件ずつ当てた人手の適用の側にある（README §11 の「命名規約節の例外を測る題材が0件になった」行）。

## 事後検査（README §7）

6試行すべてについて `参照ファイル` 欄を検査した。挙がったのは次の2ファイルのみで、いずれも README §5 の許可対象である。

- `plugins/adr/skills/manage-adr/references/adr-scoping.md`（対象文書）
- `plugins/adr/skills/manage-adr/references/adr-judgment-contract.md`（対象文書が名指しする契約仕様）

本走行では破棄していない。**3回目の走行（`d1c8d83` に対するもの）では、CASE-14 試行2 の初回返却が `参照ファイル` 欄へ `plugins/adr/skills/manage-adr/references/adr-scoring-thresholds.json` を挙げていた**。これは判定側が能動的に開いたファイルであり、README §7.1 の自動注入には当たらない。§7 に従い無効として破棄し、同題材・同試行をやり直した。破棄した返却は判定記録・返却全文・構造化返却のいずれにも書いていない。

やり直した返却でも、4回目の走行の8試行でも、同じファイルは挙がっていない。**判定器が設定ファイル（しきい値）へ自ら手を伸ばすことがある**ことは、本題材集合において初めて観測された逸脱の型である。プロンプト雛形は読んでよいものを3つに限定しているが、対象文書は判定手続きの中でしきい値を参照するため、判定側がその実体を開きにいく誘因が残る。延べ35回の起動で1件の観測であり、再現性は低い。

## 返却形式の逸脱（フェンス）

4回の走行すべてで、全試行の返却が ` ```json ` フェンスで包まれていた（4回目の8試行も例外なく同じ形であった）。プロンプト雛形は「JSON オブジェクトだけを返すこと。前置き・要約・感想・提案を付けない」と指示しており、前置き・要約・感想・提案の混入は1件も無かったが、フェンスは付いていた。

- 破棄の対象としなかったのは、これが全試行に一様に現れる担い手側の直列化の癖であり、特定の試行の欠陥ではないためである。やり直しても同じことが起きる
- 起動側から返却形式を上書きする指示（「フェンスを付けるな」等）は足していない。足すと、対象文書とプロンプト雛形だけで判定が決まるという設計から外れる
- 転記時にフェンスのみを外し、内容は加工していない。フェンスを外した内容は JSON オブジェクト単体である

## 返却の必須フィールドの欠落（1回目・2回目の走行で各1件）

返却型検査に通らない返却が、1回目と2回目の走行でそれぞれ1件出た（1回目は CASE-24 試行1、2回目は CASE-03 試行2）。いずれも新スキーマの必須フィールド `欠測` を欠いていた。判定の内容ではなく形式の欠落であり、README 手順5 に従って無効として破棄し、同題材・同試行をやり直した。破棄した返却は判定記録・返却全文・構造化返却のいずれにも書いていない。**4回の走行の起動回数は延べ35回（記録に採った8試行 × 4 と、破棄した3件）であり、そのうち2件で再現している。** 担い手が偶発的に必須フィールドを落とすことがある（雛形は `"欠測": {}` を含む形で返却例を示している）。分母を記録済みの32試行では取れないのは、欠落した2件が破棄されて記録に入っていないためである。3回目・4回目の走行では8試行とも `欠測` を備えていた。

## 入力の限定に関する限界

実行基盤が `CLAUDE.md`（ユーザースコープおよびリポジトリルート）を判定側のコンテキストへ自動注入する既知の逸脱の可能性は、README §7.1 のとおり残る。本走行の担い手は `claude -p` であり、この注入は判定側の操作ではないため、破棄・やり直しでは解消しない。6試行のいずれも `参照ファイル` 欄へ `CLAUDE.md` を挙げていないが、挙げなかったことは注入が無かったことの証明にならない。

## 禁止語走査

題材文（`cases.md`）は本Issueで触っておらず、本走行の判定記録14列目は3題材とも `4d35d86` である。改訂前の走行の14列目は題材によって分かれる——CASE-02 の比較対象（`runs/2026-08-16-case624-judgments.tsv`）は `4d35d86` で本走行と同一だが、CASE-14 / 24 の比較対象（`runs/2026-08-15-judgments.tsv`）は `10fb0f7` である。ただし `10fb0f7` と `4d35d86` の差分は CASE-02 近傍・CASE-09 / 11 近傍と CASE-30 / 31 / 32 の追加に限られ、**CASE-14 / 24 の題材文ブロックは両版で同一である**（ブロック単位のハッシュ照合で確認）。比較対象の題材文が動いていないため、帰結の差を条文改訂へ帰属させる根拠は保たれる。題材文へ新たな語が混入する余地も無いため、題材文ブロックへの禁止語走査は本走行では実施していない。
