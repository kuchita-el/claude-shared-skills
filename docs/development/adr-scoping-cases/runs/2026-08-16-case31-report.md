# CASE-31 部分集計レポート

## 測っているもの

CASE-31を同一入力で2回判定した再現性を測っている。隣接題材の文脈効果や判定者間信頼性は測っていない。

## 実行条件

- 実行日: 2026-08-16
- 対象文書: `plugins/adr/skills/manage-adr/references/adr-scoping.md`
- 対象文書commit: `03a5b79`
- 題材集合commit: `dbd1911`
- 担い手: `claude -p --model opus --effort high`
- 走行範囲: CASE-31、独立2試行
- 判定記録: `runs/2026-08-16-case31-judgments.tsv`
- 返却全文: `runs/2026-08-16-case31-raw-returns.md`
- 破棄した試行: なし

## 部分集計

一時複製へ `report` を掛けた結果:

```text
題材 1 件中 1 件を記録が覆う
試行 1 と 試行 2: 一致 4 / 4 セル (100.0%)
試行1: 項目1 0.0% (0/1)、項目2 0.0% (0/1)、項目3 0.0% (0/1)、項目4 0.0% (0/1)
試行2: 項目1 0.0% (0/1)、項目2 0.0% (0/1)、項目3 0.0% (0/1)、項目4 0.0% (0/1)
期待帰結との差: 差は無い
```

κは分母が1題材で周辺分布が一致し、Pe=1となるため未定義であり、数値は掲げない。

## 期待帰結との差と帰属

両試行とも `derive` は項目1=0、項目2=0、項目3=0、項目4=0、合計0、行き先 `実装・テスト資産・操作手順` を返した。期待帰結との差は無い。

ただし、項目3の読みは試行間で異なる。試行1は値域A=false・値域B=true、試行2は値域A=true・値域B=trueだった。これはD5（値域Aの列挙が所在を限定しないことを承知したうえで、受け入れ側設定をリポジトリ外に置く裁定）の残余リスクが観測されたものであり、判定側の保持先同定へ帰属する。項目3は両試行とも0点へ導出され、項目4の測定行は成立した。

## 入力の限定に関する限界

`参照ファイル` は両試行とも対象文書と契約仕様だけだった。実行基盤が `CLAUDE.md` を自動注入する既知の逸脱の可能性は、README §7.1のとおり残る。返却側は対象文書commitを `03a5b79` と申告した。

## 本走行が動かさなかった既知の穴

- 項目1・項目2の測定対象回復は行っていない（#624へ追跡）。
- 条件3の入力事実は置いていない（#624へ申し送る）。
- `ADR化する` 側の2分岐は対象外である。
- 既存題材30件は再走行していない。

## 測定の限界

標本は2試行、担い手は1系統、阻止の担い手も1系統である。非言及が不使用の証拠になることや、条項を削ったときに帰結が動くことは本走行だけでは保証しない。`derive` は `発見型_短絡` を読まないため、短絡の有無は点数へ効かない。今回は両試行とも短絡=falseだった。

1点率・偶然一致率の更新はフル再実行を要するため、本PRでは行わない。

## 対象文書への差分

対象文書は改訂していない。対象文書の `git diff` と `git log main..HEAD -- plugins/adr/skills/manage-adr/references/adr-scoping.md` は空である。
