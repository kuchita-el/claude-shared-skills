# 判定記録の集計レポート（2026-07-29）

## 本レポートが測っているもの

本レポートが測っているのは、**同一入力に対する判定の再現性**である。隣接題材の文脈効果は測っていない。

題材は1件ずつ判定側へ渡しており、判定側の入力に他の題材は現れない。したがって試行1と試行2の差は、同一の入力に対して判定が揺れるかどうかだけを表す。バッチで複数題材を渡し試行ごとにバッチ構成を変える設計（`adr-scope-observation.md` §1.3 の先例）は採っていないため、隣接する題材の記述に判定が引かれる効果は本レポートの測定対象に含まれない。

独立性の根拠は、試行ごとに新規コンテキストのサブエージェントを起動し、前試行の判定・記録・他題材の判定を一切引き継がせていないことに置く。

## 実行条件

| 項目 | 値 |
|---|---|
| 対象文書 | `plugins/adr/skills/manage-adr/references/adr-scoping.md` |
| 対象文書の最終変更 commit | `7a5d93d` |
| 実行時のリポジトリ HEAD | `b3af20d` |
| 実行日 | 2026-07-29 |
| 題材数 | 24（`CASE-01`〜`CASE-24`） |
| 試行数 | 2（題材ごとに独立） |
| 判定の担い手 | 題材1件につき1つの汎用サブエージェント |
| 判定記録 | `runs/2026-07-29-judgments.tsv`（48行） |
| 破棄した試行 | 0件 |

事後検査として、全48行の参照ファイル一覧を確かめた。いずれも組み立て済みプロンプトファイルと対象文書の2つに限られており、改訂の経緯・レビュー履歴・過去の判定記録・期待帰結を記した資料を読んだ試行は無い。入力の限定は守られている。

### 題材文の修正と再実行

初回の通しののち、セルフレビューで**題材文が判定の結論に相当する記述を含んでいる**箇所が見つかったため、当該題材の題材文を修正して再実行した。

| 題材 | 落とした記述 | 理由 |
|---|---|---|
| CASE-13・CASE-17・CASE-18・CASE-19・CASE-20 | 末尾の「…ため、どちらか一方の記載では規範が成立しない。」 | 対象文書 82行の条件3 の述語（規範が複数の資産へ分かれざるを得ない）の言い換えにあたる。直前の機構文（一方だけに書くと何が起きるか）が同じ情報を事実の形で担っているため、述語節のみを落とした |
| CASE-02 | 「ただし表記に閉じる規約であり」 | 対象文書 165行の表の「特徴」列の文言の写しであり、判定側が表と照合するだけで分類できてしまう。原理的な強制可能性は 171行がタイブレーカーの入力として名指しする事実であるため残した |

修正後、当該6題材のみ独立2試行で再実行した。修正について次の3点を確認している。

1. 禁止語の走査が0件のまま（`grep -nE '値域|段1|段2|同居判定|条件[1-3]|項目[1-4]|[0-9]点|短絡|保持先|非本数条件|ざるを得(ない|ず|ません)|別に要る|別途必要|別に必要|発火'` が無出力）
2. `bash plugins/adr/scripts/adr-scoping-cases.sh validate docs/development/adr-scoping-cases` が exit 0
3. 期待値は動かない。修正で落としたのは判定の結論の言い換えであり、判定の入力となる事実は落としていないため、`expectations.tsv` は更新していない

**修正前後の帰結の対比**（項目1〜4 と合計はいずれも前後で変化なし）:

| 題材 | 修正前 試行1／試行2 の行き先 | 修正後 試行1／試行2 の行き先 | 変化 |
|---|---|---|---|
| CASE-02 | 実装・テスト資産・操作手順／同左 | 実装・テスト資産・操作手順／同左 | なし |
| CASE-13 | ADR化する／同左 | ADR化する／同左 | なし |
| CASE-17 | 実装・テスト資産・操作手順／同左 | 実装・テスト資産・操作手順／同左 | なし |
| CASE-18 | 実装・テスト資産・操作手順／同左（両試行とも命名規約表を当てた） | **ADR化する／実装・テスト資産・操作手順** | **あり。試行間で割れるようになった** |
| CASE-19 | ADR化する／同左 | ADR化する／同左 | なし |
| CASE-20 | ADR化する／同左 | ADR化する／同左 | なし |

CASE-18 の変化は後述の差1 で扱う。他の18題材は題材文を修正しておらず、初回の通しの結果をそのまま記録に残している。

## 集計（`adr-scoping-cases.sh report` の出力）

```
== カバレッジ ==
  題材 24 件中 24 件を記録が覆う

== 試行間一致（セル単位。項目1〜4 を1セルと数える） ==
  試行 1 と 試行 2: 一致 96 / 96 セル (100.0%)
    項目1: 一致 24 / 24 (100.0%)
    項目2: 一致 24 / 24 (100.0%)
    項目3: 一致 24 / 24 (100.0%)
    項目4: 一致 24 / 24 (100.0%)
    合計: 一致 24 / 24 (100.0%)
    行き先: 一致 22 / 24 (91.7%)

== 各項目の1点率（周辺分布。試行ごと） ==
  試行 1:  項目1 12.5% (3/24)  項目2 50.0% (12/24)  項目3 29.2% (7/24)  項目4 95.8% (23/24)
  試行 2:  項目1 12.5% (3/24)  項目2 50.0% (12/24)  項目3 29.2% (7/24)  項目4 95.8% (23/24)

== 期待帰結との差 ==
  CASE-14 試行2 行き先: 期待 ADR化しない/実装・テスト資産・操作手順 / 判定 ADR化しない/分岐未特定
  CASE-18 試行2 行き先: 期待 ADR化する / 判定 ADR化しない/実装・テスト資産・操作手順
  差 2 件
```

## 生の一致率・該当率・κ の3者

`adr-scope-observation.md` §4.3 は「κ は該当率を代替しない。両方を見る。」と述べ、あわせて生の一致率を単独では使わないとしている。スクリプトは κ を算出しないため、周辺分布から手計算して併記する。算出式は同 §2.2 に倣い、κ =（実測一致率 − 偶然一致率）/（1 − 偶然一致率）、偶然一致率は各試行の周辺分布の積和とした。

| 対象 | 実測一致率 | 偶然一致率 | κ | 試行1の1点率 | 試行2の1点率 |
|---|---|---|---|---|---|
| 項目1 | 1.000 | 0.781 | 1.000 | 12.5%（3/24） | 12.5%（3/24） |
| 項目2 | 1.000 | 0.500 | 1.000 | 50.0%（12/24） | 50.0%（12/24） |
| 項目3 | 1.000 | 0.587 | 1.000 | 29.2%（7/24） | 29.2%（7/24） |
| 項目4 | 1.000 | 0.920 | 1.000 | 95.8%（23/24） | 95.8%（23/24） |
| 合計 | 1.000 | 0.330 | 1.000 | — | — |
| 行き先 | 0.917 | 0.264 | 0.887 | — | — |

合計と行き先は二値ではないため、1点率にあたる周辺分布は次のとおりである。

- 合計: 試行1・試行2とも 1点 10件／2点 8件／3点 5件／4点 1件
- 行き先: 試行1 は 実装・テスト資産・操作手順 7件／共有規約文書 8件／ADR化する 6件／PR説明 3件。試行2 は 実装・テスト資産・操作手順 7件／共有規約文書 8件／ADR化する 5件／PR説明 3件／分岐未特定 1件

**3者を並べて読むと次のことが言える。** 項目1〜4 と合計は実測一致率も κ も上限に張り付いた。ただし項目4 は偶然一致率が 0.920 と高く（1点率が 95.8% に偏っているため）、この項目で一致が完全であることは項目2（偶然一致率 0.500）で一致が完全であることに比べて情報量が小さい。生の一致率だけを見ると項目1〜4 が横並びに見えるが、偶然一致率で割り戻す前と後で意味が変わる項目がある、という §4.3 の指摘は本標本にも当てはまる。行き先だけが実測一致率 0.917／κ 0.887 で上限を割った。

## 期待帰結との差（2件）と帰属

差はいずれも行き先の列に集中しており、項目1〜4 と合計では期待帰結との差が0件である。

### 差1: CASE-18（試行2）

- 期待 `ADR化する` に対し、試行2 は `ADR化しない/実装・テスト資産・操作手順` を返した。試行1 は期待どおり `ADR化する` である。**試行間でも割れている。**
- 両試行とも項目1〜4 は期待どおり 0/1/1/1、合計3点まで一致している。分岐したのは3点に達した後の行き先だけである。
- 実装側を返した試行は、いずれも「命名規約の ADR 化基準」節の書式的命名規約の行を当て、同節末尾の「書式的命名規約は、強制がまだ実装されていない場合も ADR 外とする」「粒度判定基準の合計が3点へ届く場合も、振り分けは表が決める」を根拠に、合計3点でも表の例外側へ振り分けている。
- **帰属: 規範側の一意性の欠陥。** 題材文の記述密度の不足ではない。CASE-18 の題材文は必須要素 (a)〜(i) をすべて備えており、判定側が事実を補えなかったとは報告していない。割れているのは同節 160行の「命名規約」の外延が定義されていないことであり、165行の表が挙げる例（`import 順`、`空白・改行`）は名前を付ける対象ではないため、何が本節の射程に入るかを本文から一意に決められない。CASE-18 の決定（参照をどの基点で書くか）が命名規約に当たるかどうかで、行き先が「ADR 化する」と「ADR 外＝実装側」に分かれる。
- **題材文の修正がこの割れを顕在化させた。** 修正前は両試行とも表を当てて実装側へ倒れていたが、条件3 の述語の言い換えを落としたところ、試行1 が表を当てず `ADR化する` へ移り、試行2 は表を当てたままとなった。**題材文が判定の結論を先に与えていた間は、規範側の非一意性が試行間一致の陰に隠れて見えなかった**ことになる。題材文から結論の言い換えを落とすことが退行検出力に効くという設計上の前提の、実測による裏付けにあたる。
- この割れは期待値を作る段階（`expectations.tsv` の人手導出）でも予告されていた。独立2名の導出者の一方が CASE-18 について同じ二読みを報告している。**期待帰結（`ADR化する`）は人手導出2名が採った側であり、実行時の試行はそこへ倒れた回と表を当てた回に分かれた。** 期待帰結は判定側へ寄せて書き換えず、この差をそのまま記録に残す。書き換えると、次に対象文書を改訂したときにこの非一意性が解消したかどうかを検出できなくなるためである。

### 差2: CASE-14（試行2）

- 期待 `ADR化しない/実装・テスト資産・操作手順` に対し、試行2 は3分岐のどれかを特定せず `ADR化しない（2点以下）` とだけ返した（記録上は `ADR化しない/分岐未特定`）。試行1 は期待どおり実装側の分岐を返している。
- 項目1〜4 と合計は両試行とも期待どおり 0/0/0/1、合計1点で一致している。
- **帰属: 主たる原因は規範側の一意性の欠陥、従たる原因はプロンプト雛形の返却形式の緩さ。** CASE-14 は差1 と同根の論点（識別子の採番形式が「命名規約」の射程に入るか）を踏む題材であり、試行1 は表を当てて実装側へ、試行2 は表を当てるかどうかを決め切れずに分岐名を空けたと読める。加えて、雛形の返却形式が行き先を「判定対象の文書が定める行き先の分岐名」としか指定しておらず、4値のいずれかを必ず選ばせる形になっていないため、判定側が一段上の粒度で返しても形式違反にならない。
- 雛形を4値の選択に狭める案は、本レポートの対象となった実行の入力条件を変えることになるため本実行では採らない。次回の実行に向けた検討事項として残す。

### 題材文の記述密度の不足に帰属する差

**0件。** 記録された差2件はいずれも規範側へ帰属する。なお前掲「題材文の修正と再実行」で行った修正は、期待帰結との差を解消するためのものではなく、題材文が判定の結論を含んでいたこと（退行検出力の欠損）への対応である。

## 期待値の人手導出で割れた項目

`expectations.tsv` を作る際の期待値の導出も、判断依頼 J8 に従い1試行として扱っている。独立2名で導出して突き合わせた結果、項目1〜4 と合計は全120値で一致し、行き先だけが割れた。割れは本レポートの帰属分析の対象に含める。

| 割れ | 対象題材 | 候補値 | 帰属 | 現状 |
|---|---|---|---|---|
| 第2分岐と第3分岐の境界 | CASE-05・07・08・10・15 | `ADR化しない/共有規約文書` ／ `ADR化しない/PR説明` | 当初は題材文の事実の不足（規範に従う作業者の単複が書かれていなかった）。事実を足した後は規範側の一意性の欠陥として残る | 題材文へ必須要素 (i) を追加して再導出し、2名が一致。ただし 140行の第2分岐が「複数の作業者・複数の箇所へ効かせたい規範」を中黒で並べており選言か連言かが明示されていないため、連言と読むとこの5題材は `ADR化しない/PR説明` へ移る。実行時の2試行はいずれも選言側と同じ値を返した |
| 命名規約節の射程 | CASE-14 | `ADR化しない/実装・テスト資産・操作手順` ／ `ADR化しない/PR説明` | 規範側の一意性の欠陥（160行の「命名規約」の外延が未定義） | 射程に入る側を暫定値として採用。実行時の試行1 は同じ値、試行2 は分岐を特定しなかった（差2） |
| 命名規約節の射程 | CASE-18 | `ADR化する` ／ 表を当てて実装側 | 規範側の一意性の欠陥（同上） | 人手導出2名は `ADR化する` で一致。実行時は試行1 が同じ値、試行2 が表を当てる側（差1） |

3件とも同じ節（「判定結果の行き先」の第2分岐／第3分岐の境界、および「命名規約の ADR 化基準」の射程）に集中している。**期待値の人手導出・題材文の修正前の通し・修正後の通しの3つの機会すべてで同じ2箇所が割れており、規範側の非一意性としての再現性は高い。**

## 測定の限界

- **標本が小さい。** 題材24件・試行2回であり、κ も生の一致率も単独では読めない。項目4 のように1点率が 95.8% へ偏った項目では偶然一致率が 0.920 に達し、κ の分解能がほとんど無い。
- **担い手が1系統である。** 本実行の判定はすべて同一系統のモデルによる汎用サブエージェントが担った。したがって測れているのは同一の担い手における再現性であり、判定者間信頼性ではない。担い手を変えた試行は本実行の2試行には含めていない。
- **試行間の完全一致は「判定手続きが一意である」ことを意味しない。** 項目1〜4 の一致が完全であっても、それは同一の担い手が同一の入力に対して同じ読みを再現したことを示すにすぎない。CASE-18 は修正前に両試行が一致していたが、題材文から結論の言い換えを落とすと割れた。一致は、題材文が結論を先に与えていないことを前提にして初めて意味を持つ。
- **判定記録の参照ファイル一覧が正規化ラベルである。** 13列目は実パスではなく「プロンプトファイル,adr-scoping.md」という2語のラベルへ畳んでいる。事後検査は実施して合格を確認しているが、記録からは第三者が追検証できない。次回の実行では返却の実パスをそのまま記録する。
- **改訂前の基準としてのみ意味を持つ。** 本レポートの数値は、対象文書を改訂したときに同じ手順で通して差を見るための基準値である。単独では対象文書の品質を評価しない。

## 対象文書への差分

本Issueでは対象文書を改訂しない（判断依頼 J5）。規範側の欠陥と判断したものは本レポートへの記録に留めている。

`git diff` および `git log main..HEAD -- plugins/adr/skills/manage-adr/references/adr-scoping.md` のいずれも出力が空であり、本ブランチで対象文書を変更したコミットは無い。
