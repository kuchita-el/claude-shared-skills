# #749 部分走行レポート（CASE-03、2026-08-27）

## 実行条件

- 対象: CASE-03、2試行
- 対象文書: plugins/adr/skills/manage-adr/references/adr-scoping.md（`d1c8d83`。命名規約節へ射程と適用順を条文化した改訂と、そのセルフレビュー指摘・PRレビュー指摘の反映の後）
- 題材集合: `4d35d86`（題材文は本Issueで触っていない）
- 担い手: `claude -p --model opus --effort high`、題材1件・試行1件につき別起動
- 判定記録: runs/2026-08-27-issue749-case03-judgments.tsv
- 返却全文: runs/2026-08-27-issue749-case03-raw-returns.md
- 構造化返却: runs/2026-08-27-issue749-case03-returns/
- 破棄した試行: なし（本走行の2試行はいずれも返却型検査と事後検査に通った）。前回の走行で1件を破棄している（下記「破棄した試行」）

記録を #749 の主走行（`runs/2026-08-27-issue749-judgments.tsv`）と分けているのは、AC6 が主走行側の記録に対して照合件数6 を字義で要求するためである。1本にまとめると同一記録への `crosscheck` が8を返し、AC6 を満たせない。

本記録も主走行と同じく**3回目の走行**である。1回目は `4abad2a`、2回目はセルフレビュー指摘の反映後の `54f8755` に対して採ったが、いずれもその後の再改訂で対象文書の現行本文と食い違うため、`d1c8d83` に対して採り直した。3走行の帰結は一致した（3回とも 0/1/0/1・`ADR化しない/共有規約文書`）。

## report の結果

CASE-03 だけを持つ題材集合の複製（コミットしていない）へ `report` を実行した。終了状態は0であった。

- 1題材、2試行、カバレッジ 1/1
- 試行間一致: セル単位 4/4、行き先 1/1
- **期待帰結との差: 0件（点数欄を含む）**

差が0件であるため、帰属を記す対象は無い。

## 判定結果

| 題材 | 試行 | 必要条件 | 項目1〜4 | 行き先 | 期待との差 |
|---|---|---|---|---|---|
| CASE-03 | 1 | 成立 | 0/1/0/1 | `ADR化しない/共有規約文書` | 無し |
| CASE-03 | 2 | 成立 | 0/1/0/1 | `ADR化しない/共有規約文書` | 無し |

本Issueで `期待_項目1`・`期待_項目2`・`期待_項目4` を備考の確定値（0/1/0/1）へ充填したため、4項目すべてが比較対象に入っている。充填前は `期待_項目3` のみが比較対象であった。

## 改訂前の走行との突き合わせ

| 題材 | 改訂前（`03a5b79`）の判定 | 改訂後（`d1c8d83`）の判定 | 動いたか |
|---|---|---|---|
| CASE-03 | `runs/2026-08-15-judgments.tsv` の2試行とも 0/1/0/1・`ADR化しない/共有規約文書` | 2試行とも 0/1/0/1・`ADR化しない/共有規約文書` | 動いていない |

担い手の条件は 2026-08-15 の走行と同一である。条文改訂の前後で帰結が動いていない。

## 事後検査（README §7）

2試行とも `参照ファイル` 欄に挙がったのは対象文書と契約仕様（`adr-judgment-contract.md`）のみで、いずれも README §5 の許可対象である。参照禁止対象は1件も挙がっていない。

## 破棄した試行

3回目の走行では破棄していない。2回目の走行（`54f8755` に対するもの）では CASE-03 試行2 の初回返却を無効として破棄し、同題材・同試行をやり直した。

- **破棄の理由**: 返却が新スキーマの必須フィールド `欠測` を欠いており、返却型検査に通らないため。返却の内容（判定の中身）ではなく形式の欠落である
- **破棄の扱い**: 破棄した返却は判定記録・返却全文・構造化返却のいずれにも書いていない
- **やり直し後**: 同一条件で再起動し、必須フィールドの揃った返却を得た

同型の欠落は1回目の走行でも1件（CASE-24 試行1）出ている。件数と見立ては主走行のレポート（`runs/2026-08-27-issue749-report.md`）の当該節が持つ。3回目の走行で破棄した1件（CASE-14 試行2）は欠落ではなく入力の限定への違反であり、これも同レポートが持つ。

## 返却形式の逸脱（フェンス）と入力の限定に関する限界

いずれも主走行のレポート（`runs/2026-08-27-issue749-report.md`）の当該節と同一である。CASE-03 の2試行も ` ```json ` フェンスで包まれた返却であり、転記時にフェンスのみを外した。実行基盤による `CLAUDE.md` の自動注入の可能性は README §7.1 のとおり残る。

## 禁止語走査

題材文（`cases.md`）は本Issueで触っておらず、判定記録14列目も改訂前の走行と同一の `4d35d86` である。題材文ブロックへの禁止語走査は本走行では実施していない。
