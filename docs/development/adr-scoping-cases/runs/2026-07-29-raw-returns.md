# 判定側の返却全文（2026-07-29・再実行2巡目）

本ファイルは、判定サブエージェントが返した**返却の全文**をそのまま保持する。`judgments.tsv` の各列は、この全文を人手で符号化した派生物である。符号化の妥当性を後から独立に確かめられるよう、原文をここへ残す。

保持しているのは**再実行2巡目（CASE-05・CASE-13・CASE-17・CASE-18・CASE-19・CASE-20 の各2試行、計12件）だけ**である。初回の通しと再実行1巡目の返却全文は保持していない（当時は全文を残す運用にしていなかった）。したがって、それ以外の題材については `judgments.tsv` の符号化を原文へ突き合わせる手段が無い。

## 参照ファイル欄に現れた入力の限定の逸脱

12件すべてが、参照ファイル欄でリポジトリルートの `CLAUDE.md` を挙げている。うち4件（CASE-05 試行1・CASE-13 試行1・CASE-19 試行2・CASE-20 試行1）はユーザースコープの `CLAUDE.md` も併せて挙げている。いずれも「自分で開いたのではなく、実行基盤がセッション開始時に自動注入した」と申告している。判定側が能動的に開いたファイルはプロンプトファイルと対象文書の2つに限られるが、**入力の限定（プロンプトと対象文書のみ）は実行基盤の水準で守られていない。**

この逸脱は試行の破棄では解消しない。判定側の起動経路そのものに由来するためである。詳細と扱いは `2026-07-29-report.md` の「入力の限定に関する限界」に記す。

---

## CASE-05

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 —「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書（規約ファイル・プロジェクト指示）に置く」分岐
根拠_項目1: 反転で直すのは共有規約ファイルとプロジェクト指示ファイルの2本のみ＝「3本以上で1点、2本以下で0点」に照らし0点、かつ構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無く非本数条件のORも成立せず0点
根拠_項目2: 影響が及ぶのは1つのスキル定義のみ＝「2つ以上で1点、1つ以下で0点」で0点、決定を知らない作業者が別の選択をしうる箇所も現時点で同じ1箇所のみでOR条件も不成立（作業者が複数居ることは本項目の数える単位＝モジュール／スキル／スクリプト／成果物種別に当たらない）
根拠_項目3: 段1では規範を担う2文書がいずれも散文の言明のみで(A)を満たさず、共有規約ファイル・プロジェクト指示ファイルは(B)だが採用理由がどちらにも書かれておらず値域外（説明できる人物の記憶は閉じた値域の外）、よって段2の同居判定へ進み、却下選択肢なし＝条件1不成立・受け入れた犠牲なし＝条件2不成立・2文書が相互参照しており一方へ理由を書けば到達できる＝条件3の「いずれか1つの傍らに置けない」に当たらず、段2（同居判定）で0点確定
根拠_項目4: 本文埋め込みを機械的に検出する検査が現に無く作業も阻止されないため、「検査が無い」に該当し1点（判定時点の状態のみを見る）
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md（なお CLAUDE.md および ~/.claude/CLAUDE.md はツールで開いていないがハーネスにより自動提示され本文が可視であった。判定の入力には使用していない）
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転で直すのは2本の文書のみ＝2本以下で0点、非本数条件（構造変更・スキーマ/データ形式・配布済み成果物への影響・蓄積データ移行）はいずれも題材文が無しと述べるため加点なし。
根拠_項目2: 影響が及ぶのは1つのスキル定義のみ＝最も細かい単位で1つ以下で0点、OR条件も「決定を知らない作業者が別の選択をしうる箇所は現時点でこの1つのスキル定義だけ」で複数に当たらず0点。
根拠_項目3: 段1は保持先が共有規約ファイル・プロジェクト指示ファイル＝値域(B)だが採用理由がどちらにも書かれておらず（(A)も「散文で述べるだけ・強制していない」ため不成立）値域を満たさず、後段の同居判定で確定＝却下選択肢なし（条件1非該当）・受け入れた犠牲なし（条件2非該当）・2文書は相互参照し一方へ理由を書けば到達できるため「いずれか1つの傍らに置けない」に当たらず（条件3非該当）で0点。
根拠_項目4: 埋め込みを機械的に検出する検査が現に無く作業も止まらないため、判定時点の状態のみを見て1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自身では開いていないが、ハーネスによりプロジェクト指示として自動注入され本文が文脈に入ったため申告）
```

---

## CASE-13

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 「直す必要のあるファイルは無い」＝0本で本数条件0点、かつ構造変更・スキーマ／データ形式・配布済み成果物への影響・蓄積データ移行のいずれも無しと明記されるため非本数条件（OR）も不成立で0点。
根拠_項目2: 影響が及ぶのが2つのプラグイン＝最も細かい単位で2つ以上により1点（決定を知らない作業者が別選択をしうる箇所が現に2つ在るというOR条件も同時に成立）。
根拠_項目3: 段1で保持先が (A)（強制資産・型／テンプレート／スキーマ・実行を組み込む手順）にも (B)（採用理由が現に書かれた共有規約文書／指示ファイル）にも当たらず値域外、段2の同居判定へ進み、採用理由が到達不能で確かめられないため条件1・条件2は非該当側へ倒し、規範を書くとすれば雛形とスキーマ定義の2ファイルへ分かれざるを得ず片方だけでは漏れるため条件3が成立して1点（段2で確定）。
根拠_項目4: 判定時点で機械的検出が無く欠けても作業が止まらないため1点。なお題材文には既存 ADR の有無・内容が書かれておらず、「この慣行を述べた記述はどの文書にも無い」以上の事実を補えなかったため、行き先は既存 ADR が扱っていない枠組みを立てる場合として新規 ADR 側へ振り分けた。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, （自身では開いていないがハーネスにより自動注入された）CLAUDE.md, ~/.claude/CLAUDE.md
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 「直す必要のあるファイルは無い」＝0本で本数条件0点、構造変更・スキーマ/データ形式変更・配布済み成果物の利用者影響・蓄積データ移行のいずれも無いと題材が明示するため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶのが「サブエージェント定義を持つ2つのプラグイン」＝最も細かい単位で2つ以上により1点（加えて、慣行を知らない作業者が明示を省きうる箇所が現に2つあるためORでも1点）。
根拠_項目3: 段2（同居判定）で確定。規範を体現・強制する資産も散文の記述も無く値域(A)(B)いずれも満たさず段1で0点にならず、採用理由は離任・記録なしで「確かめられない」ため条件1・条件2は非該当扱い、規範が雛形ファイルとスキーマ定義ファイルの2ファイルへ分かれざるを得ない（片方だけでは既存定義が漏れる／新規作成で漏れる）ため条件3該当で1点。
根拠_項目4: 明示の有無を機械的に検出する検査が無く欠けていても作業が止まらないため、判定時点の状態のみを見る規定により1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分では開いていないが、ハーネスにより本文が自動注入されたため隠さず記載する。判定には使用していない）
```

---

## CASE-17

### 試行1

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 → 「ツールで自動強制される規範 → 実装・テスト資産・操作手順に置く」分岐（検査が警告どまりで阻止しない規範も第1分岐に置くとの規定による）
根拠_項目1: 反転で直すのは検査スクリプト本体と判定用入力データの2本のみ（参照を書いた文書は直さなくてよい＝数えない対象）で「3本以上で1点」に届かず、構造変更・スキーマ/データ形式変更・既配布成果物の利用者影響・蓄積データ移行のいずれも無いため非本数条件でも0点。
根拠_項目2: 影響が及ぶ成果物種別が ADR 文書とスキル定義の2つで「2つ以上で1点」に該当し、規範を知らない作業者が別の書き方を選びうる箇所も現にこの2種別にあるため OR 条件でも1点。
根拠_項目3: 段1で確定。保持先が「規範を強制する実装本体」とその「fixture／テスト」であり、どちらも散文でなく規範そのものを体現しているため値域(A) を満たす。(A) は採用理由の記載の有無も強制の稼働も問わない（警告どまりでも満たす）ので0点となり、同居判定へは進まない。採用理由は確かめられないが、段1確定のため判定に影響しない。
根拠_項目4: 検査は現に走っているが出力は警告に留まりコミットも CI も止めないため、「検査はあるが警告どまりで作業を阻止しない」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md（なお worktree の CLAUDE.md 本文がハーネスにより自動注入されたが、自ら開いておらず判定には使用していない）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先のうち「ツールで自動強制される規範 → 実装・テスト資産・操作手順に置く」分岐（警告どまりでも第1分岐に置くとの規定による）
根拠_項目1: 反転で直すのは検査スクリプト本体と判定用入力データの2本（既存の相対パス記述は残せるため数えない）で2本以下→0点、かつ配置・分割・命名の変更なし・スキーマ/データ形式変更なし・配布済み成果物の利用者側影響なし・蓄積データ移行なしで非本数条件のいずれにも当たらない
根拠_項目2: 影響が及ぶ成果物種別が ADR 文書とスキル定義の2つで「2つ以上で1点」に該当し、加えて規範を知らない作業者が別の書き方を選びうる箇所が現にその2種別に在るため OR 条件でも1点
根拠_項目3: 段1（値域(A)）で確定。保持先が「規範を強制する実装本体」と「その fixture／テスト」であり、どちらも散文の言明でなく規範を体現しているため、採用理由の記載が無く強制が警告どまりでも (A) を満たし0点（採用理由を確かめられない点は同居判定へ進まないため影響しない）
根拠_項目4: 検査は現に走るが出力は警告に留まりコミットも CI も止めないため、「検査はあるが警告どまりで作業を阻止しない」に当たり1点
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自発的に開いたものではなく system-reminder により自動注入されたもの。判定には使用していない）
```

---

## CASE-18

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 書式的命名規約 → ADR 外（Linter／フォーマッタ規約とその設定＝強制の実装へ配置。「命名規約の ADR 化基準」末尾の例外により、合計3点でも表が決める）
根拠_項目1: 反転しても既存の相対パス参照はそのまま残せ直すファイルが無い（0本）ため本数条件で0点、かつ配置・分割・名前は変わらず、スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも不成立。
根拠_項目2: 影響が及ぶ成果物種別が ADR 文書とスキル定義の2つで「2つ以上で1点」に該当し、規範を知らない作業者が別の書き方を選びうる箇所も現に同2単位あるためORでも1点。
根拠_項目3: 段1では規範を体現・強制する資産も散文記述も現に無く値域(A)(B)いずれも満たす保持先が無いため後段へ進み、同居判定で確定（採用理由は離任・記録不在で確かめられないため条件1・条件2は該当しない側へ倒し、規範を書くとすれば検査スクリプト本体と判定用入力データの2ファイルへ分かれざるを得ず一方の傍らに理由を置けないため条件3で1点）。
根拠_項目4: 違反を機械的に検出する検査が現に無く別の書き方をしても作業が止まらないため、判定時点の状態のみを見る規定により1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（システムリマインダにより自動提示。能動的に開いてはいないが内容が入力へ混入したため申告する）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR（ADR 化する（3点以上）の行き先のうち、既存 ADR が扱っていない枠組みを立てる場合）
根拠_項目1: 反転しても既存の相対パス記述をそのまま残せ直す必要のあるファイルが無い（0本）ため本数条件で0点、かつ配置・分割・名前の変更もスキーマ・形式変更も蓄積データ移行も配布済み成果物の利用者影響も無いため非本数条件のORにも当たらない。
根拠_項目2: 規範の影響が及ぶ成果物種別が ADR 文書とスキル定義の2つであり、最も細かい単位で数えて2つ以上のため1点（規範を知らない作業者が別の書き方を選びうる箇所も現にこの2種別にあり、OR条件でも1点）。
根拠_項目3: 段1では規範を担う実装・fixture・テンプレート・手順も共有規約文書の記載も存在せず値域(A)(B)いずれも満たさないため段2の同居判定へ進み、採用理由が確かめられない（離任・記録なし）ため条件1・条件2は該当しない側へ倒す一方、規範を書くとすれば検査スクリプト本体と判定用入力データの2ファイルへ分かれざるを得ずどちらか一方の傍らに理由を置けないため条件3該当で1点（段2で確定）。
根拠_項目4: 違反を機械的に検出する検査が現に無く別の書き方をしても作業が止まらないため、判定時点の状態のみを見る規定により1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（ツールが自動挿入したもので、判定には使用していない）
```

---

## CASE-19

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 反転で直すのは共有規約ファイル1本（2本以下→0点）、かつ構造変更・スキーマ/データ形式変更・配布済み成果物への影響・蓄積データ移行はいずれも無いと題材文が明示するため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶのがスキル定義を持つ2つのプラグイン（最も細かい単位で2つ以上→1点）、加えて規範を知らない作業者が列挙を省きうる箇所が現に同2箇所ある（ORの現在時制条件）ため1点。
根拠_項目3: 段1で保持先は規範を散文で述べるだけの共有規約ファイルのみ＝(A)非該当・採用理由が未記載で(B)も非該当のため値域を満たす保持先が無く段2の同居判定へ進み、採用理由が確かめられないため条件1・条件2は非該当側へ倒し、条件3（規範をスキーマ定義ファイルと雛形ファイルの2ファイルへ分けざるを得ず片方だけでは漏れ／既存すり抜けが生じる＝別ファイルは別資産）に該当して1点（段2で確定）。
根拠_項目4: 列挙の有無を機械的に検出する検査が現に無く欠けていても作業が止まらない（検査が無い→1点）、判定時点の状態のみを見る規定により将来の実装可能性は混ぜない。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分では開いておらず、ハーネスにより自動注入されたもの。判定には使用していない）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: ADR 化する（3点以上）→ 新規 ADR
根拠_項目1: 反転で直すのは共有規約ファイル1本のみ（3本未満で0点）、かつ構造変更・スキーマ/データ形式変更・配布済み成果物の利用者影響・蓄積データ移行のいずれも無いと題材文が明示するため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶ単位が2つのプラグインで「2つ以上で1点」に該当し、加えて規範を知らない作業者が別の選択をしうる箇所が現に2つあるOR条件も満たす。
根拠_項目3: 段1では保持先の共有規約ファイルが散文の言明のみで採用理由の記載が無く値域(B)不成立・(A)も不成立、段2の同居判定で採用理由が確かめられないため条件1・条件2は非該当へ倒し、条件3（スキーマ定義ファイルと雛形ファイルへ分かれざるを得ず片方では漏れる／既存が通る、別ファイルにつき別資産）に該当して1点確定。
根拠_項目4: 列挙の有無を機械的に検出する検査が現に無く欠けても作業が止まらないため「検査が無い」に該当し1点（発見型の短絡は保持先が共有規約文書のため不適用）。既存 ADR との枠組みの異同は題材文に事実が無く補えなかったため、行き先の2分岐は安全則（迷えば core 寄り＝新規 ADR）に倒した。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自動注入により本文が提示された。判定には使用していない）, ~/.claude/CLAUDE.md（同上）
```

---

## CASE-20

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR（「ADR 化する（3点以上）の行き先」の第1分岐）
根拠_項目1: 反転しても既存の指示文はそのまま残せ直す必要のあるファイルが無く0本（3本未満で0点）、かつ構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶ単位がエージェント起動の2つのスキル定義（最も細かい単位で2つ）で「2つ以上で1点」に該当し、決定を知らない作業者が別の選択をしうる箇所も現にその2箇所あるためORでも1点。
根拠_項目3: 段1では規範を体現・強制する資産も散文の記述も現に無く値域(A)(B)を満たす保持先が無いため段2の同居判定へ進み、採用理由が確かめられないため条件1・条件2は非該当へ倒したうえで、規範を書くとすれば共有規約ファイルと手順書の2ファイルへ分かれざるを得ずどちらか1つの傍らに理由を置けない（片方だけでは書き落とし／手順外起動に効かない）ことから条件3に該当し、段2（同居判定）で1点確定。
根拠_項目4: 明記の有無を機械的に検出する検査が無く範囲を書かずに起動しても作業が止まらないため、判定時点の状態で「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md（なお worktree の CLAUDE.md およびユーザースコープの CLAUDE.md はツールで開いたものではなくセッション開始時に自動提示されたが、判定には使用していない）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR（「ADR 化する（3点以上）の行き先」のうち、既存 ADR が扱っていない枠組みを立てる分岐）
根拠_項目1: 反転しても既存記述はそのまま残せて直す必要のあるファイルが無い（0本＝2本以下で0点）うえ、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれにも当たらないため非本数条件のORも成立しない。
根拠_項目2: 影響が及ぶのがエージェント起動の2つのスキル定義であり最小単位で2つ＝「2つ以上で1点」に該当（決定を知らない作業者が別の選択をしうる箇所も現にこの2つで、ORでも1点）。作業者1人という形容は列挙単位の数え方を動かさない。
根拠_項目3: 保持先が実行者の記憶のみで値域(A)(B)とも満たさず段1で0点が成立せず、段2の同居判定で、採用理由が到達手段も記録も無く確かめられないため条件1・条件2は非該当側へ倒し、規範の置き場所が共有規約ファイルと手順書の2ファイルへ分かれざるを得ない（片方だけでは書き落とし／手順外起動に効かない）ことから条件3に該当し1点＝段2で確定。
根拠_項目4: 明記の有無を機械的に検出する検査が無く、範囲を書かずに起動しても作業が止まらないため「検査が無い」に該当し1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（ツールで開いたものではなく、セッション開始時にシステム側から自動提示されたもの。判定には使用していない）
```
