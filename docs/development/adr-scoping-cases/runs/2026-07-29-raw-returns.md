# 判定側の返却全文（2026-07-29・再実行3巡目）

本ファイルは、判定サブエージェントが返した**返却の全文**をそのまま保持する。`judgments.tsv` の各列は、この全文を人手で符号化した派生物である。符号化の妥当性を後から独立に確かめられるよう、原文をここへ残す。

保持しているのは**再実行3巡目（CASE-03〜CASE-16・CASE-18〜CASE-24 の21題材、各2試行、計42件）だけ**である。初回の通しと再実行1〜2巡目の返却全文は保持していない（当時は全文を残す運用にしていなかった）。したがって CASE-01・CASE-02・CASE-17 の6行については `judgments.tsv` の符号化を原文へ突き合わせる手段が無い。

パス表記は読みやすさのため短縮している（`（プロンプトファイル）`＝`adr-scoping-cases.sh prompt` が組み立てた一時ファイル、`adr-scoping.md`＝対象文書、`CLAUDE.md`＝リポジトリルート、`~/.claude/CLAUDE.md`＝ユーザースコープ、`MEMORY.md`＝メモリ index）。それ以外の字句は判定側の返却のままである。

## 参照ファイル欄に現れた入力の限定の逸脱

42件の多くが、参照ファイル欄で `CLAUDE.md`（リポジトリルート）を挙げている。ユーザースコープの `CLAUDE.md` やメモリ index を併せて挙げた試行もある。いずれも「自分で開いたのではなく、実行基盤がセッション開始時に自動注入した」と申告している。判定側が能動的に開いたファイルはプロンプトファイルと対象文書の2つに限られるが、**入力の限定（プロンプトと対象文書のみ）は実行基盤の水準で守られていない。**

申告が現れなかった試行についても、注入が無かったことの証拠にはならない。判定側は返却形式に沿って自分が読んだと認識したものを挙げているにすぎず、注入の有無を観測する手段を持たないためである。

この逸脱は試行の破棄では解消しない。判定側の起動経路そのものに由来するためである。詳細と扱いは `2026-07-29-report.md` の「入力の限定に関する限界」に記す。

---

## CASE-03

### 試行1

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 — ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書
根拠_項目1: 反転で直すのは共有規約ファイル1本のみ（既存コミットは書き換え不要で矛盾も破損も生じない）ため本数条件は2本以下で0点、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無く非本数条件のORも成立しない。
根拠_項目2: 影響が全成果物種別（スキル定義・スクリプト・文書）へのコミットに一様適用されるため「全単位へ一様に適用される場合は2つ以上に当たる」に該当し1点（規範を知らない作業者が別言語を選びうる箇所が現に複数在るというOR条件も同時に成立）。
根拠_項目3: 段2の同居判定で0点確定 — 段1は保持先の共有規約ファイルが規範を散文で述べるのみで(A)不成立、(B)は採用理由が現に書かれていないため不成立、続く同居判定で却下理由が採用理由「チームの共通語が日本語だから」と同一（条件1非該当）・犠牲無し（条件2非該当）・規範は1本の資産に収まる（条件3非該当）。
根拠_項目4: コミットメッセージ言語を機械的に検出する検査が現に無く作業も阻止されないため、判定時点の状態のみを見る規定により1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分で開いたのではなくハーネスにより自動提示された。判定には使用していない）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 —「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転で直すのは共有規約ファイル1本のみ（既存コミットは書き換え不要＝矛盾も破損も生じないため数えない）で2本以下、かつ構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも不成立で0点。
根拠_項目2: 影響が全成果物種別（スキル定義・スクリプト・文書）へ一様に適用されるため「全単位へ一様に適用される場合は2つ以上に当たる」に該当し1点（規範を知らない作業者が別言語を選びうる箇所が現に複数あるOR条件も併せて成立）。
根拠_項目3: 段2（同居判定）で確定。段1は共有規約ファイルが散文で述べるだけで(A)非該当、採用理由がどの文書にも書かれていないため(B)も非該当で値域を満たす保持先が無く、同居判定では却下理由が採用理由と同一（条件1不成立）・犠牲無し（条件2不成立）・規範は1本の資産に収まる（条件3不成立）ゆえ0点。
根拠_項目4: 言語を機械的に検出する検査が現に無く英語でも作業が止まらないため、「検査が無い」に該当し1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自身で開いたのではなく環境により自動注入されたが、内容が提示されたため申告する。判定には用いていない）
```

---

## CASE-04

### 試行1

```
項目1: 1
項目2: 1
項目3: 1
項目4: 1
合計: 4
行き先: 新規 ADR
根拠_項目1: 反転で修正が要る追跡下のファイルは共有規約1本＋実装2本の計3本で「3本以上で1点」に該当（構造変更・スキーマ・配布済み成果物・蓄積データ移行の非本数条件はいずれも無し）。
根拠_項目2: 影響が及ぶのは2つの実装モジュールで「2つ以上で1点」に該当し、決定を知らない作業者が別の選択をしうる箇所も現に2つあるためOR条件でも1点。
根拠_項目3: 段1は共有規約ファイルが散文で規範を述べるのみ＝(A) 非該当、採用理由が同文書に書かれていないため(B) も非該当で値域を満たす保持先無し。段2の同居判定で、決定者が現に述べる却下理由（利用条件・保守停止・機能未実装）が採用理由（当時の性能要件を満たす唯一の候補）と別に要るため条件1に該当し1点（条件2は犠牲無し、条件3は規範が1本に収まるため非該当）。段2（同居判定）で確定。
根拠_項目4: 別ライブラリ使用を検出する検査が現に無く作業も阻止されないため「検査が無い」＝1点。［補えなかった事実］既存 ADR の枠組みに関する事実が題材文に無いため、行き先2分岐は「迷えば core 寄り＝新規 ADR に倒す」安全則に従い新規 ADR 側とした。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, ~/.claude/CLAUDE.md（システムにより自動注入。判定には不使用）, CLAUDE.md（同上）, （worktree の）CLAUDE.md（同上）, MEMORY.md（同上）
```

### 試行2

```
項目1: 1
項目2: 1
項目3: 1
項目4: 1
合計: 4
行き先: 新規 ADR（「ADR 化する（3点以上）の行き先」の第1分岐）
根拠_項目1: 反転で修正が要る追跡下ファイルが共有規約1本＋実装2本の計3本であり、項目1の「3本以上で1点」に該当（構造変更・スキーマ・配布済み成果物への影響・データ移行の非本数条件はいずれも無しと明記）
根拠_項目2: 影響が及ぶ最も細かい単位が実装モジュール2つで「2つ以上で1点」に該当し、加えて決定を知らない作業者が別選択をしうる箇所が現に2つある OR 条件にも該当
根拠_項目3: 段2（同居判定）で確定。段1は保持先が散文のみの共有規約ファイルで (A) 不成立、採用理由がどの文書にも無く (B) も不成立。段2は採用理由（性能要件を満たす唯一の候補）と別の却下理由が候補ごとに3つ現に述べられており条件1に該当して1点（条件2は犠牲無しで非該当、条件3は規範が共有規約1本のため非該当）
根拠_項目4: 別ライブラリ使用を機械的に検出する検査が現に無く作業も阻止されないため、項目4の「検査が無い」で1点
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分で開いたものではなく、セッション開始時にハーネスが自動注入した。判定には使用していない）
```

---

## CASE-05

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 — ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書
根拠_項目1: 反転で直すのは2本の文書だけ＝本数条件「3本以上で1点」に届かず、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶ範囲は1つのスキル定義のみ＝「2つ以上で1点、1つ以下で0点」に照らし0点、決定を知らない作業者が別の選択をしうる箇所も現時点で1つのみでORも不成立（「作業者が現に複数居る」は本項目の数える単位＝モジュール／実行可能単位／成果物の種類に当たらない）。
根拠_項目3: 段1では規範を担う資産が共有規約ファイルとプロジェクト指示ファイル＝値域(B)だが採用理由がどちらにも書かれておらず値域不成立（散文記述は(A)を満たさない）、後段の同居判定で確定＝却下選択肢が無く条件1不該当・受け入れた不利益が無く条件2不該当・2本は互いを参照し合い題材文も「分かれざるを得ない」とは述べておらず「現在の配置がたまたま分かれている」に当たるため条件3不該当で0点。
根拠_項目4: 本文の埋め込みを機械的に検出する検査が現に無く埋め込んでも作業が止まらないため、「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, （以下は自分で開いたのではなくハーネスが自動で文脈へ注入したもの）~/.claude/CLAUDE.md, CLAUDE.md, （worktree の）CLAUDE.md, MEMORY.md
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書（規約ファイル・プロジェクト指示）に置く」分岐
根拠_項目1: 反転で直す必要があるのは2本の文書だけ（3本以上で1点の本数条件を満たさず）、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも成立せず0点
根拠_項目2: 影響が及ぶ単位は1つのスキル定義のみで「2つ以上で1点」に届かず、規範を知らない作業者が別の選択をしうる箇所も現時点で1箇所のみのためORも成立せず0点（作業者が複数居る事実は本項目が数える単位＝箇所ではない）
根拠_項目3: 段1は保持先が共有規約ファイルとプロジェクト指示ファイル＝値域(B)だが採用理由がどちらにも書かれておらず値域を満たさず、(A)に当たる強制資産も無いため段2の同居判定へ進み、却下選択肢なし＝条件1不該当・受け入れた犠牲なし＝条件2不該当・2本の文書は互いを参照し合っておりいずれか1つの傍らに理由を置けるため条件3不該当（現在の配置がたまたま分かれていることは含まない）で、段2（同居判定）にて0点確定
根拠_項目4: 埋め込みを機械的に検出する検査が現に無く作業も阻止されないため「検査が無い」に当たり1点（判定時点の状態のみを見る）
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分で開いたのではなくハーネスにより自動注入されたが、内容が文脈へ入ったため隠さず記載。判定には用いていない）
```

---

## CASE-06

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 — ツールで自動強制される規範 → 実装・テスト資産・操作手順に置く
根拠_項目1: 反転で直すのはスクリプト1本のみ（3本未満で0点）、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶ単位はスクリプト1つのみで「2つ以上で1点」に届かず、決定を知らない作業者が別の選択をしうる箇所も現に1本だけのためOR条件も成立せず0点。
根拠_項目3: 段1（値域判定）で確定。規範を体現する lint スクリプトが値域(A)の「規範を強制する実装本体」に当たり、採用理由の記載有無と強制の稼働（警告どまり）を問わず値域を満たすため0点、同居判定へ進まない。
根拠_項目4: 違反は現に機械的に検出されるが警告どまりでコミットもCIも止めないため、「検査はあるが警告どまりで作業を阻止しない」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, （以下は自動注入により参照せず提示された。判定には用いていない）CLAUDE.md, （worktree の）CLAUDE.md, ~/.claude/CLAUDE.md, MEMORY.md
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールで自動強制される規範 → 実装・テスト資産・操作手順に置く」分岐
根拠_項目1: 反転で直すのはスクリプト1本のみ（2本以下で0点）、かつ構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数OR条件も不成立。
根拠_項目2: 影響が及ぶ単位はスクリプト1つ（1つ以下で0点）、決定を知らない作業者が別の選択をしうる箇所も現時点で1本のみでOR条件不成立、作業者も1人。
根拠_項目3: 段1で確定。規範を体現する lint スクリプトが「規範を強制する実装本体」として値域(A)を満たし、(A) は採用理由の記載の有無も強制の稼働（警告どまり）も問わないため0点、同居判定へ進まない。
根拠_項目4: 検査は存在するが警告出力に留まりコミットも CI も止めないため「警告どまりで作業を阻止しない」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md（以下は自分で開いたものではなく、セッション開始時に環境から自動提示された分: ~/.claude/CLAUDE.md, CLAUDE.md, （worktree の）CLAUDE.md, MEMORY.md。判定には使用していない）
```

---

## CASE-07

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転で直すのは共有規約ファイル1本のみで「3本以上で1点」に届かず、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件（OR）も立たない。
根拠_項目2: 影響が及ぶのは PR を作る手順1つで「2つ以上で1点」に届かず、規範を知らない作業者が別の選択をしうる箇所も現に1つだけのため OR 条件も立たない（作業者が複数居ることは本項目の列挙単位＝モジュール／実行可能単位／成果物種別ではないため入力にしない）。
根拠_項目3: 段1では保持先の共有規約ファイルが規範を散文で述べるだけで (A) を満たさず、採用理由がその文書に書かれていないため (B) も満たさず（Issue は閉じた値域外）、段2の同居判定で却下選択肢なし＝条件1不該当・犠牲なし＝条件2不該当・規範は1資産に収まる＝条件3不該当となり、同居判定で0点が確定した。
根拠_項目4: 手順記載の有無を機械的に検出する検査が現に無く作業も止まらないため、判定時点の状態のみを見る規定により1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md（以下はセッション開始時に自動注入されたもので判定に使用していない: （worktree の）CLAUDE.md, CLAUDE.md, ~/.claude/CLAUDE.md, MEMORY.md）
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 → ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書（規約ファイル・プロジェクト指示）に置く
根拠_項目1: 反転で直すのは共有規約ファイル1本のみ（3本未満で0点）、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無しでOR条件も不成立。
根拠_項目2: 影響範囲は「PR を作る手順」1つで2つ未満、決定を知らない作業者が別選択をしうる箇所も現に1つだけでOR不成立（作業者が複数居ることは項目2の数える単位＝箇所ではない）。
根拠_項目3: 段1で保持先は規範を担う共有規約ファイル＝値域(B)だが採用理由が当該文書に書かれておらず（Issue コメントは列挙外＝確定的に値域外）(B)不成立、段2の同居判定で却下選択肢無し（条件1不成立）・犠牲無し（条件2不成立）・規範は1資産に収まる（条件3不成立）となり同居判定で0点確定。
根拠_項目4: 手順記載の有無を機械的に検出する検査が現に無く作業も阻止されないため、判定時点の状態のみを見る規定により1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自動注入されたもので、判定には使用していない）
```

---

## CASE-08

### 試行1

```
項目1: 0
項目2: 0
項目3: 1
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 — ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書
根拠_項目1: 反転で直すのは共有規約ファイル1本のみで本数条件（3本以上）を満たさず、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件（OR）にも当たらず0点。
根拠_項目2: 影響が及ぶ単位はレビュー応答の手順1つで「2つ以上」に満たず、決定を知らない作業者が別の選択をしうる箇所も現時点で1つだけなのでOR条件も不成立（従う作業者が複数居ることは項目2が数える単位＝モジュール／実行可能単位／成果物種別・箇所に当たらない）0点。
根拠_項目3: 段1では保持先の共有規約ファイルが規範を散文で述べるだけで(A)を満たさず、採用理由がどの文書にも書かれていないため(B)も満たさず値域外、段2の同居判定で確定し、却下した「解決マークだけで管理」案の却下理由「履歴に残らず経緯を辿れない」が採用理由「未応答を追える状態を保つ」では説明できず別に要るため条件1に該当して1点（条件2は犠牲無しで非該当、条件3は規範が共有規約ファイル1本に収まるため非該当）。
根拠_項目4: 応答粒度を機械的に検出する検査が現に無く、まとめて返しても作業が止まらないため「検査が無い／作業を阻止しない」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 1
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 —「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転で直すのは共有規約ファイル1本のみで「3本以上で1点」に届かず、構造変更・スキーマ・配布成果物への影響・蓄積データ移行のいずれの非本数条件（OR）にも当たらないため0点。
根拠_項目2: 影響が及ぶのは手順1つで「2つ以上で1点」に届かず、決定を知らない作業者が別の選択をしうる箇所も現に1つだけでOR条件に当たらない（「従う作業者が複数居る」は列挙単位＝モジュール／実行可能単位／成果物種類ではないため入力にしない）ため0点。
根拠_項目3: 段2の同居判定で確定。段1は保持先が共有規約ファイルで、散文の言明のみは(A)を満たさず(B)は採用理由の記載が無いため値域外、段2で却下選択肢の却下理由「解決マークは履歴に残らず経緯を辿れない」が採用理由「未応答を追える状態を保つ」と別に要る＝条件1該当で1点。
根拠_項目4: 応答粒度を機械的に検出する検査が現に無く、まとめて返しても作業が止まらないため「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md
```

---

## CASE-09

### 試行1

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転しても既存の1件ずつの呼び出しはそのまま残せ表記混在のみで動作も整合も損なわれず直す必要のあるファイルが無い（0本）ため本数条件は0点、構造変更・スキーマ変更・配布済み成果物の利用者側影響・蓄積データ移行のいずれも無いため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶ範囲がサブエージェントを起動する2つのスキル定義であり最も細かい単位で2つ以上に当たるため1点（決定を知らない作業者が別のまとめ方を選びうる箇所も現にこの2つでORも成立）。
根拠_項目3: 段1では規範を体現・強制する資産も散文記述も無く保持先が実行者の記憶のみで値域(A)(B)いずれも満たさず確定した値域外、段2の同居判定で確定—却下2案の却下理由が採用理由「1件ずつが最も単純だから」と同一で条件1に当たらず、受け入れた不利益・犠牲が無く条件2に当たらず、規範は1つの共有規約文書へ書けて分かれざるを得ないとは言えず条件3にも当たらないため0点。
根拠_項目4: 違反を機械的に検出する検査が無くまとめて渡しても作業が止まらないため、判定時点の状態として「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分では開いていないが実行環境により自動注入されたため事後検査用に申告する。判定には用いていない）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 ─ ツールでは強制できないが複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書（規約ファイル・プロジェクト指示）
根拠_項目1: 「直す必要のあるファイルは無い」で本数0本＝2本以下、かつ構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行がいずれも無いと明記されているため非本数条件も満たさず0点。
根拠_項目2: 影響が及ぶ単位が「サブエージェントを起動する2つのスキル定義」でスキル単位2つ＝2つ以上、加えて決定を知らない作業者が別の選択をしうる箇所も現に2つあり OR 条件も満たすため1点。
根拠_項目3: 段1で保持先が「実行者の記憶のみ」＝値域(A)(B)いずれも満たさず（列挙外として確定）、段2の同居判定へ進み、却下2案の却下理由が採用理由「1件ずつが最も単純だから」と同一で条件1非該当・犠牲無しで条件2非該当・規範が複数資産へ分かれざるを得るとは言えず（迷う側は同点処理で0）条件3非該当のため、段2（同居判定）で0点確定。
根拠_項目4: 「違反を機械的に検出する検査も無く、まとめて渡しても作業は止まらない」ため判定時点で検査が無い状態に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md（なお（worktree の）CLAUDE.md と ~/.claude/CLAUDE.md はツールで開いていないが環境により自動提示され、判定には用いていない）
```

---

## CASE-10

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 ／ 「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」の分岐
根拠_項目1: 反転で直すのは2本の文書のみで既存コミットは書き換えなくても矛盾・破損が生じない（数えない対象）ため本数条件は2本以下で0点、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無く非本数条件のOR も不成立。
根拠_項目2: 影響が及ぶのはコミットを作る手順1つで最も細かい単位でも1つ以下ゆえ0点、決定を知らない作業者が別の選択をしうる箇所も「現時点でこの1つだけ」でOR 条件も不成立（作業者が複数居る事実は本項目が数える単位＝モジュール／実行可能単位／成果物種別の入力にならない）。
根拠_項目3: 段1では強制を担う資産が無く散文記述のみで値域(A) を満たさず、2本の文書は共有規約ファイル／プロジェクト指示ファイルだが採用理由がどちらにも書かれておらず値域(B) も満たさないため同居判定へ進み、却下選択肢無し（条件1不該当）・受け入れた犠牲無し（条件2不該当）・2本への分散は「読み手が辿りやすいように写しを足した」たまたまの配置で分かれざるを得ないものではない（条件3不該当）ことから、段2（同居判定）で0点が確定。
根拠_項目4: 末尾の行の有無を機械的に検出する検査が現に無く欠けても作業が止まらないため、判定時点の状態のみを見て1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（Read ツールでは開いていないが、セッション開始時にハーネスが本文を自動注入したため挙げる。判定には使用していない）
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書（規約ファイル・プロジェクト指示）」
根拠_項目1: 反転で直す必要があるのは2本の文書のみ（既存コミットは直さなくても矛盾・破損なし＝数えない）で「3本以上で1点」に届かず、非本数条件（構造変更・スキーマ・配布済み成果物への影響・蓄積データ移行）はいずれも題材文で否定されているため0点。
根拠_項目2: 影響が及ぶ単位はコミット作成手順1つで「2つ以上で1点」に届かず、OR条件も「知らない作業者が別の選択をしうる箇所は現時点でこの1つだけ」で複数に当たらないため0点（作業者が複数居る事実は本項目の数える対象＝モジュール／実行可能単位／成果物種別でない）。
根拠_項目3: 段1では保持先が散文で述べるだけの2文書のため値域(A)を満たさず、採用理由がどちらにも書かれていないため値域(B)も満たさず、段2の同居判定で条件1（却下選択肢なし）・条件2（犠牲なし）・条件3（規範が分かれざるを得ないわけではなく現在の2本は写しによるたまたまの分散）いずれにも当たらず0点＝同居判定（段2）で確定。
根拠_項目4: 末尾行の有無を機械的に検出する検査が現に無く、欠けても作業が止まらないため「検査が無い」に該当し1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自身では開いていないがハーネスにより本文が自動注入された。判定には使用していない）
```

---

## CASE-11

### 試行1

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 — ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書
根拠_項目1: 「反転しても既に1件ずつ渡している呼び出しはそのまま残せ、表記が混在するだけで動作も整合も損なわれず、直す必要のあるファイルは無い」＝数える対象0本で本数条件0点、かつ構造変更・スキーマ/データ形式変更・配布済み成果物の利用者側影響・蓄積データ移行のいずれも無いと題材文が明示するため非本数条件のORも不成立。
根拠_項目2: 「影響が及ぶのはサブエージェントを起動する2つのスキル定義」＝最も細かい単位で2つ（2つ以上で1点）、かつ「決定を知らない作業者が別のまとめ方を選びうる箇所が現に同じ2つのスキル定義に在る」でOR条件も成立。
根拠_項目3: 段1で「規範を体現・強制する資産も散文で述べた記述も現に存在しない」ため値域(A)(B)いずれも満たす保持先が無く同居判定へ進み、段2で採用理由が「決定者は離任し到達手段も記録も無く内容を知りようがない」＝確かめられないため条件1・条件2は該当しない側へ倒し、条件3も規範が複数資産へ分かれざるを得ないという事実が題材文に無く（迷う場合は0点の同点処理）不成立で、段2（同居判定）で0点確定。
根拠_項目4: 「違反を機械的に検出する検査も無く、まとめて渡しても作業は止まらない」＝判定時点で検査が無く作業を阻止しないため1点。なお発見型シグナルに該当するが保持先が「どこにも無い（実行者の記憶にのみ在る）」ため短絡は適用されず4項目を通常判定した。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自動投入され本文が提示された。判定には使用していない）, ~/.claude/CLAUDE.md（同上）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 0
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書（規約ファイル・プロジェクト指示）」
根拠_項目1: 「直す必要のあるファイルは無い」（反転後も既存の呼び出しをそのまま残せ表記混在のみ）ため本数条件は0本で0点、構造変更・スキーマ・配布済み成果物の利用者影響・蓄積データ移行のいずれも無く非本数条件のORも成立しない
根拠_項目2: 影響が及ぶのはサブエージェントを起動する2つのスキル定義で、最も細かい単位（スキル）で2つ以上のため1点。決定を知らない作業者が別選択をする箇所も現に同2箇所ありORでも1点
根拠_項目3: 段1で保持先が実行者の記憶のみ（規範を体現・強制する資産も散文の記述も無い）ため値域(A)(B)いずれも満たさず段2の同居判定へ進み、採用理由は決定者が離任し記録も無く「確かめられない」ため条件1・条件2は該当しない側へ倒れ、規範は共有規約文書1本に書け複数資産へ分かれざるを得ないとは言えず条件3も非該当で0点（段2で確定）
根拠_項目4: 違反を機械的に検出する検査が無く、まとめて渡しても作業が止まらないため「検査が無い」に当たり1点
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分では開いていないが実行環境により本文が自動挿入された。判定には使用していない）
```

---

## CASE-12

### 試行1

```
項目1: 1
項目2: 1
項目3: 0
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 反転で直すのが共有規約1本＋実装2本の計3本という事実へ項目1の「3本以上で1点」を当てた（構造変更・スキーマ・配布済み成果物・蓄積データ移行はいずれも無しと明記され非本数条件は不成立）。
根拠_項目2: 影響が及ぶのが2つの実装モジュールという事実へ「2つ以上で1点」を当てた（決定を知らない作業者が別選択しうる箇所が現に同2モジュールにあるOR条件も成立）。
根拠_項目3: 段1は保持先が散文で述べるだけの共有規約ファイルのため(A)不成立・(B)は採用理由未記載で不成立、値域を満たす保持先が無く段2の同居判定へ進み、採用理由が離任により確かめられないため条件1・条件2は該当しない側へ倒し、規範は共有規約1本に収まり条件3も不成立で段2にて0点確定（却下理由・受け入れた不利益の内容は題材文に無く推測で補っていない）。
根拠_項目4: 機械的検出が現に無く作業も止まらないという事実へ項目4の「検査が無い→1点」を当てた（既存ADRの枠組みに収まるかの事実は題材文に無いため、行き先は迷えば新規ADRへ倒す安全則に従った）。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自動注入により本文が提示されたもの。判定には使用していない）
```

### 試行2

```
項目1: 1
項目2: 1
項目3: 0
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 反転で修正が要る追跡下ファイルが共有規約1本＋実装2本の計3本＝「3本以上で1点」に該当（構造変更・スキーマ・配布物影響・データ移行はいずれも無く非本数条件は不成立）。
根拠_項目2: 影響が及ぶ単位が実装モジュール2つ＝「2つ以上で1点」に該当。加えて決定を知らない作業者が別選択をしうる箇所が現に2つある OR 条件も満たす。
根拠_項目3: 段1で保持先は散文のみの共有規約ファイルで(A)非該当・採用理由未記載により(B)非該当のため値域を満たさず、段2の同居判定へ進み、採用理由が記録なし＋決定者離任で確かめられないため条件1・条件2は非該当扱い、規範は1ファイルに収まり条件3も非該当となり同居判定で0点確定。
根拠_項目4: 別ライブラリ使用を機械的に検出する検査が現に無く作業も止まらないため「検査が無い」＝1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, （自分では開いていないが環境により自動注入された: ~/.claude/CLAUDE.md, CLAUDE.md, （worktree の）CLAUDE.md）
```

---

## CASE-13

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 「反転しても直す必要のあるファイルは無い」で本数0本→0点、かつ配置・分割・名前の変更なし／形式変更するデータなし／移行の要る蓄積データなし／配布済み成果物の利用者側影響なしにより非本数条件（構造変更・スキーマ変更・配布物影響・データ移行）も全て不成立。
根拠_項目2: 影響が及ぶのは2つのプラグインで、最も細かい単位で数えて2つ以上→1点（加えて慣行を知らない作業者が明示を省きうる箇所が現に2つあるOR条件も成立）。
根拠_項目3: 段1では規範を体現・強制する資産も散文の記述も現存せず値域(A)(B)いずれも満たす保持先が無いため同居判定へ進み、採用理由が確かめられない（決定者が離任・記録なし）ため条件1・条件2は非該当側へ倒れるが、規範が雛形ファイルとスキーマ定義ファイルの2ファイルへ分かれざるを得ずいずれか1つの傍らに理由を置けないため条件3が成立し、段2（同居判定）で1点確定。
根拠_項目4: 明示の有無を機械的に検出する検査が無く欠けていても作業は止まらないため、判定時点の状態で「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md（以下は自分で開いたものではなくハーネスが自動注入したもの: ~/.claude/CLAUDE.md, CLAUDE.md, （worktree の）CLAUDE.md, MEMORY.md）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR（ADR 化する（3点以上）の行き先のうち、既存 ADR が扱っていない枠組みを立てる分岐）
根拠_項目1: 反転しても既存記述をそのまま残せ直すファイルが無く（0本＜3本）、構造変更・スキーマ／データ形式変更・配布済み成果物への影響・蓄積データ移行のいずれも題材文が明示的に否定しているため非本数条件も不成立で0点。
根拠_項目2: 影響が及ぶのがサブエージェント定義を持つ2つのプラグインで「2つ以上で1点」に該当し、加えて決定を知らない作業者が別の選択をしうる箇所が現に2箇所ある点でOR条件も満たすため1点。
根拠_項目3: 段2（同居判定）で確定。規範を体現・強制する資産も散文の記述も無く値域(A)(B)を満たす保持先が無いため段1で0点にならず、採用理由が確かめられないため条件1・条件2は非該当へ倒したが、規範が雛形ファイルとスキーマ定義ファイルへ分かれざるを得ず（片方だけでは既存定義の素通り／新規作成時の漏れが生じる）理由を1つの傍らに置けないため条件3該当で1点。
根拠_項目4: 明示の有無を機械的に検出する検査が無く欠けても作業が止まらないため、判定時点の状態として「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（ハーネスが自動注入したもので、判定には使用していない）, ~/.claude/CLAUDE.md（同上）
```

---

## CASE-14

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）→ 命名規約節の書式的命名規約として ADR 外（強制未実装につき同節末尾の例外により、行き先は強制の実装＝Linter／フォーマッタ規約とその設定であり、一般則の第1分岐「実装・テスト資産・操作手順」に対応する）
根拠_項目1: 反転で修正が要る追跡下ファイルは雛形1本（既存識別子は表記混在のまま残せるので数えない）で3本未満、かつ配置・分割・名前の変更／スキーマ・データ形式の変更／蓄積データ移行／配布済み成果物の利用者側影響のいずれも題材文が明示的に否定しているため、本数条件も非本数条件も満たさず0点
根拠_項目2: 影響が及ぶ単位は「ADR を新規に起こす手順」1つで2つ未満、規範を知らない作業者が別選択をしうる箇所も現時点で1つのみ（OR条件は「現に複数」を要求）、従う作業者も1人のため0点
根拠_項目3: 段1では雛形は値域(A)の「テンプレート」に当たるが規範を散文で述べるだけで採番を体現も強制もしておらず(A)不成立、採用理由はどこにも書かれておらず(B)も不成立、値域を満たす保持先が無いので段2の同居判定へ進み、却下選択肢なし（条件1非該当）・受け入れた不利益なし（条件2非該当）・規範は雛形1ファイルに収まり分かれざるを得ない事情なし（条件3非該当）で段2（同居判定）にて0点確定
根拠_項目4: 違反を機械的に検出する検査が現に無く、形式違反でも作業が阻止されないため「検査が無い／実装していない」に当たり1点
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分では開いていないが、システムにより本文が自動挿入されたため申告）
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）／命名規約の書式的行 = Linter／フォーマッタ規約として配置（ADR 外・強制未実装のため表が例外として働き、行き先は強制の実装とその設定）
根拠_項目1: 反転で直すのは雛形1本のみ（3本以上に届かず0点）で、構造変更・スキーマ/データ形式変更・配布済み成果物への影響・蓄積データ移行のいずれも題材文が明示的に否定するため非本数条件のOEも不成立。
根拠_項目2: 影響が及ぶ範囲は「ADR を新規に起こす手順」1つで2つ以上に届かず、決定を知らない作業者が別形式を選びうる箇所も現に1つのみでOR条件も不成立。
根拠_項目3: 段1では保持先が雛形内の散文コメントのみで「規範を散文で述べるだけの記述は (A) を満たさない／それ以外はどちらにも当たらない」により値域外（決定者の説明も値域外の保持先）、段2の同居判定で却下選択肢なし＝条件1不成立・犠牲なし＝条件2不成立・規範は雛形1本に収まり分かれざるを得ないとは言えず条件3不成立となり、段2で0点確定。
根拠_項目4: 違反を機械的に検出する検査が現に無く作業も阻止されないため、判定時点の状態のみを見て1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, ~/.claude/CLAUDE.md（ハーネスが自動注入・判定には未使用）, （worktree の）CLAUDE.md（ハーネスが自動注入・判定には未使用）
```

---

## CASE-15

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転で直すのは共有規約ファイル1本のみで「3本以上で1点、2本以下で0点」の本数条件に届かず、構造変更・スキーマ変更・配布済み成果物の利用者側影響・蓄積データ移行のいずれも無いため非本数条件（OR）も成立せず0点。
根拠_項目2: 影響が及ぶ範囲はレビュー応答を書く手順1つで「2つ以上で1点、1つ以下で0点」により0点、かつ規範を知らない作業者が別の選択をしうる箇所も現時点で1つだけのため OR も不成立（「従う作業者が現に複数居る」は本項目が数える単位＝モジュール／実行可能単位／成果物種別に当たらない）。
根拠_項目3: 段1で確定。保持先の共有規約ファイルは値域(B) に当たり、当該決定の採用理由「どの指摘が未応答か追える状態を保つため」が規範を述べた同じ節に現に書かれているため値域を満たし0点（同居判定へは進まない）。
根拠_項目4: 応答粒度を機械的に検出する検査が現に無く、まとめて返しても作業が阻止されないため「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 ―「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転で直すのは共有規約ファイル1本のみ（2本以下で0点）、かつ構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも成立せず0点。
根拠_項目2: 影響が及ぶのはレビュー応答を書く手順1つのみで「2つ以上で1点、1つ以下で0点」の1つ以下に当たり、知らない作業者が別の選択をしうる箇所も現に1つだけでORも不成立（作業者が複数居ることは本項目の数える対象＝モジュール／実行可能単位／成果物種別に当たらない）ため0点。
根拠_項目3: 段1（値域判定）で確定。保持先は規範を担う共有規約ファイルで、散文記述のため値域(A)は満たさないが、当該決定の採用理由「どの指摘が未応答か追える状態を保つため」が同じ節に現に書かれており値域(B)を満たすため0点（同居判定へは進まない）。
根拠_項目4: 応答粒度を機械的に検出する検査が現に無く、まとめて返しても作業が阻止されないため「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```

---

## CASE-16

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「単一の変更に閉じる判断 → その変更の PR 説明」
根拠_項目1: 反転で直すのは共有規約ファイルとスクリプトの計2本で「3本以上で1点、2本以下で0点」の本数条件に届かず、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件（OR）も立たない
根拠_項目2: 影響が及ぶ単位は最も細かい単位で数えて1スクリプトのみで「2つ以上で1点、1つ以下で0点」に照らし0点、独立に別の選択をする箇所も現に1つ・作業者1人のためOR条件も立たない
根拠_項目3: 段1で保持先の共有規約ファイルは規範を散文で述べるだけのため値域(A)を満たさず、採用理由がどの文書にも書かれていないため値域(B)も満たさず（PRレビューコメントは確定した値域外）、段2の同居判定で採用理由が確かめられない（決定者へ到達不能・記録なし）ため条件1・条件2は該当しない側へ倒し、規範は共有規約ファイル1本で分かれざるを得ない状況にないため条件3も不該当となり、段2（同居判定）で0点確定
根拠_項目4: 外部ライブラリ使用を機械的に検出する検査が現に無く使っても作業が阻止されないため、判定時点の状態のみを見て「検査が無い」に当たり1点
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（Readでは開いていないが、ハーネスにより本文が自動的に文脈へ投入されたため申告する）
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 → 単一の変更に閉じる判断 → その変更の PR 説明
根拠_項目1: 反転時に直すのは共有規約ファイル1本とスクリプト1本の計2本で「3本以上で1点」の本数条件を満たさず、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件（OR）も成立せず0点。
根拠_項目2: 影響が及ぶ単位はスクリプト1つで「2つ以上で1点」に満たず、決定を知らない作業者が別の選択をしうる箇所も現に1つだけなのでOR条件も成立せず0点。
根拠_項目3: 保持先は規範を散文で述べるだけの共有規約ファイルで値域(A)に当たらず、(B)は採用理由が現に書かれていないため不成立、よって段2の同居判定へ進み、採用理由が確かめられない（離任・記録なしで内容を知りようがない）ため条件1・条件2は該当しない側へ倒れ、規範も複数資産へ分かれざるを得ないわけではなく条件3も非該当で、段2で0点確定。
根拠_項目4: 外部ライブラリ使用を機械的に検出する検査が現に無く作業も阻止されないため、判定時点の状態のみを見る規定により1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（Readツールでは開いていないが、ハーネスにより本文が自動注入されたため隠さず記載）
```

---

## CASE-18

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 反転しても直す必要のあるファイルは無い（0本）ため本数条件で0点、かつ置き場所・分割・名前の不変、データ形式変更なし、蓄積データ移行なし、既配布成果物の利用者側影響なしで非本数条件のOR4項もすべて非該当。
根拠_項目2: 影響が及ぶのが参照を書く2つの成果物種別（ADR 文書・スキル定義）で「2つ以上で1点」に該当し、規範を知らない作業者が別の書き方を選びうる箇所も現にこの2単位に在るためOR条件でも1点。
根拠_項目3: 段1では規範を担う資産も散文の記述も現に存在せず値域(A)(B)いずれも満たす保持先が無いため後段へ進み、段2の同居判定で採用理由が確かめられない（離任・記録なし）ため条件1・条件2は非該当とし、規範が検査スクリプト本体と判定用入力データの2資産へ分かれざるを得ず理由をいずれか一方の傍らに置けないため条件3で1点確定。
根拠_項目4: 違反を機械的に検出する検査が現に無く別の書き方をしても作業が止まらないため、判定時点の状態のみを見る規定の「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 命名規約の ADR 化基準の例外（書式的命名規約 → Linter／フォーマッタ規約として配置＝ADR 外。強制の実装とその設定へ置き、採用理由を併記）
根拠_項目1: 「既存の相対パス参照はそのまま残せ表記が混在するだけ／直す必要のあるファイルは無い」＝数える対象0本で本数条件0点、かつ「置き場所も分割も名前も変わらず、形式変更・蓄積データ移行・配布済み成果物の利用者側影響も無い」ため非本数条件4つのいずれにも当たらず0点。
根拠_項目2: 「影響が及ぶのは2つの成果物種別（ADR 文書とスキル定義）」を最も細かい単位で数えて2つ＝「2つ以上で1点」に該当（規範を知らない作業者が別の選択をしうる箇所が現に2つある点も OR 条件を満たす）。
根拠_項目3: 段1では「規範を体現・強制する資産も散文の記述も現に存在しない」ため値域(A)(B) を満たす保持先が無く、段2の同居判定へ進み、採用理由が確かめられない（記録も到達手段も無い）ため条件1・条件2 は非該当側へ倒し、条件3（検査スクリプト本体だけでは境界が揺れ、判定用入力データだけでは検査が動かない＝規範が別ファイルの2資産へ分かれざるを得ない）に該当して1点＝段2で確定。
根拠_項目4: 「違反を機械的に検出する検査は現に無く、別の書き方をしても作業は止まらない」＝判定時点で検査が無く阻止もされないため1点。合計3点は粒度判定上「ADR 化を推奨」だが、当該規範は反転しても配置・分割・名前が変わらず表記に閉じ、検査スクリプトを書ける＝原理的にツール強制可能であるため命名規約表の書式的の側に振り分けられ、「書式的命名規約は強制がまだ実装されていない場合も ADR 外」「合計が3点へ届く場合も振り分けは表が決める」の例外が掛かる。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```

---

## CASE-19

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR
根拠_項目1: 反転で直すのは共有規約ファイル1本のみ（2本以下で0点）、かつ構造変更・スキーマ/データ形式変更・配布済み成果物への影響・蓄積データ移行のいずれも無しと題材が明示するため非本数条件も成立しない。
根拠_項目2: 影響が及ぶ単位がスキル定義を持つ2つのプラグインであり「2つ以上で1点」に該当（決定を知らない作業者が別の選択をしうる箇所が現に2つある点でも OR 条件を満たす）。
根拠_項目3: 段2の同居判定で確定。段1は規範を担う資産が散文の共有規約ファイルのみで (A) 非該当・(B) は採用理由が書かれておらず非該当、値域を満たす保持先が無いため後段へ進み、採用理由は確かめられないので条件1・条件2 は該当しない側へ倒し、規範がスキーマ定義ファイルと雛形ファイル（同じ列挙項でも別ファイルゆえ別資産）へ分かれざるを得ない点で条件3 が成立し1点。
根拠_項目4: 列挙の有無を機械的に検出する検査が現に無く、欠けていても作業が止まらないため「検査が無い」に該当し1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（開いたのではなくセッション開始時に自動提示されたもの。判定には使用していない）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR（ADR 化する（3点以上）の行き先）
根拠_項目1: 反転で直すのは共有規約ファイル1本のみ（3本未満で0点）、かつ構造変更・スキーマ/データ形式変更・配布済み成果物の利用者影響・蓄積データ移行のいずれも無しと題材文が明示するため非本数条件のORも成立しない。
根拠_項目2: 影響が及ぶ範囲がスキル定義を持つ2つのプラグインで「2つ以上で1点」に該当し、加えて規範を知らない作業者が列挙を省きうる箇所が現に同2箇所に在るためOR条件でも1点。
根拠_項目3: 段1では保持先が共有規約ファイルの散文のみで(A)を満たさず(B)も採用理由が未記載のため値域外、段2の同居判定へ進み、採用理由が確かめられない（決定者が離任し記録も無い）ため条件1・条件2は非該当へ倒し、スキーマ定義と雛形の双方へ書かざるを得ず一方の傍らに理由を置けない点が条件3（同じ列挙項でも別ファイルは別資産）に該当して1点確定。
根拠_項目4: 列挙の有無を機械的に検出する検査が現に無く欠けても作業が止まらないため「検査が無い」で1点。なお題材文に既存 ADR の枠組みに関する事実が無く補えなかったため、行き先は本文の安全則（迷えば core 寄り＝新規 ADR に倒す）に従い新規 ADR とした。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分では開いていないが、システムにより本文が自動提示されたため申告。判定には使用していない）
```

---

## CASE-20

### 試行1

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: ADR 化する（3点以上）の行き先 → 新規 ADR
根拠_項目1: 反転しても既存の指示文はそのまま残せ直す必要のあるファイルは無い（0本）ため本数条件0点、構造変更・スキーマ・配布済み成果物への影響・蓄積データ移行のいずれも無く非本数条件のORも成立しないため0点。
根拠_項目2: 影響が及ぶ単位がエージェントを起動する2つのスキル定義であり「2つ以上で1点」に該当、加えて慣行を知らない作業者が別の選択をしうる箇所が現にその2箇所にあるOR条件にも該当するため1点。
根拠_項目3: 段1では規範を担う資産も散文の記述も存在せず値域(A)(B)を満たす保持先が無いため段2へ進み、採用理由を確かめられない（決定者が離任・記録なし）ため条件1・条件2は該当しない側へ倒し、規範が共有規約ファイルと手順書のいずれか一方では効かず分かれざるを得ない条件3に該当して段2（同居判定）で1点を確定。
根拠_項目4: 明記の有無を機械的に検出する検査が無く範囲を書かずに起動しても作業が止まらないため「検査が無い」に該当し1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自動注入により内容が提示されたもの。自発的に開いてはいない）
```

### 試行2

```
項目1: 0
項目2: 1
項目3: 1
項目4: 1
合計: 3
行き先: 新規 ADR（「ADR 化する（3点以上）の行き先」のうち、既存 ADR が扱っていない枠組みを立てる分岐）
根拠_項目1: 反転しても既に範囲を明記した指示文はそのまま残せ直すファイルが無く0本で本数条件0点、かつ構造変更・スキーマ変更・既配布成果物の利用者側影響・蓄積データ移行のいずれも無しと明記されており非本数条件のORも成立しないため0点。
根拠_項目2: 影響が及ぶのがエージェント起動の2つのスキル定義で「2つ以上で1点」に該当し、加えて慣行を知らない作業者が別の選択をしうる箇所が現に同2箇所あるためORでも1点。
根拠_項目3: 段1で保持先が「どこにも無い」（規範を体現・強制する資産も散文の記述も無い）ため値域(A)(B)いずれも満たさず後段へ進み、同居判定で採用理由が確かめられないため条件1・条件2は該当しない側へ倒したが、規範が共有規約ファイルと手順書の2ファイルへ分かれざるを得ずいずれか1つの傍らに理由を置けないため条件3で1点確定（2段判定の後段＝同居判定で確定）。
根拠_項目4: 明記の有無を機械的に検出する検査が無く範囲を書かずに起動しても作業が止まらないため、項目4の「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分で開いたものではなく、セッション開始時に本文が自動提示されたもの。判定には使用していない）
```

---

## CASE-21

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールで自動強制される規範 → 実装・テスト資産・操作手順に置く」分岐
根拠_項目1: 反転時に直すのは共通実装1本のみ＝2本以下で0点、かつ配置・分割・命名の変更なし／スキーマ・データ形式の変更なし／蓄積データ移行なし／配布済み成果物の利用者側影響なしで非本数条件（OR）も不成立。
根拠_項目2: 影響が及ぶ単位は CLI を呼び出す1つのスキル定義のみ＝1つ以下で0点、かつ規範を知らない作業者が別の選択をしうる箇所も「現時点でこの1つ」だけでありOR条件も不成立（将来の見込みは加算しない）。
根拠_項目3: 段1（値域）で確定。保持先が「規範を強制する実装本体」（本文を必ず一時ファイルへ書き出してファイル引数を組み立てる共通実装1本）であり、値域(A)は採用理由の記載の有無も強制の稼働も問わない（検査が無くても満たす）ため0点となり、同居判定へは進まない。
根拠_項目4: 判定時点で共通実装を経由しない直接呼び出しを機械的に検出する検査が現に無く、直接渡しても作業が止まらないため「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（ハーネスが自動注入。判定には使用せず）, ~/.claude/CLAUDE.md（同上）
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 ／「ツールで自動強制される規範 → 実装・テスト資産・操作手順に置く」分岐
根拠_項目1: 反転で直すのは共通実装1本のみ（3本未満）で、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため、本数条件・非本数条件とも不成立で0点。
根拠_項目2: 影響範囲は CLI を呼び出す1つのスキル定義のみ（最も細かい単位で1つ以下）、かつ規範を知らない作業者が別の選択をしうる箇所も現に1つだけで OR 条件も不成立、従う作業者も1人のため0点。
根拠_項目3: 段1（値域判定）で確定。規範は「CLI 呼び出しを組み立てる共通の実装本体」に置かれ実装が規範を体現しており値域(A)「規範を強制する実装本体」に当たる。(A) は採用理由の記載の有無も強制の稼働も問わない（検査が無くても満たす）ため0点で、同居判定へ進まない。
根拠_項目4: 共通実装を経由しない直接呼びを機械的に検出する検査が現に無く作業も止まらないため、「検査が無い」に当たり1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自動注入により本文が提示されたもの。判定には使用していない）
```

---

## CASE-22

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先 — 単一の変更に閉じる判断 → その変更の PR 説明
根拠_項目1: 反転で直すのは共有規約ファイル1本のみで「3本以上で1点」に届かず、非本数条件（構造変更・スキーマ・配布済み成果物への影響・蓄積データ移行）もいずれも無いため0点。
根拠_項目2: 影響が及ぶ単位はスキル定義1つで「2つ以上で1点」に届かず、決定を知らない作業者が独立に別の選択をしうる箇所も現に1つのみのため OR 条件も成立せず0点。
根拠_項目3: 段1では規範を担う資産が共有規約文書で散文の言明のみ（値域(A)不成立）かつ採用理由がどの文書にも書かれていない（値域(B)不成立）ため値域を満たす保持先が無く、段2の同居判定で条件1（却下理由が採用理由「引数の解釈が壊れる書き方を避けるため」と同一）・条件2（犠牲なし）・条件3（規範は1本に収まる）いずれも不成立となり0点で確定。
根拠_項目4: 本文を直接引数へ渡す書き方を機械的に検出する検査が現に無く作業も阻止されないため、判定時点の状態で1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（ハーネスにより自動注入されたもので、判定には使用していない）
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールでは強制できないが、複数の作業者・複数の箇所へ効かせたい規範 → 共有規約文書」
根拠_項目1: 反転で直すのは共有規約ファイル1本のみで本数条件（3本以上）を満たさず、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件（OR）にも当たらない。
根拠_項目2: 影響が及ぶ最小単位は CLI を呼び出す1つのスキル定義のみで「2つ以上」に届かず、決定を知らない作業者が別の選択をしうる箇所も現時点で1つだけなので OR 条件も成立しない。
根拠_項目3: 段1では保持先が散文1行の共有規約ファイルのみで (A) を満たさず、採用理由がどの文書にも書かれていないため (B) も満たさず値域外、段2の同居判定で却下理由が採用理由と同一（条件1不該当）・犠牲無し（条件2不該当）・規範は1本の資産に収まる（条件3不該当）となり、段2（同居判定）で0点確定。
根拠_項目4: 本文をそのまま引数へ渡す書き方を機械的に検出する検査が現に無く作業も止まらないため、項目4の1点条件「検査が無い」に当たる。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```

---

## CASE-23

### 試行1

```
項目1: 0
項目2: 0
項目3: 1
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先 —「単一の変更に閉じる判断 → その変更の PR 説明」
根拠_項目1: 反転で直す必要があるのは実装本体1本のみ（2本以下で0点）で、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件のORも成立しない。
根拠_項目2: 影響が及ぶ単位は1つのスクリプトのみ（1つ以下で0点）、かつ規範を知らない作業者が別の選択をしうる箇所も現に1つだけでOR条件も成立しない。
根拠_項目3: 段1では、規範を担う実装が出力先を体現も強制もしていない「強制を担っていない実装の中の散文コメント」で (A)(B) いずれの値域にも当たらず、段2の同居判定で却下案「専用ファイルへ書き出す」の却下理由（書き込み可能場所が環境ごとに異なる）が採用理由（標準出力の機械処理と混ざらない）と別に要るため条件1に該当し1点で確定。
根拠_項目4: 違反を機械的に検出する検査が現に無く標準出力へ出しても作業が止まらないため、判定時点の状態として1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md, ~/.claude/CLAUDE.md, MEMORY.md
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 1
項目4: 1
合計: 2
行き先: ADR 化しない（2点以下）の行き先のうち「単一の変更に閉じる判断 → その変更の PR 説明」
根拠_項目1: 反転で直すのは実装本体1本で「3本以上」に届かず、構造変更・スキーマ変更・配布済み成果物への影響・蓄積データ移行のいずれも無いため非本数条件の OR も成立せず0点。
根拠_項目2: 影響が及ぶのはスクリプト1つのみで「2つ以上」に満たず、規範を知らない作業者が別の選択をしうる箇所も現に1つだけのため OR 条件も成立せず0点。
根拠_項目3: 段1では規範が実装本体内の散文コメントのみで実装が強制を担っていないため (A) を満たさず（`references/` 配下でもないため (B) にも当たらず）値域を満たす保持先が無く、段2の同居判定で却下案「専用ファイルへ書き出す」の却下理由（環境ごとに書込先が異なり取り決めが増える）が採用理由（標準出力の機械処理と混ざらないため）と別に要るため条件1に当たり1点（段2で確定）。
根拠_項目4: 違反を機械的に検出する検査が現に無く標準出力へ出しても作業が止まらないため、判定時点の状態として1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md, CLAUDE.md（自分で開いたものではなく、セッション開始時にシステム側から自動注入された。判定には用いていない）, ~/.claude/CLAUDE.md（同上、自動注入）
```

---

## CASE-24

### 試行1

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の行き先のうち「ツールで自動強制される規範 → 実装・テスト資産・操作手順に置く」分岐（命名規約節では書式的命名規約＝ADR 外・強制の実装へ配置に対応）
根拠_項目1: 反転で直すのは雛形1本のみ（2本以下→0点）、かつ配置・分割・命名の変更もスキーマ／データ形式変更も蓄積データ移行も配布済み成果物への影響も無いとされ、非本数条件のいずれにも当たらない
根拠_項目2: 影響が及ぶ単位は「ADR を新規に起こす手順」1つで1つ以下→0点、決定を知らない作業者が別の選択をしうる箇所も現に1つのみで OR 条件も不成立（作業者1人も同様）
根拠_項目3: 段1（値域判定）で確定。規範を担う資産が ADR の雛形＝値域(A)「型定義・テンプレート・スキーマ定義」に当たり、採用理由の記載有無も強制の稼働有無も問わないとの規定により0点（同居判定へは進まない）
根拠_項目4: 識別子形式を機械的に検出する検査が現に無く、形式違反でも作業が止まらないため「検査が無い」に該当し1点
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```

### 試行2

```
項目1: 0
項目2: 0
項目3: 0
項目4: 1
合計: 1
行き先: ADR 化しない（2点以下）の第1分岐＝実装・テスト資産・操作手順に置く（書式的命名規約の例外により、強制の実装＝雛形および形式検査の設定が行き先）
根拠_項目1: 反転で直すのは雛形1本のみ（既存識別子はそのまま残せ矛盾も破損も生じない）で3本未満、かつ構造変更・スキーマ／データ形式変更・配布済み成果物の利用者影響・蓄積データ移行のいずれも題材文が現に無いとするため、本数条件・非本数条件とも0点。
根拠_項目2: 影響が及ぶ単位は「ADR を新規に起こす手順」1つのみで2つ未満、かつ決定を知らない作業者が別の選択をしうる箇所も現時点で1つだけのため OR 条件も満たさず0点。
根拠_項目3: 段1（値域判定）で確定。規範を担う資産が書式指定つき front-matter を備えた雛形であり値域(A)の「型定義・テンプレート・スキーマ定義」に当たる（採用理由の記載の有無・強制の稼働は問わないと明記）ため0点、同居判定へは進まない。
根拠_項目4: 識別子形式を機械的に検出する検査が現に無く、形式違反でも作業が阻止されないため、判定時点の状態のみを見て1点。
参照ファイル: （プロンプトファイル）, （対象文書）adr-scoping.md
```
