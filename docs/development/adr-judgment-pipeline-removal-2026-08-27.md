# ADR 化要否判定パイプラインの廃止に伴う判定記録（2026-08-27）

Issue #783 の受入条件 AC6・AC7 が求める 2 つの判定の記録である。AC7（本簡素化そのものの ADR 化要否）を §1 に、AC6（既存 ADR の遷移要否）を §2 に置く。

判定に使った条文の版は **commit `19daa82`**（本 Issue による改訂前の main）である。判定対象の決定が改訂しようとしている条文で自分を判定しないよう、`adr-scoping.md`・`adr-judgment-contract.md`・`adr-scoring-thresholds.json` はいずれも改訂前の状態で読んだ。

## §1: 本簡素化の設計判断に対する ADR 化要否の判定（AC7）

### 判定対象の決定

ADR 化要否の判定を機械実行する一式（固定題材集合の実行支援スクリプト・その期待値と fixtures・判定結果の直列化契約・点数の閾値設定・開発専用台帳・退行検出のテスト）を、配布価値の本体でないと見て削除し、判定の実体（閾値の数値と値域の定義）を存続する条文 `adr-scoping.md` へ内製化する。

### 必要条件: 却下代替が在ること

**成立**。採らなかった選択肢とその却下理由が、対として特定できる。

| 採らなかった選択肢 | 却下理由 | 出所 |
|---|---|---|
| 点数化ごと撤去し、判定を定性判断へ転換する | 判定の枠組み（4 項目・閾値・合計点）の変更は本簡素化の射程を超える。装置を持つか否かとは独立に反転する決定である | 設計 spec 節1「存続対象」（`docs/superpowers/specs/2026-08-27-adr-plugin-simplification-design.md`）が「4項目・閾値・合計点という判定の枠組み自体は現状維持とし、点数化の撤去は行わない」と明記 |
| 参照除去を後続工程（スキル再設計）まで遅らせ、削除だけを先に行う | 条文が判定の実体を削除対象へ外出ししているため、条文が指示先を失う期間が生じる | 同 spec 節2 が「PR2 まで参照除去を遅らせる代替案を棄却した帰結としての二段作業」と記録 |
| 削除ではなく退避・縮退（凍結資産としての保存）に留める | 動かさない検査資産は常設ゲートへの追随義務だけを残し、検証の便益を生まない。復元は git 履歴で足りる | 同 spec「決定事項」1「退避・縮退ではなく削除（git履歴には残る）」 |

代替案の名指しだけでも、トレードオフの記述だけでもなく、名指しと却下理由が対になっている。成立するか迷う余地は無い。

### 粒度判定基準（4 項目）

発見型の短絡は適用しない。本件は未記録の規範の発見ではなく、意図的に下した設計判断であり、短絡の前提（保持先が実装・テスト資産・操作手順であり、違反が現に検出・阻止されている）にも当たらないためである。したがって 4 項目すべてを判定した。

| 項目 | 点数 | 根拠 |
|---|---|---|
| 1. 後戻りコストが高い | **1** | 「配布済み成果物への利用者影響」に該当する。配布プラグイン `plugins/adr` から実行支援スクリプト 1 本とスキル参照ファイル 2 本が消え、版が `0.5.0` → `0.6.0` へ上がる。利用者から見れば提供物の削除である。あわせて反転時に直す追跡下ファイル（追加だけでは矛盾が解けないもの）も、条文 `adr-scoping.md`・スキル定義 `SKILL.md`・2 つの plugin manifest で閾値 3 件に達する |
| 2. 複数の適用先に波及する | **0** | 本決定の規範（判定を機械実行する装置を持たない）が適用される最小単位を、判定を実行する `manage-adr` スキル 1 単位と読むか、装置が跨っていた実行可能単位・成果物種別 4 単位と読むかで結論が割れる。項目単位の同点処理に従い 0 点へ倒した |
| 3. 採用理由が時間経過で揮発しやすい | **1** | 保持先が値域内に無い。(A) 本決定を強制する実装・テスト資産・操作手順は、装置の削除後に存在しない。(B) 条文 `adr-scoping.md` は `references/` 配下の共有規約ファイルとして値域に入るが、本決定の採用理由は同ファイル本文に書かれず（規範本文の責務は何が正しいかを述べることであり、廃止の経緯は書かない）、契約仕様の「(B)でも採用理由がなければ対象外」に当たる。保持先が無いため条件 1〜3 を評価し、条件1（却下理由が採用理由とは別に必要である）に該当した。採用理由（配布価値の本体でないこと・規模比 3.5 倍）からは、なぜ点数化ごと撤去しなかったか・なぜ後続工程へ遅らせなかったかは導けない |
| 4. ツールで自動強制できない | **1** | 阻止の状態は `検査なし`。本決定への違反（判定を機械実行する装置の再導入、判定の実体の条文外への再外出し）を検出・阻止する検査はリポジトリに無い。削除対象の bats が持っていた「条文が契約仕様と閾値設定を名指していること」を要求するアサーションは旧規範の側を強制するものであり、本決定の規範を守らない。Issue #783 の制約により代替ガードも新設しない |

**合計 3 点**（閾値: スコア境界 3 点以上で ADR 化を推奨）。

### 結論

**ADR 化する。行き先は新規 ADR。**

行き先の振り分けは、既存 ADR が扱っていない枠組みを立てるかどうかで決めた。

- `ADR-202608011651-01`（`validity: 有効`）決定2 は、粒度判定基準の項目名とスコア境界を ADR の決定内容から外し、実体を運用文書へ委ねている。したがって「閾値と値域の定義を条文へ内製化する」側は同 ADR の枠組みの中の事項であり、運用文書の改訂として行えば足りる（同 ADR :50「項目の追加・削除・改名、および境界の点数の変更は、運用文書の改訂として行い、本 ADR の反転を要さない」）。この面は新規 ADR の決定内容に含めない
- 一方「判定の品質担保を機械検査に置かず、条文の質だけで支える」側は、判定手続きの中身ではなく品質担保手段の選択であり、これを決めた記録は corpus に存在しない。`ADR-202607230648-01` も抽出時点の配置対象を定めたもので、抽出後に何を持ち続けるかは扱っていない。既存 ADR が扱っていない枠組みにあたるため、新規 ADR の側へ振り分けた

束ねの制約に照らし、新規 ADR の core は「判定の品質担保を機械検査に置かない」1 つに絞った。閾値の内製化はこれと独立に反転しうるうえ、上記のとおり ADR の決定内容に含めない事項であるため、束ねていない。

### 起票した ADR

`docs/adr/ADR-202608280911-01-adr-judgment-quality-without-machine-verification.md`

決定内容そのものは 2026-08-27 にユーザー承認済み（設計 spec「決定事項」1 および レビュー文書 §0 決定1）であるため、`status: 承認済み` / `validity: 有効` で起票した。`docs/adr/index.md` は同一コミットで再生成し、worktree から `bash plugins/adr/scripts/lint-adr.sh docs/adr` を明示実行して exit 0 とレイヤ2（index 同期）・レイヤ3（相互参照双方向性）の不発火を確認した。

## §2: 判定仕様・固定題材集合・退行検出網に関わる既存 ADR の遷移要否（AC6）

### 走査した範囲と絞り込み条件

設計 spec 節1 付随工程とレビュー文書 §F は「遷移を要する ADR は無い」と実測しているが、本節はその追認ではなく独立の走査として行った。走査対象は `docs/adr/` 配下の ADR ファイル全件（本 Issue で起票した `ADR-202608280911-01` を含め 72 件）である。絞り込みは 3 つの角度で行い、いずれかに掛かったものを候補として本文を読んだ。

1. **削除対象のパス断片による走査**: `adr-scoping-cases` / `adr-judgment-contract` / `adr-scoring-thresholds` / `migrate-return-schema` / `return-schema-migration`。該当は `ADR-202607230648-01` の 1 件（`:26`）のみ
2. **判定仕様の語彙による走査**: `固定題材` / `判定器` / `閾値` / `スコア境界` / `退行検出`。該当は 9 件。うち 5 件（`ADR-202606020032-01`・`ADR-202606270040-01`・`ADR-202607160857-02`・`ADR-202607261002-01`・`ADR-202608011353-01`）は「閾値」を一般語として用いているだけで、ADR 化要否の判定仕様とは無関係であることを本文で確認した
3. **機構の語彙による走査**: `scoping-cases` / `判定パイプライン` / `題材` / `expectations` / `defect-ledger` / `crosscheck` / `再導出` / `検査器`。該当 8 件のうち 6 件は growth プラグインの ADR で、distill の「再導出」を指す同綴りの別概念であることを本文で確認した

角度 1〜3 で残った候補に、ADR 運用そのものを主題とする ADR（drift-lint・編集機構・格下げ判定・配布境界）を目視で加えて評価した。

### 該当した ADR と判定結果

| ADR | validity | 判定 | 根拠 |
|---|---|---|---|
| `ADR-202607230648-01`（ADR 運用機構のプラグイン抽出） | 有効 | **変更不要（無編集）** | `:26` の射程注記が削除対象 `adr-scoping-cases.sh` を配置判断の**例示**として引用するのみ。決定内容は「抽出時点で移す対象の列挙」と「以後のテスト・fixture の配置は本 ADR の射程外」であり、例示ファイルの消滅では無効化されない。ADR は時点の決定の記録であるため、陳腐化の注記も追記しない（ユーザー裁定2・2026-08-27）。陳腐化の注記は生きた規約文書 `docs/distribution-boundary.md` の側のみへ置く。`edit-decision.md` の変更種別分類（core／非core／些末）も発生しない |
| `ADR-202608011651-01`（必要条件と粒度判定基準の委譲） | 有効 | **変更不要** | 決定2 が「粒度判定基準が何項目で、各項目が何という名称を持ち、境界の点数が何点であるかは本 ADR が保持しない」と定め、`:50` が「項目の追加・削除・改名、および境界の点数の変更は、運用文書の改訂として行い、本 ADR の反転を要さない」と続ける。本 Issue の内製化は閾値の数値と値域の定義を運用文書 `adr-scoping.md` の内側へ移す改訂であり、同 ADR が保持対象とする 4 つ（固有価値の定義と前置構造・順序・同点処理の向き・適用範囲）のいずれも反転しない。`:52` の「実務手順の所在や表現が変わっても、上記4つの骨子は本 ADR が保持する」もこの読みを支持する |
| `ADR-202607191825-01`（旧: ADR 化要否の判定基準と記録の参照原則） | 上書き済み | **変更不要** | 決定1（4 項目チェックリストとスコア境界）は `ADR-202608011350-01` により既に反転済みで、同 ADR 自身も `ADR-202608011651-01` へ上書きされている。退役した ADR は凍結原則により決定内容の改訂を受けない。判定パイプラインへの言及も持たない |
| `ADR-202608011350-01`（旧: 却下代替の必要条件） | 上書き済み | **変更不要** | 同上。`validity: 上書き済み` であり凍結対象。本文の「スコア境界」への言及は当時の決定内容の記録であって、現在の判定仕様ではない |
| `ADR-202608011354-01`（起票のタイミングと命名規約の ADR 化基準） | 有効 | **変更不要** | 決定内容は起票時期のエスカレーションと命名規約の射程・振り分けであり、閾値の所在にも判定の機械実行にも依存しない。本 Issue の Task 6 は命名規約節の射程 3 類型と適用順を改訂前後で同一に保つため、同 ADR の反転は生じない |
| `ADR-202608061516-01`（配布境界の実行不能性テスト） | 有効 | **変更不要** | 第3の判断軸を据える決定であり、削除対象への参照を持たない。本 Issue の内製化はむしろ同軸の適用結果（判定手順が評価不能にならないよう定義を配布物側へ入れる）と同じ向きであり、決定を反転しない |
| `ADR-202607261459-01`・`ADR-202607261501-01`・`ADR-202607261503-01`（ADR 状態モデルと drift-lint、編集機構、ライフサイクルのツール委譲） | 有効 | **変更不要** | いずれも drift-lint の 5 レイヤ・編集判定・遷移操作を主題とし、本 Issue はこれらの検査内容・発火条件に触れない（設計 spec「決定事項」2 が明示） |
| `ADR-202608011351-01`・`ADR-202608011353-01`（格下げ判定の独立基準と入力の採否） | 有効 | **変更不要** | 既に記録された ADR の退役判定を扱い、実体は `adr-demotion.md` に在る。本 Issue が触れるのは起票前段の判定条文 `adr-scoping.md` であり、`adr-scoping.md` 自身が両者は判定項目・境界・安全側を共有しないと規定する |

**遷移（上書き・廃止）を要する ADR は 1 件も無い。** したがって既存 ADR ファイルへの編集は発生せず、`docs/adr/` に生じる差分は §1 で起票した新規 ADR とその index 再生成分に限られる。

### 完了条件との対応

Issue #783 AC6 が名指しで根拠を求める 2 件は、上表のうち `ADR-202607230648-01`（例示引用のみ）と `ADR-202608011651-01`（閾値の内製化を自ら反転から除外）である。いずれも変更不要と判定した根拠を上表に記した。

## §3: 検証の記録

### 参照残存の走査（AC2）

削除対象を指すパス断片 5 語（`adr-scoping-cases` / `adr-judgment-contract` / `adr-scoring-thresholds` / `migrate-return-schema` / `return-schema-migration`）で追跡下全体を走査した結果、ヒットしたファイルは 10 件で、いずれも下記の除外集合に属する。これ以外のパスにヒットは 1 件も出ていない。

**Issue #783 AC2 が固定する除外集合 (a)〜(f)**

- **(a) 凍結記録**: `docs/development/test-execution.md` の §6 採点表（`:185`）と §7 移行対応表（`:199`）。いずれも節の範囲（§6 = `:133`-`:191`、§7 = `:192`-`:217`）の内側にあり、同ファイルの他の節にヒットは無い
- **(b) `docs/development/` 配下の日付付き観測記録**: `docs/development/adr-demotion-cases/2026-08-01-run.md`、`docs/development/adr-destination-branch-measurability-2026-08-23.md`、`docs/development/token-discipline-redesign-2026-08-21.md`
- **(c) 過去の事実として削除対象を例示引用する箇所**: `docs/distribution-boundary.md`、`docs/adr/ADR-202607230648-01-adr-machinery-plugin-extraction.md`
- **(d) 本 PR が追跡下へ入れる設計 spec 2 ファイル**: `docs/superpowers/specs/2026-08-27-adr-plugin-simplification-design.md`、`docs/superpowers/specs/2026-08-27-adr-plugin-simplification-review.md`
- **(e) 本 PR が作成する判定記録ファイル**: `docs/development/adr-judgment-pipeline-removal-2026-08-27.md`（本ファイル）
- **(f) 本 PR が起票する ADR**: **該当なし**。§1 で起票した `ADR-202608280911-01-adr-judgment-quality-without-machine-verification.md` は本文で削除対象パスを名指していないため、5 語走査にヒットしない。§2 のとおり遷移で生まれた後継 ADR も無い

**プラン独自の「広域走査に伴う対象外」(P1)**（AC2 の除外集合ではない）

- `docs/superpowers/specs/2026-08-10-issue-710-bats-drift-design.md`（`:9`・`:10`・`:28`・`:29`）。マージ済み PR #710 の設計時点の事実の記述であり、現在の指示・規約ではない。本ファイルは AC2 の走査対象カテゴリ（規約文書・スキル定義・エージェント定義・テストランナー）のいずれにも当たらず AC2 の充足判定の射程外であるため、(a)〜(f) には含まれない。追跡下全体へ走査を広げたことに伴う対象外として (a)〜(f) と書き分ける

据え置きの確認として、(a)(b)(c)(P1) の各パスに `git diff` の差分が無いことを確かめた。差分を持つのは (c) のうち `docs/distribution-boundary.md` の陳腐化注記のみである（除外集合は「編集しない」ではなく「残存として数えない」を意味する）。

### 全スイートとガードの状態（AC3）

- `bash scripts/run-tests.sh`: 全 6 スイート成功終了（bats 144 tests / 0 failures、`validate-skills` / `validate-plugin-manifests` / `validate-plugin-versions` / `validate-plugin-portability` / `validate-plugin-path-references` はいずれも ok）
- 期待テスト一覧と実ファイル集合の双方向照合、TAP のプラン行と結果件数の照合: いずれも不発火
- 参照台帳の双方向照合: `checked=86 missing=0 stale=0`（照合件数が 0 でないことを含む）
- プラグイン版の検査: 据え置きの報告・2 manifest 間の不一致の報告のいずれも無し（両者 `0.6.0` で一致）
- 配布境界の検査・配布物移植性の検査（`壊れた参照` / `2段参照`）: いずれも不発火
- `bash plugins/adr/scripts/lint-adr.sh docs/adr` を **worktree から明示実行**して exit 0。レイヤ2（index 同期）・レイヤ3（相互参照双方向性）のいずれも発火せず。adr プラグイン同梱ゲートは検査対象を `CLAUDE_PROJECT_DIR` 優先・cwd フォールバックのみで解決し worktree では別ツリーを検査しうるため、その自動実行は充足判定に使っていない

### 残存スイートの skip 使用有無

`scripts/tests/` 配下を 1 回 grep した結果、bats の `skip` コマンドの呼び出しは **0 件**である。ヒットした 4 箇所はいずれも既知の偽陽性で、内訳は次のとおり。

| 箇所 | 内容 |
|---|---|
| `scripts/tests/lint-adr-stem.bats:225` | fixture 名 `07-legacy-filename-skipped` の一部 |
| `scripts/tests/helpers/common.bash:198` | コメント中の `skip`（bats の `skip` が収集型ケースで使えない旨の説明） |
| `scripts/tests/helpers/common.bash:199` | 関数定義 `collect_skipped` |
| `scripts/tests/helpers/common.bash:201` | 出力文字列 `# skipped:` |

`collect_skipped` は判定パイプラインの削除により呼び出し元が 0 件になったが、残置している。本 Issue の射程が判定を機械実行する一式の削除であって残存ヘルパーの未使用関数の整理を含まないこと、同関数が削除対象に固有ではない汎用機構であること、未使用関数を検出する検査が存在せず残置が commit ゲートを赤にしないこと、および「単純化の名目で既存のケースを削除しない」ガードレールによる。Issue の制約に従い、常設の skip 計数機構は追加していない。

### 前提の消える OPEN Issue 15 件の処理（AC5）

| 区分 | 件数 | 対象 | 結果 |
|---|---|---|---|
| クローズ | 8 | #604 / #606 / #607 / #608 / #724 / #727 / #747 / #766 | 各件に本 Issue（#783）へのリンクを含むコメントを添えたうえで not planned でクローズ。全件 `state=CLOSED` / `stateReason=NOT_PLANNED` を照合済み |
| 再スコープ | 6 | #718 / #720 / #745 / #722 / #719 / #750 | 新本文のドラフトを提示し、**適用前にユーザーの承認を得てから** `gh issue edit` を実行。適用後に本文を再取得し、受入条件節から廃止対象の機構への依存（題材の再導出・独立 2 試行・欠陥台帳の更新・検査器サブコマンドとの同期・返却フィールドの改名・凍結記録との互換経路）が消えていること、および指摘していた条文欠陥そのものが残っていることを 1 件ずつ照合済み |
| 変更なし | 1 | #700 | 状態・本文とも無操作。`state=OPEN`、`updatedAt` は着手前の 2026-08-08 のまま |

**#719 / #750 は当初クローズしたものを再オープンして再スコープへ移した。** PR #784 のレビューで、両件が指す欠陥が削除された機構ではなく判定手続きの規定そのものであり、内製化後の `adr-scoping.md` へ存続していることが指摘されたためである。

- **#719**（項目3 の点数の時点依存）: 差し替え対象の 1 文「採用理由が確かめられない場合、条件1・2は不成立とし、条件3は理由によらず判定する。」が「判定する入力の値域と定義」へ逐語で存続する。廃止された機構に依存していたのは受入条件の後半（判定器フィールドの改名・凍結記録との互換経路・題材の再走行）であり、条文の改訂を求める前半は縮んでいない。本 Issue の本文が持つ決着済みの条文設計は、根拠メモ（git 管理外）が失われているため唯一の記録でもある
- **#750**（第2分岐の入口の述語の操作的定義）: 述語「複数の作業者・複数の箇所へ効かせたい規範」は「判定結果の行き先」節に存続する一方、操作的定義は改訂前から無く、参照していた返却フィールドの消滅で手掛かりはむしろ減っている

いずれも #718 / #720 / #745 / #722 と同一の判断基準（受入条件から機構依存を外し、指摘している欠陥は縮めない）で扱った。あわせて #718 の OUT が持っていた「必要なら再起票する」という未決の記述を、再オープン済みの事実を述べる記述へ置き換えた。

再スコープ 4 件で残した条文欠陥は次のとおり。いずれも改訂後の `adr-scoping.md` に現存することを確認した。

- **#718**: A-01（「判定結果の行き先」節の「ADR 化しない（合計3点未満）の行き先」の第2分岐が中黒で並べた 2 条件について選言か連言かを明示しない）／A-03（検査が皆無の場合にどの分岐へ置くかを明示しない。同節末尾の「強制する資産はあるが検査が警告どまりで阻止しない規範」の段落が警告どまりだけを扱う）
- **#745**: A-04（第1分岐が入口をツール強制の有無で述べる一方、置き場所を資産の種類で述べるため二読みになる）
- **#720**: 「命名規約の ADR 化基準」節の 3 欠陥（類の外延が未定義／タイブレーカーが表自身の例と矛盾／「ツールで自動強制できる」が節内で二時制）と、`ADR-202608011354-01` 決定2 との drift 1 件
- **#722**: 正本の重複・責務の混在・規範と例の混在の 3 種、規約文書を持たない lint 検査 3 件、`SKILL.md` のレイヤ第二列挙面

なお #718 と #722 については、判定パイプラインの廃止に伴い参照先が変わった箇所を追随させている（値域定義の所在が `adr-judgment-contract.md` から `adr-scoping.md` の同一ファイル内へ移ったこと、#722 の参照面が `.md` 9 本 ＋ `.json` 1 本から `.md` 8 本へ縮んだこと）。#720 と #722 が引用していた検討メモ 2 件（`adr-scoping-case-reduction-2026-08-09.md`・`adr-operation-audit-2026-08-09.md`）は履歴を含めリポジトリに存在しないため、ファイル名の直接引用のみを落とし、主張は本文へ温存した。本 Issue の射程外の既存問題である。

### 削除した判定契約の「判定ごとの必須ルール」の帰趨

`adr-judgment-contract.md` は自らの節を 3 分類し、末尾（`:122`）で「(a) 実測事実の定義と (c) 必須ルールは、判定主体を問わず適用する。(b) JSON 直列化は判定器から保存者など機械が読む境界だけに適用する」と適用範囲を宣言していた。設計 spec と Issue #783 は同ファイルを「前半（値域と定義）と後半（JSON 直列化契約）の二重構造」と記述しており、この二分では (c) が分類の外へ落ちる。内製化にあたり (c) の 5 規定を 1 件ずつ分類し直した結果は次のとおりである。

| 原文（`adr-judgment-contract.md:116`-`:120`） | 分類 | 帰趨 |
|---|---|---|
| 必要条件の成否を必ず申告し、偽なら項目の点数を算出せず ADR 化しない | (a) 相当（既存） | 「成立しなければ点数を数えず、ADR 化しない」が `adr-scoping.md`「必要条件」節に逐語で存続。同規定のうち欠測語・`欠測` 辞書に関する後半は (b) として落とす |
| 発見型短絡の適用と理由、省略した項目を申告する | (c) | **内製化した**（`adr-scoping.md`「判定に添える申告」）。短絡は全項目判定の例外であり、申告が無ければ省略と判定漏れを区別できない |
| `adr-scoping.md` の同点処理に従い、迷った項目は0点へ倒す | (a) 相当（既存） | 同点処理として `adr-scoping.md`「粒度判定基準」「各項目の判定手続き」に存続。判定契約側は参照にすぎない |
| 題材に無い事実を推定で補わず、補えなかった事実を申告する | (c) | **内製化した**（同上）。配布物の内側にあった唯一の推測禁止規定であり、「迷う場合は0点」は迷いを0点へ倒す規定であって事実を推測で埋めることを禁じる規定ではないため、等価物として代替できない |
| 条文に無かった／返却に無い事実は 0 や偽で補わず算出対象外とする | (b) | **落とす**。「返却」「`欠測` 辞書」という機械可読フィールドの充填規則であり、判定器と保存者の境界に固有である。生きた決定を判定する経路では、確かめられない事実は上記「推定で補わない」の申告と同点処理で処理される。過去版の条文を遡って判定する `当時未施行` の場面は削除した corpus に固有であった |

あわせて (a) の内製化で「列挙と件数」が「件数」へ縮んでいた点を戻した（`adr-scoping.md`「判定する入力の値域と定義」の項目1・項目2、および「判定に添える申告」の第1項）。除外規則の適用可否を第三者が照合する材料の要求にあたるため、除外規則だけを逐語で残す形は取らない。

この分類と内製化は、`ADR-202608280911-01` が受容したコスト（機械検査の喪失）とは別事象である。同 ADR は判定の品質担保を機械検査に置かないことを決めたのであって、判定主体へ掛かる手続き規律を落とすことは決めていない。

### #694 の受入条件 AC3（AC5 の処理 15 件の外）

OPEN Issue #694「サブエージェントへの CLAUDE.md 明示読み取りの指示を発生源ごと絶つ」の AC3 は「`docs/development/adr-scoping-cases/README.md` の委譲時のプロンプトに含める項目から、CLAUDE.md を読ませる項目が取り除かれている」であり、本 PR による同ファイルの削除で検証対象を失い、無条件に真となる。

同 Issue は AC5 の処理対象 15 件（クローズ 10・再スコープ 4・変更なし 1）に含めていなかった。前提が消えるのは AC3 の 1 項目のみで、Issue 全体（発生源であるユーザースコープ `~/.claude/CLAUDE.md` の条文撤回、および本リポジトリ全体の再走査）の前提は消えないためである。ただし AC3 が空虚に真のまま Ready 判定へ流れることを避けるため、本記録へ 1 行として残す。

## §4: 射程外・follow-up 候補

本 PR では対処せず、恒久対処を別 Issue へ切り出した事象が 3 件ある。いずれも PR #784 のレビュー時点では未起票であり、レビューを受けてマージ前に起票した（#785 / #786 / #787）。

1. **`adr-commit-gate` の worktree 盲点**（#785）: `plugins/adr/hooks/adr-commit-gate:40` は検査対象を `PROJECT_DIR="${CLAUDE_PROJECT_DIR:-$(pwd)}"` でのみ解決し、`scripts/hooks/pre-commit-gate.sh:49`-`:71` が受けた 3 候補解決（PreToolUse の JSON が載せる cwd → フックプロセス自身の cwd → `CLAUDE_PROJECT_DIR`）の書き換えを受けていない。このため worktree から commit すると、コミット対象ではない project root の `docs/adr` を検査して緑を返しうる。本 PR では `lint-adr.sh` の明示実行で回避しており、ゲート自体は修正していない（本 Issue の射程は判定パイプラインの廃止と条文への内製化であり、ゲートの対象解決は別事象）
2. **削除に伴って失われる汎用ガード 1 件**（#786）: `scripts/tests/adr-scoping-cases-edge.bats` のアサーション44「配布物（`plugins/adr` 配下）が配布元リポジトリの Issue 番号を含まない」は、被テスト検査器 1 本に閉じず `plugins/adr` 配下全体を走査する汎用ガードだった。同 bats の削除により、配布物へ配布元リポジトリの Issue 番号が混入することを検出する検査はリポジトリから消えている。`scripts/tests/distribution-boundary.bats` の面② が覆う走査語彙 5 語に Issue 番号は含まれず、代替にならない。削除自体は AC1 が命じるため不可避であり、Issue の制約「新たな検査機構を追加しない」に従い代替ガードは新設していない。本 PR 自身が `plugins/adr/skills/manage-adr/references/adr-scoping.md` と `plugins/adr/README.md` を編集する無検査化の初回の当事者にあたるため、両ファイルに Issue 番号を書き込んでいないことを個別に確認した
3. **行番号固定台帳の是非**（#787）: 設計 spec の射程外節が「扱うなら別 Issue として起票する」と明記している事象。本 PR の射程外である

