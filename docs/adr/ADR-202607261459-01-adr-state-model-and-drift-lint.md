---
status: 承認済み
validity: 廃止済み
superseded-by:
---

# ADR-202607261459-01: ADR の状態モデル（承認軸と有効性軸の2軸分離・導出ビュー index）と drift-lint

## Context

本 ADR は ADR-202607112231-01-adr-two-axis-status-validity-model（以下、原 ADR）の分割により生じた後継のひとつである。決定内容は原 ADR で承認済みのものを逐語 restate したものであり、内容の再決定ではない。

### 分割の経緯

原 ADR は8つの決定を束ねる多決定 ADR だった。そのうち決定8「採番方式・配置」の採番方式項が ID 形式の単一化（#550）で core 反転したため、原 ADR 決定3 が定める多決定 ADR の部分 core 反転手続き（ADR分割）に従い、生存決定を独立に反転しうるクラスタごとの後継へ逐語 restate し、原 ADR を `上書き済み` へ退役させた。

後継は6本である。本 ADR は原 ADR の**決定1（2軸分離）・決定2（front-matter 権威と導出ビュー index）・決定5（drift-lint 3レイヤと合流先）**を担う。この3決定を一体のクラスタとしたのは、決定2 の front-matter スキーマ必須ルール表が決定1 の値集合を各行に持ち、決定5 のレイヤ1（スキーマ検証）・レイヤ2（index 同期）が front-matter 権威と index 導出を前提に定義されているためである。いずれか1つの反転は残り2つの決定本文の書き換えを強制する。残る決定は ADR-202607261501-01（決定3・4）、ADR-202607261503-01（決定6）、ADR-202607261503-02（決定7）、ADR-202607261503-03（決定8 の配置項）が担い、反転した採番方式は ADR-202607261504-01 が新決定として置き換える。

### 原 ADR からの改変箇所と理由（開示）

1. **見出しの先頭番号の振り直し**: 原 ADR の `### 1.` / `### 2.` / `### 5.` を本 ADR では `### 1.` / `### 2.` / `### 3.` とした。番号は原 ADR 内での決定の並び順を示すものであり、後継では自身の決定集合における位置へ振り直す。見出しタイトルと決定本文は、分割の時点では変更していない（分割後に本 ADR へ加えた編集は `## 変更履歴` を参照）。
2. **`## Consequences` の取捨**: 本 ADR が担う決定1・2・5 に対応する記述のみを原 ADR から保持した（得られる利益2項・受容したトレードオフ1項・将来の留保事項3項）。複数の決定にまたがる項目は文を切らず全文のまま保持したため、同じ文が他の後継にも現れる。配分の全体像は分割を実施した #570 が持つ。

### 決定本文に残る原 ADR 内の決定番号（読み替え）

分割時の逐語 restate の不変条件により、決定本文・帰結記述は原 ADR から改変せずに移した（分割後に加えた編集は `## 変更履歴` を参照）。このため本文中の決定番号は原 ADR での番号のままである。読み替えは次のとおり。

- 本 ADR の決定2 が引く「決定3（多決定 ADR の部分 core 反転（ADR分割））」と決定3 が引く「決定4（Amend 機構の廃止）」はいずれも原 ADR の決定であり、分割後は ADR-202607261501-01 の決定1 および決定2 が担う
- `## Consequences`「将来の留保事項」の機構の実装に関する項がいう「決定5 本文」は本 ADR の決定3、「2026-07-21 変更履歴」は原 ADR の `## 変更履歴` を指す（本 ADR の `## 変更履歴` 節は分割後の非core 編集で新設したものであり、原 ADR のそれとは別物である）

## Decision

### 1. Status を承認軸と有効性軸の2軸に分離する

1 つの `Status` 欄に混在していた 2 軸を、別フィールドに分離する。値は日本語のユビキタス言語で確定する。

- **承認軸（不変・承認の歴史記録）**: `提案中` / `承認済み` / `却下`
    - `提案中`: 起案され、承認者の判断を待つ
    - `承認済み`: 承認された履歴事実。以後不変
    - `却下`: 提案が承認に至らず終了。一度も運用されない終端
- **有効性軸（可変・現在の効力）**: `有効` / `上書き済み` / `廃止済み`
    - `有効`: 現在運用中
    - `上書き済み`: 後継 ADR に置き換えられた（後継識別子を保持）
    - `廃止済み`: 後継 ADR なしで ADR としての効力を終えた。決定そのものを放棄した場合と、記録の正本が ADR 外の資産へ移った場合の両方を含む。いずれの場合も、この ADR を現在の効力の判断材料にしない

`却下`（承認軸: 一度も承認されなかった終端）と `上書き済み` / `廃止済み`（有効性軸: 承認後に ADR としての効力を失った状態）を別語とし、2 軸が語彙レベルで衝突しないようにする。これは ADR-202605110956-01 のライフサイクル状態（`Proposed / Accepted / Deprecated / Superseded` の英文4状態）と、`docs/development/event-storming.md` の「状態名を英文表記で確定」という決定の両方を置き換える。

### 2. front-matter を唯一の権威とし、有効 ADR index は導出ビューとする

状態系は各 ADR ファイル冒頭の front-matter に置く。キーは英語（構造的フィールド名）、値は上記の日本語ユビキタス言語とする。

```yaml
---
status: 承認済み          # 承認軸・不変（提案中 / 承認済み / 却下）
validity: 有効            # 有効性軸・可変（有効 / 上書き済み / 廃止済み）
superseded-by: ADR-...   # validity=上書き済み のとき必須（分割時はリスト値）
---
```

front-matter を単一の権威とし、有効 ADR index（`docs/adr/index.md`）はそこから機械生成する純粋な導出ビューとする。index は `validity: 有効` の ADR のみを列挙し、ファイル全体が生成物（build artifact）であって人手編集しない。これにより「本文を読めば確実（確実性）」と「一覧で探せる（検索性）」を drift させずに両立する。

front-matter スキーマの必須ルール（drift-lint が強制する）:

| 遷移 | status | validity | superseded-by |
|---|---|---|---|
| 起票 | 提案中 | （無し） | （無し） |
| 承認 | 承認済み | 有効 | （無し） |
| 上書き | 承認済み（不変） | 上書き済み | 必須 |
| 廃止 | 承認済み（不変） | 廃止済み | （無し） |
| 却下 | 却下 | （無し） | （無し） |

`superseded-by` は単一後継を既定とするが、1本の ADR が複数の後継へ分割される場合（決定3「多決定 ADR の部分 core 反転（ADR分割）」）はリスト値を取りうる。この 1→N のケースでは、`validity: 上書き済み` の原 ADR が全後継を列挙し、各後継が本文 `## 関連ADR` に逆参照（`Supersedes:`）を持つ（drift-lint レイヤ3 が N 対 1 で双方向性を検査する）。

キーを英語としたのは、キーが状態概念そのものではなく構造的なフィールド名であり、英語キーでも概念の二重管理による drift を生まないためである（ユビキタス言語の本体は値側にある）。冒頭で `status` と `validity` が隣接することで「承認済み」を「有効」と誤読する罠も防ぐ。生成入力を本文見出しの正規表現抽出ではなく front-matter の key-value 解析としたのは、後者のほうが堅牢なためである。

### 3. drift-lint の検査契約と合流先

ADR corpus が満たすべき機械検査可能な不変条件は、CI 不在下でも実行できる drift-lint で担保する。有効性の同期（「index を再生成して一致するか」）はその中心にあり、検査契約は以下を最低限含む（列挙は実装時点のレイヤ構成の網羅ではなく下限であり、同種の不変条件を対象とするレイヤの追加により拡張されうる）:

- **レイヤ1（front-matter スキーマ検証）**: 上記スキーマ必須ルールを満たすこと。
- **レイヤ2（index 同期）**: `docs/adr/index.md` を再生成し、コミット済みと差分ゼロであること。
- **レイヤ3（相互参照双方向性）**: 「`A.superseded-by` が B を列挙する」⟺「B 本文 `## 関連ADR` に `Supersedes: A` が存在する」の双方向を検査すること（分割による 1→N では A が複数の後継を列挙し、各後継が A を逆参照する N 対 1 を含む）。前方向（A の列挙に対する B の逆参照欠落）だけでなく、逆方向——B が `Supersedes: A` を宣言するのに A の `superseded-by` に B が無い——の検出も要件に含む。corpus に残存する `Amends:` / `Amended by:` 関係は検査対象外とする（決定4 により生存語彙へ移行のうえ corpus から除去されるため、移行完了後は該当関係が存在しない）。

合流先は独立プラグイン `adr` 同梱の `lint-adr.sh`（`plugins/adr/scripts/lint-adr.sh`）として実体化し、同プラグインの PreToolUse フック（`git commit` 前ゲート。導入先に `docs/adr` がある場合のみ発火し、無い場合は no-op）から呼ぶ。本リポでは `.claude/settings.json` の `enabledPlugins` へ `adr` を登録して常時発火させる。人間も手動実行できる二面構成とする。CI 設定ファイル（`.github`）は基盤不在のため作らない。

## Consequences

**得られる利益**:

- 記憶を持たないエージェントが「今どれが有効な ADR か」を単一の導出ビュー（`docs/adr/index.md`）から安価に判別でき、承認の歴史記録（承認軸）が不変に保たれる。
- 有効性の同期を drift-lint で機械的に担保できるため、CI 不在の制約が本質的問題でなくなる。

**受容したトレードオフ**:

- front-matter への移行が完了するまで、既存 ADR の `## Status` 見出し形式と新スキーマが混在する過渡状態を受容した（本 ADR 自身では解消せず移行フェーズ3へ委ねた）。この過渡状態は、フェーズ3の front-matter 一括移行（#476）と `## Status` 節の遡及除去（#489→#514）の完了により corpus 全体で解消済み。

**将来の留保事項（本 ADR のスコープ外＝ downstream）**:

- 機構の実装（`scripts/lint-adr.sh` と PreToolUse フック合流、`docs/adr/index.md` 生成、`docs/adr/README.md` 廃止手順・`template.md` の改訂、ライフサイクル操作スキル）は実装 Issue に委ねる。この機構は実装済み。drift-lint はその後 ADR 運用機構のプラグイン抽出（#492/#493）で独立プラグイン `adr` 同梱の `plugins/adr/scripts/lint-adr.sh`＋同プラグインの PreToolUse ゲートへ移った（現配置は決定5 本文と原 ADR の 2026-07-21 変更履歴を参照。ここでの `scripts/lint-adr.sh` は起票当時に想定した合流先を指す）。
- 移行（全 ADR への front-matter 付与、`ADR-202605312147-01` の Superseded 実例の validity 移設、既存 `Amends:` / `Amended by:` 関係の生存語彙への移行（live は `Supersedes:` / `Superseded by:` / `Related:` / `## 変更履歴` へ移し、retired は後継の supersede 連鎖へ吸収して Amend 語彙を corpus から除去する。凍結して grandfather 固定はしない。取りこぼしを防ぐため移行対象はファイル数でなくエッジ単位で列挙し、1ファイルが複数の `Amends:` を宣言するケースを含める）、英語状態名から日本語ユビキタス言語への移行、`docs/development/event-storming.md`「技術的意思決定」集約の状態機械の作り替え〔英文4状態→2軸・`却下` 終端追加〕、index 初期生成）は Issue #463 フェーズ3に委ねる。
- 本 ADR および ADR-202605110956-01 の front-matter 化もフェーズ3の一括移行に含めた（起票時点では両 ADR とも `## Status` 形式だった）。両 ADR とも #476 で front-matter 化済み。

**退役（2026-07-30）**: 本 ADR は `validity: 廃止済み` へ退役した。決定内容（2軸分離・front-matter 権威と導出ビュー index・drift-lint の検査契約）を lint-adr.sh のレイヤ1・レイヤ2 が現に検出して commit を阻止しており、現行の ADR 化要否基準では ADR として保持する要件を満たさない。決定を放棄したのではなく、記録の正本を ADR 外の資産へ移した移管による退役である。規定そのものは `adr` プラグインの `manage-adr` スキル（状態モデルと遷移手順の参照ファイル）と同梱スクリプトが担い、採用理由の移送先は `plugins/adr/skills/manage-adr/references/template.md`（2軸を別フィールドへ分けた理由、1欄混在と英文4状態の却下、キー英語・値日本語の理由）、`plugins/adr/scripts/gen-adr-index.sh`（生成入力を本文見出しの正規表現抽出とせず front-matter の key-value 解析とした理由、index を導出ビューへ閉じ込めた理由）、`plugins/adr/scripts/lint-adr.sh`（語彙定数の近傍に置いた、2軸が語彙レベルで衝突しないようにした理由）であり、2026-07-30 時点でいずれも当該箇所へ併記した。なお本 ADR の退役により、原 ADR ADR-202607112231-01 の決定1・2・5 の系譜は `docs/adr/index.md` からは辿れなくなる。系譜は本 ADR `## 関連ADR` の `Supersedes:` 行と原 ADR の front-matter `superseded-by` が引き続き保持する。

## 関連ADR

- Supersedes: ADR-202607112231-01-adr-two-axis-status-validity-model（ADR分割〔ADR-202607261501-01 決定1〕により、原 ADR の決定1・2・5 を逐語 restate で re-home。原 ADR の決定3・4 は ADR-202607261501-01、決定6 は ADR-202607261503-01、決定7 は ADR-202607261503-02、決定8 の配置項は ADR-202607261503-03 が担い、決定8 の採番方式項は ADR-202607261504-01 が反転した。原 ADR の `superseded-by` はこれら6本の後継を列挙する）
- 関連Issue: #463（原 ADR の起票元）, #130（技術的意思決定集約モデル化）, #462（外部監査の見取り図）, #484（決定2 のリスト値許容・決定5 のレイヤ3 N対1 拡張の起票元）, #570（ADR分割による re-home）

## 変更履歴

- 2026-07-27: 決定3 の検査契約からレイヤ数を数える表現（見出しの「3レイヤ」とリード文の「検査契約は3レイヤ」）を除き、列挙が網羅ではなく下限であって同種の不変条件を対象とするレイヤの追加により拡張されうる旨を明記した。レイヤ4（Related / 保留した決定 欄の参照の生存性・実在性）とレイヤ5（ファイル名形式・識別子重複・H1 整合）の追加のたびに本記述が実態から乖離し、ADR を追随させるかの判断が再発していたため、実装を追いかける代わりに記述の側を開いて陳腐化の源を断った。あわせて検査契約の枠付けを「有効性の同期」から「ADR corpus が満たすべき機械検査可能な不変条件」へ改め、有効性の同期をその中心と位置づけた。追加された2レイヤはいずれも有効性や index の同期とは独立した corpus の健全性検査であり、枠付けを元のまま下限だけ開くと、実態とのずれを追認したうえで以後の任意の追加を無条件に事前承認することになって決定が反証されなくなる。「機械検査可能な corpus 不変条件か」という資格要件を与えることで、レイヤ追加の可否をレイヤ数の追随ではなく資格判定として閉じられるようにした。レイヤ1〜3 の各項目の記述と合流先の記述は不変。あわせて、決定本文が分割時から不変であることを前提していた `## Context` の記述2箇所（原 ADR からの改変箇所の開示、および本 ADR が `## 変更履歴` 節を持たない旨の但し書き）と、本節の新設により file-local な誤読先を持った `## Consequences`「将来の留保事項」の「2026-07-21 変更履歴」への参照を、本編集後の実態へ整合させた。適用範囲に触れる非core 編集（#596）。
- 2026-07-30: 決定1 の有効性軸 `廃止済み` の定義を「後継なしで放棄された」から「後継 ADR なしで ADR としての効力を終えた」へ改め、決定そのものを放棄した場合と記録の正本が ADR 外の資産へ移った場合の両方を収容する状態として定義し直した。あわせて退役2値をまとめて説明する記述を「承認後に効力を失った状態」から「承認後に ADR としての効力を失った状態」へ改めた。既存 ADR の適格性監査で現行の ADR 化要否基準に照らして非該当となる ADR が検出され、これらを「決定が覆ったのではなく、採用理由の正本が実装・テスト・共有規約文書へ移った」状態で退役させる用途が生じたためである。退役2値を分けている軸は「後継 ADR の有無」であって「決定が覆ったか」ではないため、移管による退役を `廃止済み` へ入れることは軸の設計と整合し、ずれていたのは定義文の語が効力そのものの喪失を含意している点だけだった。新しい有効性値を立てる案は lint の値域・遷移表・違反種別・参照生存性検査、index 生成、テスト、fixture の全てへ波及するため却下し、値域は `有効` / `上書き済み` / `廃止済み` の3値を維持した。受け入れた犠牲として、「廃止」という語が放棄を含意するずれは値の改名を行わないため残る。有効性軸の値集合・遷移ごとの front-matter 必須ルール・決定3 の機械検査の契約はいずれも不変。適用範囲に触れる非core 編集（#625）。
