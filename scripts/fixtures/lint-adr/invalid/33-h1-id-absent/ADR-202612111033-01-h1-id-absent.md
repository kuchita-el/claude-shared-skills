---
status: 承認済み
validity: 有効
---
# 識別子を欠く決定見出し: H1 に ADR 識別子が現れない決定

## Context

fixture 用（invalid/33）。H1 見出しは存在するが、そこに ADR 識別子が1つも現れない。27-h1-id-mismatch が「H1 の識別子部がファイル名と食い違う」経路を担うのに対し、本ファイルは「H1 に識別子部がそもそも無い」経路を担う。両者は lint-adr.sh の別々の分岐（不一致 / 抽出結果が空）で報告される。

`gen-adr-index.sh` は H1 の `: ` 以降のみをタイトルとして抽出するため、識別子を欠く H1 でも index は正常に生成される。この不整合はレイヤ2 には現れず、レイヤ5 の H1 整合検査だけが検出する。

## Decision

fixture 用のため実質的な決定内容は無い。

## Consequences

レイヤ5 が `H1 整合違反（本文の最初の "# " 見出しに ADR 識別子が見つかりません...）` を報告し exit 1 になる。
