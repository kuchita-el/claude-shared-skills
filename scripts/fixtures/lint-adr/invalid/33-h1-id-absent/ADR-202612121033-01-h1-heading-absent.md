---
status: 提案中
---

## Context

fixture 用（invalid/33）。**このファイルは H1 見出しを1つも持たない。** 条文は「H1 に識別子が現れない」に見出しを持たない場合を含めており、実装も抽出結果が空であることを一様の判定材料にしている。H1 行を追加すると本ファイルが担う経路が消える。

同居する ADR-202612111033-01-h1-id-absent が「H1 はあるが識別子を欠く」側を担い、本ファイルが「H1 そのものが無い」側を担う。両者は同じ違反メッセージへ合流するが、`extract_h1_adr_id` の到達経路が異なる（前者は `# ` 行に当たって識別子抽出に失敗し、後者は `# ` 行に一度も当たらずループを抜ける）。

`status: 提案中` であり有効性軸を持たないため index には採録されず、レイヤ2 は本ファイルについて発火しない。superseded-by も持たないためレイヤ3 も発火しない。ファイル名は規約に適合しており、レイヤ5 の形式検査・識別子重複検査も発火しない。

## Decision

fixture 用のため実質的な決定内容は無い。
