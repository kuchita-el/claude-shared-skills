---
status: 承認済み
validity: 有効
---
# ADR-202612091009-01: ADR- 接頭辞を欠く誤名ADR

## Status

承認済み

## Context

fixture 用（invalid/29）。front-matter もセクション構成も規約に適合しているが、ファイル名が `ADR-` 接頭辞を欠く。走査対象の収集は `ADR-*.md` グロブであり、このファイルはそもそも収集されないため、レイヤ1〜4 は素通りする。`gen-adr-index.sh` も同じグロブのため index にも現れず、レイヤ2 も発火しない。

## Decision

fixture 用のため実質的な決定内容は無い。

## Consequences

レイヤ5 がファイル名形式違反として報告し exit 1 になる。
