---
status: 承認済み
validity: 上書き済み
superseded-by: ADR-202702031023-01-related-dup-new
---
# ADR-202702021023-01-related-dup-target: 退役（上書き済み）の参照先（dedup 検証用）

## Status

承認済み（上書き済み）

## Context

fixture 用（invalid/23）。source が複数の `Related:` 行から重複して指す退役（上書き済み）ADR。superseded-by と本文の双方向宣言を持つためレイヤ1合法・レイヤ3合法。

## Decision

fixture 用のため実質的な決定内容は無い。

## Consequences

index には現れない（有効でないため）。

## 関連ADR

- Superseded by: ADR-202702031023-01-related-dup-new（レイヤ3双方向 fixture のペア）
