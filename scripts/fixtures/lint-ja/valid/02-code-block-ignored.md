# 正例: コードブロックは検査しない

以下のコードブロックには、本文であれば検出されるはずの内容が入っている。

```bash
# retention の値をそのまま渡す。ファセットの扱いはここでは決めない。
# ADR-202606040737-01 は読み込みの優先順位を定める。
echo "この行は句点を含まない長い行であり、本文であれば一文の長さの上限を超えているはずの分量をわざと持たせてあるが、コードブロックの内側にあるため検査の対象にならない。"
```

コードブロックの外は上限より短い文だけで書く。
