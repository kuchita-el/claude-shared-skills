<!--
このファイルは refine-issue スキルの 1件モード出力テンプレート。
SKILL.md 手順3 において 1件/--input モードのサブエージェントが自身で Read し、refine-prompt.md の {OUTPUT_FORMAT} プレースホルダ位置に適用する形で出力形式として運用する。
末尾の「精査実施: Claude Code」フッタは再精査検出の識別子として必須（refine-prompt.md「再精査時の注意」が依拠）。
このコメントブロックを除く以下の本文をテンプレートとして遵守すること。
-->

以下の形式で精査結果を出力してください。該当しないセクションは省略してよい（ただし「DoRチェック結果」の項目表は全DoR項目を必ず列挙する。一部項目の省略は早期収束につながるため禁止）。

## Issue精査結果: #{issue_number}

### 概要

[Issueの要約を1-2文で]

### DoRチェック結果

**サイズ**: Small / Medium / Large
**種別**: bug / feature / refactor / spike / chore / docs / -（判定不能時は `-`）
**Ready判定**: Ready / Not Ready

| 項目 | 結果 |
| ---- | ---- |
| 課題が明確 | ✅ |
| ... | ⚠️ — [補足内容] → Q{n} |
| ... | ⚠️ — [補足内容] → Q{n}、AC改善提案 |
| ... | ❌ — [補足内容] → AC改善提案 |
| ... | ⚠️ — [補足内容] → 助言A{n} |

各 ⚠️/❌ 行には `— [補足内容]` をインラインで付記し、対応する詳細セクションへのクロス参照（`→ Q{n}`、`→ AC改善提案`、`→ 助言A{n}`）を末尾に添える。複数セクションを参照する場合は `→ Q1、AC改善提案` のようにカンマ区切りで列挙する。✅ 行にも有益な補足があればインラインで付記してよい（クロス参照は不要）。

### 未解決論点（要決定 / 要確認）

リファイン段階のブロッカーが存在しない場合は本セクション自体を省略する（空リストは出力しない）。
各項目は **Q{n}** の番号で識別する（DoRチェック結果のクロス参照と対応）。

**Q1. [論点タイトル]**（要決定 / 要確認）
- [確認または決定が必要な内容]
- （決定論点の場合）選択肢: A … / B … / 推奨: A — [理由]

### 受入条件の改善提案

検証不可・主観的な受入条件が存在しない場合は本セクション自体を省略する（空テーブルは出力しない）。
判定基準はDoR定義「受入条件の検証可能性・客観性 判定基準」節を参照する。
書き換え案は観測可能な事象・定量閾値・合否境界のいずれかを含む文言とする。
元の意図が読み取れない場合は書き換え案を出さず「未解決論点（要決定 / 要確認）」セクションで原意を質問する。

| 元の文言 | 問題 | 書き換え案 | 補足理由 |
| -------- | ---- | ---------- | -------- |
| [AC原文] | 検証不可 / 主観的 / 両方 | [改善後の文言] | [なぜこの書き換えが妥当か] |

### 助言（非ブロッカー、Ready 判定に影響しない）

タイトル・ゴール・ACに指定的HOWの混入が無い場合、または種別が完全抑制対象（refactor / chore / spike / docs）の場合は本セクション自体を省略する（空テーブルは出力しない）。
本セクションの内容は Ready/Not Ready 判定に影響しない。
各項目は **A{n}** の番号で識別する（DoRチェック結果のクロス参照と対応）。
判定基準・種別連動は refine-prompt.md「実現方法の混入（HOW混入）の指摘」を参照する。

| # | 対象 | 元の文言 | 提案 |
| - | ---- | -------- | ---- |
| A1 | タイトル / ゴール / AC{n} | [指定的HOWを含む原文] | 成果表現への書き換え案、または「制約 / 参考: 実現の手がかり」欄への移動 |

### スコープ評価

- **規模**: 適切 / 要分割
- **分割案**（該当する場合）:
  1. [サブタスク1の概要]
  2. [サブタスク2の概要]

---

精査実施: Claude Code
